# Decisions — Market & Product

> 시장 정의·BM·서비스 모델·영상 가치 관련 결정. 새 결정은 맨 위에 추가 (최신이 위).

## 2026-09-01 — "우선 처리" 표시 제거

- **결정**: 유료 플랜 혜택에서 "우선 처리"를 앱·랜딩 두 곳 모두 제거한다.
- **이유**: 정찰 결과 코드 근거가 없다. `render/start`의 `plan`은 결제 검증과
  `createRender` 전달에만 쓰이고, `shotstack.ts`의 `plan === "free"` 분기 2곳은
  모두 결과물 내용(폰트·워터마크)이며, Shotstack 호출에 `priority`·`queue`
  파라미터가 없다. `check-rendering`은 `plan` 식별자가 등장하지 않고 스냅샷
  순서대로 순회한다. 심사가 진행 중인 화면에 근거 없는 혜택 표시를 남기는 것은
  지적 소지가 있다.
- **대안 기각**: 실제 우선순위를 구현하는 안 — 심사 중에 렌더 파이프라인을
  건드리는 것이고 현재 필요가 없다(YAGNI).
- **함의**: 유료 대비 혜택은 "워터마크 없음"과 "다운로드 가능" 2개로 줄었다.
  실제 차이가 워터마크이므로 소구력 손실은 미미하다. 향후 우선순위를 실제로
  구현하면 표시를 되살린다.

## 2026-08-24 — 클립 0개 결제 허용

토스 보완 요청(결제창 미비)에 대응해 `payment/prepare`의 클립 0개
차단을 제거했다. 심사자가 영상 업로드 없이 카드결제창을 확인해야
하는데 해당 가드가 orderId·amount 생성을 막아 위젯이 뜨지 않았다.

선택지 3안 중 "가드 제거 + 경고 배너"를 채택했다. 가드만 제거하면
클립 없이 결제한 호스트가 4시간 환불 불가 구간에 갇혀 분쟁 소지가
있고, 심사용 우회 경로를 만드는 안은 심사 목적에 반한다.

금액은 `calcPrice`의 `Math.max(계산값, MIN_PRICE)` 때문에 클립 0개여도
10,000원이 된다. 0원 결제는 구조적으로 발생하지 않는다.

## 2026-08-16 — 재렌더 유료화 사양

> 약관 정렬분(환불 시계·고지 의무·48시간 창)은 `legal.md` 2026-08-16 항목에 있다.

**결정 1: 재렌더 금액 = `round(최초 결제액 × 0.8)`**
- 상태: `payment/prepare`가 rerender mode에서 이 식으로 금액을 산출한다.
- 이유: 최초 결제에 `MIN_PRICE` 10,000원이 이미 걸려 있어, 재렌더 최소 금액 8,000원이 별도 상수 없이 자동으로 도출된다. 재렌더용 하한 상수를 따로 두면 같은 규칙이 두 곳에 생겨 드리프트 원인이 된다.

**결정 2: 80%의 기준은 최초 결제액 고정 — 현재 클립 수 재계산이 아니다**
- 상태: 재렌더 시점의 클립 수로 금액을 다시 계산하지 않는다.
- 이유: 랜딩 pricing 문구가 "처음 금액의 80%"다. 재계산 방식은 이 문구와 어긋나고, 클립을 줄여 재렌더하면 금액이 내려가 재편집을 반복할 유인이 생긴다.

**결정 3: mode를 클라이언트에서 받지 않고 서버가 자동 판정한다**
- 상태: `payment/prepare`가 요청 본문의 `mode`를 읽지 않는다. events 문서의 `status`·`unlocked`로 판정한다.
  - `open` + `!unlocked` → first
  - `closed`·`done` + `unlocked` → rerender
  - 그 외(`rendering` 포함) → 400 `INVALID_EVENT_STATE`
- 이유: 클라이언트가 mode를 보낼 수 있으면 first 결제 건에 rerender를 실어 금액을 20% 깎을 수 있다. 2026-08-06 감사 H-2(unlocked 자기부여)와 같은 계열의 우회다.

**결정 4: 80% 기준값은 `events.firstPaidAmount` 신설 필드에서 읽는다**
- 상태: 최초 결제 confirm 시점에 이 필드를 기록하고, rerender prepare가 읽는다.
- 이유: `payments` 컬렉션 쿼리로 최초 결제액을 찾는 방식은 재렌더 주문이 쌓이면 정렬과 복합 인덱스가 필요해진다. 필드 1개 추가가 비용이 작다.
- 함의: 이 필드는 `8290dbe`부터 기록된다. 그 이전에 결제된 이벤트에는 없어 재렌더가 `MISSING_FIRST_AMOUNT` 400으로 막힌다. 백필 코드는 만들지 않았다(실고객 0, YAGNI).

**결정 5: rerender confirm은 `refund50At`·`refund100At`만 쓴다**
- 상태: rerender mode의 confirm이 `closedAt`·`status`·`unlocked`·`sessionToken`·`firstPaidAmount`를 쓰지 않는다.
- 이유: `closedAt`을 쓰면 cleanup의 클립 7일 시계가 리셋된다. `status`를 쓰면 `done` 이벤트가 `closed`로 강등된다. `firstPaidAmount`를 rerender에서도 쓰면 다음 재렌더가 80%의 80%로 계산된다.

**결정 6: 409 `CLIP_COUNT_CHANGED` 검사는 first에서만 수행한다**
- 상태: rerender mode는 이 검사를 건너뛴다. `AMOUNT_MISMATCH`는 두 mode 공통으로 유지한다.
- 이유: 재편집의 목적 자체가 클립 구성을 바꾸는 것이다. rerender에 이 검사를 두면 정상 요청이 전부 막힌다. `AMOUNT_MISMATCH`는 토스 표준 위변조 방지 절차라 제거 대상이 아니다.

**결정 7: done 화면 재렌더 버튼에 `clips.length > 0` 가드를 건다**
- 상태: 클립이 없으면 버튼을 노출하지 않는다.
- 이유: 클립 만료 후에도 버튼이 보이면, 유료화 이후에는 "돈을 받고 렌더에 실패하는" 경로가 된다. 약관 제14조 ④와 정렬된다.

**결정 8: 클립 토글 비활성은 `status === "rendering"`일 때만 적용한다**
- 상태: `isClosed` 기준을 쓰지 않는다. `closed`·`done`에서는 토글이 열려 있다.
- 이유: 그 상태에서 클립을 고르는 것이 재편집의 목적이다. `isClosed`로 막으면 재편집 기능이 성립하지 않는다.

**결정 9: `render/start`의 `participantNotifiedAt: null` 리셋을 제거한다**
- 상태: 리셋 4건 중 이 한 건만 제거했다. 인접 3개 NotifiedAt과 `refundStatus`는 렌더 진행 알림 축이라 유지한다.
- 이유: 이 리셋이 있으면 재렌더마다 필드가 초기화되고 cron이 새 시각으로 재기록해, cleanup D-1의 클립 48시간 시계가 재렌더할 때마다 연장된다. 2026-08-14에 `closedAt` 축에서 막은 것과 같은 구조가 이 축에 남아 있었다.
- 함의: 부수 효과로 재렌더 시 참가자 알림 재발송이 멈춘다(`check-rendering:131` 가드가 제 역할을 회복한다).

## 2026-08-12 — 결제 흐름 사양 v2 (토스 심사 대응)

**범위 (가′)**: 캡처 최소선 + 마감 시점 서버 금액 산출. 환불 API·정산·재렌더 80% 결제·실키 전환은 범위 밖.

**과금 규칙 (2026-06-13 (7) 재확인)**
- 금액 = `event.maxClipSeconds`(호스트 설정값) × 포함된 클립 수 × 100원, 최소 10,000원
- **길이 항은 설정값 고정. 개별 클립의 `duration`은 금액에 사용하지 않는다** (정찰 보고에서 두 차례 "실측 길이로 바꿔야 한다"는 오판이 나왔으므로 명시)
- 개수 항은 `excludedAt`이 없는 클립 수 (2026-08-12 결정: 제외된 클립은 과금 제외)

**결제 시점: 결제 먼저 → 마감 나중 (ii안)**
- 마감 먼저(i안)는 결제 실패 시 이벤트가 마감된 채 남는 문제가 있어 폐기
- ii안의 약점인 "결제 중 클립 수 변동"은 승인 직전 재검증으로 처리한다 (차단이 아니라 감지 후 재발행)

**흐름**
1. [마감하기] → 확인 모달 → `plan === "paid"`면 결제 경로 분기
2. `POST /api/payment/prepare` — 포함 클립 수 집계 → 금액 산출 → `orderId` 발행 → 주문 정보 저장(`clipCount` 포함)
3. 결제 요약 화면 — 명칭·이미지·상세설명·금액 + 약관 동의 체크박스
4. 토스 결제위젯 `requestPayment` → `successUrl`로 리다이렉트
5. `POST /api/payment/confirm` — **2중 검사**
   (a) 반환 `amount` === 저장 `amount` (토스 공식 표준, 위변조 방지)
   (b) 지금 센 클립 수 === 저장 `clipCount` (우리 추가, 어긋남 감지)
   둘 다 통과 시에만 토스 승인 API 호출
6. 승인 성공 → `unlocked: true` 기록 → close 처리 → 렌더 시작

**(b)가 필요한 이유**: 토스 표준 검사만으로는 클립 수 변동을 못 잡는다. 저장액과 반환액이 둘 다 옛 금액이라 서로 일치하기 때문이다. 토스는 "금액이 조작됐나"를 보지 "금액이 여전히 맞나"를 보지 않는다.

**클립 수 집계 방법**
- `render/start/route.ts:57,63-69` 패턴을 복사한다 (`where("eventId","==",eventId).get()` 후 `.filter(d => !d.data().excludedAt)`)
- `api/clips/route.ts:63`의 `.count().get()`은 **사용 금지** — 필터를 걸 수 없어 제외된 클립까지 센다
- `excludedAt`은 클립 생성 시 미설정이며 falsy로 "포함" 취급된다 (의도된 동작)

**unlocked 필드 재사용**
- `render/start/route.ts:52`가 `plan === "paid" && !unlocked`로 이미 게이트 중
- 결제 성공 시 같은 필드를 켜면 렌더 게이트 코드 변경 불필요
- 2026-08-04 쿠폰 결정의 "결제 승계 설계"가 그대로 성립함을 정찰로 확인

**SDK: v2 확정**
- `@tosspayments/tosspayments-sdk` (2.7.1). v1 `@tosspayments/payment-widget-sdk`는 공식 문서에 "더 이상 업데이트되지 않음" 명시
- 토스가 카드사 심사 가이드에서 안내한 URL(`guides/payment-widget/integration`)은 **v1 문서**다. v2 문서는 `guides/v2/` 경로
- 승인 API: `POST https://api.tosspayments.com/v1/payments/confirm`, `Authorization: Basic {base64(시크릿키 + ":")}`
- `orderId` 규칙: `^[a-zA-Z0-9_-]{6,64}$`

**키 (2026-08-12 실측)**
- 결제위젯 키 접두사는 `gck`/`gsk`. API 개별 연동 키(`ck`/`sk`)와 다르며 혼용 시 `INVALID_API_KEY`
- Rayne 전용 결제위젯 키는 아직 미발급 — 개발자센터에 "내 키는 전자결제 신청하고 확인할 수 있어요" + [이용 신청하기] 버튼 잔존. 별도 신청이 필요한지 심사 완료 시 자동 발급인지 미확인, 담당자 문의 예정
- 현재는 공개 문서 테스트 키(`test_gck_docs_…` / `test_gsk_docs_…`) 사용. 심사 캡처에 지장 없음 (토스 가이드 4p: 테스트 결제창으로도 심사 가능)
- 환경변수: `NEXT_PUBLIC_TOSS_CLIENT_KEY` / `TOSS_SECRET_KEY`. **시크릿 키에 `NEXT_PUBLIC_` 접두사 금지**
- Vercel 설정 실측: Sensitive를 켜면 Development 환경이 잠긴다(값 읽기가 필요한 환경이라 Vercel이 차단). 시크릿 키는 Sensitive + Production/Preview, 로컬은 `.env.local` 직접 기재

**테스트 환경 (공식 문서 확인)**
- 테스트 키는 실제 카드로 승인해도 실제 청구가 없다. 캡처를 실카드로 찍어도 됨
- 테스트 환경 API 요청 제한: 분당 100건

## 2026-08-04 — 베타 쿠폰 해제 (PG 심사 대기 중 유료 기능 임시 개방)

- 배경: Toss PG 심사가 외부 블로커. 결제 없이 실베타 운영이 필요.
- 방식: 전화번호 화이트리스트(Firestore `betaCoupons`). 운영자가 콘솔에서 수동 등록.
- 서버 전권 원칙: 유효 쿠폰이면 클라이언트가 보낸 plan/maxClips/maxClipSeconds를 신뢰하지 않고 서버가 고정값으로 덮어씀. 클라 조작으로 유료 해제가 불가능한 구조.
- 단일 사용: 이벤트 생성 성공 후 쿠폰에 used:true 기록. 확인→쓰기 순차 처리이며 트랜잭션 아님 (동시 요청 race 가능성은 인지하되 운영자 수동 등록 규모에서 무시 가능 — Ray 결정).
- 결제 승계 설계: 렌더 게이트가 `unlocked` 필드를 보므로, 결제 도입 시 결제 성공이 같은 필드를 켜면 게이트 코드 변경 불필요.
- 철수법: ①생성 화면 쿠폰 입력란 제거 ②betaCoupons 컬렉션 비우기. 렌더 게이트는 손대지 않음.
- 관련 커밋: b0e7988

## 2026-06-13 (7) — 가격 모델 전환: 고정 플랜 폐지 → 계산식 단가

직전 2026-05-30 (4)(5)(6) + 2026-05-21 B5 플랜 구조를 개정. 소·중·대 등급 플랜을 폐지하고 사용량 계산식으로 전환.

**무료 플랜**
- 클립 길이 10초 / 클립 수 5개 (기존 10개에서 축소)
- 완성본에 "made by Congre" 워터마크 박힘 (유지)

**유료 플랜 (소·중·대 구분 폐지, 단일 계산식)**
- 결제액 = (호스트 설정 최대 클립 길이 초) × (마감 시 실제 업로드 클립 수) × 100원
- 금액 하한선 1만원 — 계산값이 1만원 미만이면 1만원 청구
- 클립 길이는 호스트가 정한 최대값으로 계산 (개별 클립이 짧아도 최대값 적용)
- 클립 수는 마감 시점 실제 업로드된 수 기준
- 결제 시점 = 이벤트 마감 시
- 클립 길이 상한 = 120초 (시스템 하드 상한)
- 워터마크 없음

**재렌더**
- 매번 유료, 첫 렌더 결제액의 80% (2026-05-30 (5) 유지)

**별도 문의**
- 기업 행사만 상담 트랙. 기존 길이·총시간 한도(30초·15분) 폐기.

**오늘 코드 반영 범위 (가 옵션)**
- plans.ts PLAN_CLIP_LIMITS.free 10→5 + 화면 문구만. 계산식·결제 UI·plans.ts 등급 구조 개편은 결제 트랙(S3-05)에서 일괄.
- 미반영(결제 트랙 이월): 계산식 결제 로직, "마감 시 결제" 화면 문구, 랜딩 4카드→계산식 재설계.

**개정된 옛 결정**
- 2026-05-30 (4) 소/중/대 가격표 → 폐기 (계산식으로 대체)
- 2026-05-30 (6) 유료 10~30초 → 상한 120초로 변경
- 2026-05-21 B5 플랜 구조 표(소50/중200/대5000) → 폐기
- 2026-05-29 (3) 무료 클립 수 10개 → 5개로 변경 (워터마크 정책은 유지)

**관련**: known-issues "유료 플랜 실수치 미정" 항목 — 계산식 전환으로 small/medium/large 실수치 개념 자체 소멸. 결제 트랙에서 정리.

## 2026-05-30 (4) — 가격 실수치 확정

직전 2026-05-28 (2) B5 빈칸 채우기.

- 무료: 0원 (베타 기간)
- 소형: ₩10,000 / 클립 50개까지
- 중형: ₩50,000 / 클립 200개까지 (추천 플랜, 우선 처리)
- 라지: 별도 문의 / 클립 200개 초과·커스텀 (전담 지원, 현장 옵션 상담)

라지 200+ 별도 문의 이유: 큰 행사 = 사고 리스크 + 매출 폭 + 현장 운영. 자동화로 받기 부담. SaaS 표준 Enterprise = 영업 트랙 분리.

## 2026-05-30 (5) — 재편집 정책

- 결제는 이벤트당 1회
- 재편집 시 처음 결제 금액의 80%로 재결제
- 무료 플랜은 재편집 정책 적용 영역 미정 (외부 검증 후 결정)
- 라지는 별도 문의라 적용 영역 별개

## 2026-05-30 (6) — 유료 플랜 클립 길이 확장

- 무료: 10초 (PLAN_MAX_CLIP_SECONDS.free = 10, 변경 없음)
- 소형/중형/라지: 10~30초 (신규)
- plans.ts 갱신 트리거 박음 — 다음 본 앱 코드 변경 트랙에서 PLAN_MAX_CLIP_SECONDS 영역 갱신

## 2026-05-29 (3) — 워터마크 정책 + 무료/유료 차별화 축 + 무료 플랜 사양 공식화

**배경**: 2026-05-28 (2) 5번 결정에서 "워터마크 = 없는 방향 + 정찰 영역"으로 박힘. 본 세션 채팅 클로드 BM 정찰 → 운영자 결정 뒤집기 → 기술 정찰(CC) + 외부 사양 정찰(채팅 클로드) 종합으로 사양 확정.

**결정**:

1. **워터마크 정책**: 무료 플랜에 박음 / 유료 플랜엔 제거. 2026-05-28 (2) 5번 뒤집힘.
2. **무료/유료 차별화 축**: 클립 길이 / 클립 수 / 워터마크 3축.
3. **무료 플랜 = 현재 시스템 사양 유지**:
   - 클립 길이 10초 (src/lib/plans.ts:8 PLAN_MAX_CLIP_SECONDS.free)
   - 클립 수 10개 (src/lib/plans.ts:3 PLAN_CLIP_LIMITS.free)
   - 운영자가 본 세션에 결정한 수치가 코드와 일치. plan 인프라 인지 후 결정.
4. **유료 플랜 실수치 (small/medium/large 클립 길이·수 상한)**: 본 세션 영역 아님. 별도 트랙. 현재 plans.ts:3~8 초기값(small 50/medium 200/large 5000)은 임시값으로 간주.
5. **워터마크 시각 사양**:
   - 위치: 우하 (Shotstack `position: "bottomRight"`)
   - 크기: 중간 (font.size 정밀치는 CD 시안 후 결정)
   - 텍스트: "made by Congre"
   - 투명도: 은은하게 (rich-text `font.opacity` 정밀치는 CD 시안 후 결정, 0.3~0.5 영역)
   - 폰트: Cormorant Garamond italic
   - 색: 앰버 #c8892c (디자인 토큰 --accent)

**정찰 결과**:

- Shotstack 자체 워터마크 옵션 없음. 별도 트랙 패턴이 표준 (공식 워터마크 튜토리얼 다 이 패턴).
- plan 인프라 이미 가동 중: events.plan 필드 저장(api/events/route.ts:104) + 읽기(api/clips/route.ts:54) + PlanId 타입(plans.ts:1) + PLAN_CLIP_LIMITS/PLAN_MAX_CLIP_SECONDS 강제.
- 워터마크 구현 위치: shotstack.ts createRender() 시그니처에 plan 인자 추가 + render/start route에서 eventData.plan 전달. 무료 플랜에만 워터마크 전용 트랙 추가.
- Cormorant Garamond italic은 Shotstack 자체 호스팅 폰트 목록에 없음. Google Fonts에서 ttf 받아 public/fonts/에 두고 timeline.fonts에 등록 필요 (NotoSansKR 패턴).
- rich-text asset의 font.opacity 옵션 존재. clip 레벨 position: "bottomRight" 표준 키워드. length: "auto"로 영상 전체 길이 워터마크 가능.

**구현 영역**: 별도 트랙. 본 결정은 사양·정책 박기까지.

**관련**: 2026-05-28 (2), known-issues 신규 2건 (워터마크 시각 정밀 수치, 유료 플랜 실수치)

## 2026-05-28 (2) — A 트랙 가격 정책 결정 (B5 빈칸 채우기)

### 배경

2026-05-21 B5 결정에서 명시된 "다음 결정 영역" 5개 항목 중 본 세션이 처리하는 영역. 본 세션 범위 = 가격 표시 정책 + 결제 기능 자체는 차후. 지금은 정책 정착 + 홈피 표시 테스트 단계이며 실제 결제 기능 구현은 본 결정 영역 밖.

### 결정

#### 1. 가격 산정 기준 = 행사 사진 가격대 이하

- B5의 잠정가(소형 2만원/중형 7만원)는 행사 사진 가격대를 상회하지 않는 선에서 소비자 수용 가능한 숫자로 산정한 값. 원가 기반 산정 아님.
- Shotstack 렌더당 원가는 실측 영역으로 보류 (실제 운영해봐야 안다는 운영자 판단). 사후 실측 후 가격 조정 가능성 잠재.

#### 2. 세그먼트 차등 없음 — 모든 행사 동일 가격

- 클립 수 플랜(무료·소형·중형·대형) 가격은 행사 유형(결혼식·졸업식·기업·동호회) 무관 동일 적용.
- 같은 50클립이면 결혼식이든 졸업식이든 동일 소형 가격.

#### 3. 커스텀 상담 트랙 신설 (별도 청구)

- 특정 행사에 특수 요구사항이 있을 경우 self-serve 플랜이 아닌 별도 상담 트랙으로 처리:
  - 결혼식 특수 옵션: 개인화된 인트로, 식 당일 QR 배너, 영상 내 특수 기능 등
  - 졸업식 학교 차원 공식 신청 (수백 명 단위 대규모 행사)
- 진입점: 현재 `/pricing` 폼이 사실상 그 역할 수행 중 ("기타 (직접 입력)" 옵션 활용 가능). 별도 견적 페이지·견적 메뉴 신설은 보류 (YAGNI).
- 동선 설계(상담→견적→수락→입금→작업)는 격상 트리거 대기. 본 결정 시점엔 "트랙 존재"만 정책으로 박음.

#### 4. PG 선택 = 토스페이먼츠 (구현은 보류)

- 결제 PG는 토스페이먼츠로 방향 결정.
- 사업자등록증 등 사전 준비가 필요해 현재 시점 구현 보류. 실제 통합은 추가 테스트 후 별도 트랙(S3-05).

#### 5. 워터마크 = 없는 방향 + 정찰 영역

- 무료 플랜의 워터마크는 가능하면 없는 쪽 선호. 단, Shotstack 자체 워터마크 옵션 / 후처리 구현 비용 / 무료 플랜 인센티브 구조 트레이드오프는 정찰 후 결정.
- 정찰 결과에 따라 (a) 워터마크 없이 다른 제약으로 무료 차별화, (b) 워터마크 유지 둘 중 결정.
- **2026-05-29 갱신**: 5번 "워터마크 없는 방향" 뒤집힘. 무료에 박음, 유료엔 제거 방향으로 확정. 2026-05-29 (3) 참조.

#### 6. A 트랙 범위 메타 결정

- 본 트랙은 가격 정책 정착 + 홈피 가격 표시 테스트 영역. 실제 결제 기능 구현 영역 아님.
- 결제 기능 구현은 추가 테스트(가격 표시 반응·리드 폼 데이터 누적) 이후 별도 트랙.
- 따라서 본 결정 시점 코드 변경 없음.

### 본 결정에 미포함 — 다음 결정 영역

- 홈피 가격 표시 UI (현재 `/pricing`은 "베타 무료" 명시 + 카드 4장만, 실제 플랜 표 노출 안 됨) — 덩어리 2, CD 토큰 복구 후 R 사이클에 묶일 영역
- 워터마크 구현 방식 (위 5번 정찰 결과로 결정)
- Shotstack 원가 실측 (실제 운영 데이터 누적 후)
- 결제 PG 토스 통합 (사업자 준비 완료 후 S3-05)

## 2026-05-28 — 가격 노출 전략: B(임시 페이지+리드폼) → A(BM 결정) 순서

- **결정**: 결제 시스템·가격 미정 상태에서 B(임시 가격 페이지 + 리드 수집 폼) 먼저 배포, A(BM 결정)는 다음 트랙으로 이월. 페이지 톤은 "행사 유형별 안내 + 베타 무료 명시" 혼합.
- **리드 처리**: 이메일 발송만, Firestore 저장 안 함 (YAGNI — 리드 월 30건 초과 시 격상).
- **이유**: 가격이 확정되기 전에도 수요·세그먼트를 수집하기 위함. 리드 폼 데이터가 쌓이면 A 트랙 세그먼트 가격 결정의 참고 자료가 됨.

## 2026-05-21 — B5 가격 정책 결정 (S5 모델 채택)

### 배경

2026-05-09 D1·D2 결정 본문 = "재렌더 무제한 무료 + 사용자 안심". 본 결정 시점은 영업 개시 전 가정. 본 세션에서 운영자 두 사실 짚음:
1. 재렌더는 운영자 비용 (Shotstack 렌더당 과금)
2. AI 렌더링은 비결정성 — 재렌더가 품질 향상 보장 안 함

위 두 사실이 D1·D2 전제 자체를 흔듦. 영업 개시 흐름으로 본격 진입 전 B5 결정 필요.

### 결정

#### 1. 결제 모델 = S5 (첫 렌더 유료 + 재렌더 매번 유료)

- 무료 플랜: 진짜 테스트 용도 (워터마크 + 클립 수·길이 제한)
- 유료 플랜: 실제 행사 사용. 결제 후 진행
- 재렌더 무제한 (단, 매번 결제)

#### 2. 재렌더 의미 재정의

- 영상 품질 향상 목적 ❌
- 클립 in/out 토글 목적 ⭕ (호스트가 "민준이 클립 빼주세요" 요청 응대)
- 사용자에게 "재렌더 결과는 매번 다를 수 있음" 고지 (UI + 약관)

#### 3. 플랜 구조 (기존 UI 유지 + 서버 강제 추가)

| 플랜 | 클립 수 | 길이 허용 | 워터마크 | 다운로드 |
|---|---|---|---|---|
| 무료 | 5 | 5초·10초 | 박힘 | 허용 |
| 소형 | 50 | 5~30초 (현행) | 없음 | 허용 |
| 중형 | 200 | 5~30초 | 없음 | 허용 |
| 대형 | 5000 | 5~30초 | 없음 | 허용 |

"대형 무제한"은 실질 상한 없음 → Shotstack 비용 폭주 위험. 5000 상한 둠.

2026-08-19 정정: 무료 클립 수는 10이 아니라 5다. 코드 3곳(plans.ts, api/clips, dashboard/create)이 전부 5로 일치하며, 본 문서 표기만 어긋나 있었다. 랜딩 pricing.html도 5로 게시돼 있었다. 코드가 1차 출처라는 원칙에 따라 문서를 코드에 맞춘다.

#### 4. 가격 시작점 (임의값, 추후 시장 데이터로 교체)

| 플랜 | 가격 (15초 기준) | 클립당 평균 |
|---|---|---|
| 무료 | 0원 | - |
| 소형 (50클립) | 20,000원 | 400원 |
| 중형 (200클립) | 70,000원 | 350원 |
| 대형 (5000클립 한도) | 별도 견적 | - |

길이 구간별 가격 차등: 15초 기준 위 가격, 길이별 ±20% (5초 -40%, 10초 -20%, 15초 기준, 20초 +20%, 25초 +40%, 30초 +60%)

#### 5. 재렌더 가격

- 1차 재렌더: 첫 렌더의 80% (20% 할인)
- 2차 이후: 80% 고정

#### 6. 무료 → 유료 업그레이드

- 마감 전까지 플랜 업그레이드 가능 (UI 영역. 본 결정 시점 미구현)
- 마감 후 업그레이드 불가 (렌더 시작 전제 플랜 고정)

#### 7. 완성본 보존 (D2 사양 재작성 — data-flow.md 참조)

재렌더 매번 결제 → 호스트당 N개 완성본 동시 보존. 모두 7일.

### 본 결정으로 영향받는 영역

- `docs/decisions/rendering.md` D1 (재렌더 정책) — 본 결정으로 갱신 트리거. 무제한 무료 → 무제한 유료
- `docs/decisions/data-flow.md` D2 (완성본 보존) — 본 결정과 별도 본문에서 재작성
- `docs/legal/CHANGELOG.md` — 약관 v0.2 (유료 서비스 추가 시) 트리거
- 코드 영역: 본 결정 시점 결제 흐름 0건. S3-05·S3-06·S3-07·S3-08 작업으로 분리

### 본 결정에 미포함 — 다음 결정 영역

- 영상 길이별 ±20% 단가 공식의 정확한 가격대 (시장 데이터 수집 후)
- 결제 PG 선택 (토스페이먼츠·카카오페이 등) — S3-05 결정 영역
- 워터마크 구현 방식 (Shotstack 자체 vs 후처리) — 별도 정찰 후 결정
- done 상태 재렌더 버튼 UX — 결제 흐름 구현 후 진입 (S4-09 D2 본격)
- 재렌더 비결정성 고지 정확한 UI·약관 문구 — 약관 v0.2 작성 시

---

## 2026-05-14 — /share/{eventId} 페이지 재도입

- **결정**: 5/12 결정으로 카카오 공유 link를 event.videoUrl로 되돌렸으나, cdn.shotstack.io가 카카오 개발자 콘솔 미등록 도메인이라 수신자가 링크 탭 시 fallback으로 Congre 홈으로 이동하는 사고 재발. /share/{eventId} 페이지를 d74152e 시점 형태로 재도입하고 카카오 공유 URL은 `${appUrl}/share/${eventId}`.
- **이유**: 카카오 공유의 link.webUrl은 카카오 개발자 콘솔에 등록된 도메인만 사용 가능. 외부 CDN URL을 직접 넣으면 fallback 발생. 이 기술 제약은 d74152e 커밋 메시지에는 있었으나 5/12 결정문에는 누락되어 재발의 원인이 됨.
- **관련 커밋**: d74152e (2026-05-09, 최초 도입), 09a6d9a (2026-05-12, 제거), 이번 커밋 (재도입).

## 2026-05-12 — 초대장 기능 제거

- **결정**: 이벤트 대시보드의 초대장 작성 섹션(환영 문구, 대표 사진, 갤러리) 및 `/invite`, `/share` 페이지 전체 제거. 카카오 공유 링크는 `/share/{eventId}` 대신 완성 영상 URL(`event.videoUrl`)로 직접 연결.
- **이유**: 초대장은 게스트 경험 개선 기능으로 기획됐으나 실제 사용률이 없고 코드 복잡도만 증가. 게스트 접근 경로는 QR 코드(업로드 토큰 URL) 하나로 충분. 완성 영상 공유는 영상 직링크가 더 직관적.
- **대안**: 초대장 유지 후 UX 개선 — YAGNI, 현재 요구사항 없음 → 기각.

## 2026-05-09 — 공유 채널 정리: 카카오 유지, 인스타 제거

- **결정**: 완성 화면의 인스타그램 공유 버튼 제거. 카카오톡 공유 버튼은 유지 (정찰에서 SDK 통합·환경변수·도메인 화이트리스트 모두 등록 확인).
- **이유**: 인스타그램은 외부 앱에서의 직접 공유 API를 일반 공개하지 않음. 현재 코드는 `instagram.com` 홈을 새 탭에서 여는 무의미한 동작. 사용자가 인스타에 올리려면 어차피 영상 다운로드 후 인스타 앱에서 직접 업로드 — 다운로드 + 링크복사 버튼만으로 충분.
- **대안**: "다운로드 후 인스타 앱에서 업로드" 안내 모달로 변경 — YAGNI, 사용자 학습 비용만 추가 → 기각.

## 2026-05-09 — 시장 정의 보강: 단체사진 영상 버전, 자동 편집 필수, 제작자 무관

- **결정**: congre는 "단체사진으로 남는 모든 행사의 영상 버전"이다. 결혼식·졸업식·사내행사·MT 등 행사장에서 축하 인사·소감·현장 분위기를 누군가가 담아 주최자에게 보내고, 주최자가 감사 인사·마무리 멘트를 더해 실시간 완성본을 받아 참가자·SNS로 공유하는 도구.
- **이유**:
  - 1순위 시장(학교 졸업식, DECISIONS 2026-05-06)의 "1인칭 자기 회고" 패턴은 시장 안에서의 활용 형태이지 서비스의 본질이 아님. 결혼식 하객 축하·사내행사 동료 코멘트 등은 타인 시점이 자연스러움.
  - 자동 편집은 비용 우위가 아니라 현장성 보존을 위한 필수 조건. 사람 편집자(WeddingMix류)는 이 가치와 양립 불가.
  - 제작자 = 폰 가진 누구나 (행사 주최자, 학생, 사원 등). UI는 특정 역할 가정 없이 흘러야 함.
  - 영상 가치 강화 방향(DECISIONS 2026-05-05의 6항목) 중 인물 보정·배경 정리는 "실제 얼굴 + 현장성" 가치와 충돌. 강화 방향은 행사 컨텍스트 표시(가상 포토월, 행사 타이틀) 쪽으로 좁힘.
- **대안**: 1인칭 회고 전용 도구로 좁히기 — 결혼식·사내행사 시장 진입 차단으로 기각.

## 2026-05-07 — 서비스 모델 정의: 비공식 기념품, 즉시 삭제 데이터 흐름

- **결정**: congre는 학교·기업 등 주최 측이 공식적으로 발주하는 행사 솔루션이 아니라, 참가자들끼리 사적으로 기념하는 도구로 정의한다. 데이터는 인터넷 망에 영구 보존하지 않고 단기 처리 후 삭제한다.
- **데이터 흐름**:
  - 개별 클립: 완성 영상 렌더링 직후 즉시 삭제
  - 완성 영상: 주최자에게만 전송 → 주최자가 참가자에게 공유 (공유 인프라는 congre가 제공) → 며칠 후 삭제
  - 주최자 공유 이후의 배포 책임은 congre 영역 밖
- **함의 — 보안·법무 판단의 공통 전제**:
  - 학교·기관 등 외부 주체의 신뢰 손상은 위협 모델에서 큰 가중치를 갖지 않는다 (학교가 발주처가 아님).
  - "인터넷 노출", "장기 보존 위험" 시나리오는 데이터 흐름상 발생하지 않는다.
  - 1순위 시장(학교 졸업식)이 미성년자 영상이라는 사실은 유효하지만, 데이터가 단기 삭제되고 친구 그룹 내부에서만 공유된다는 점이 보안·법적 위험 평가의 보정 변수가 된다.
  - 보안 작업 우선순위는 "유출"보다 "기능 신뢰성"(렌더 성공률, 알림 도달성) 쪽에 비중을 둔다.
- **적용**:
  - 향후 모든 보안·법무·기능 결정은 이 데이터 흐름을 전제로 평가한다.
  - 이 정의가 바뀌면(예: 영상 영구 보존 옵션 도입, 학교 공식 발주 진입) 본 항목을 업데이트하고 보안 우선순위를 재평가한다.

## 2026-05-06 — 1순위 시장 정의: 학교 졸업식 / 1인칭 짧은 인터뷰

- **결정**: congre의 1순위 시장은 초·중·고 졸업식. 핵심 사용 패턴은 "학생 본인이 폰으로 자기 졸업 소감을 짧게 찍은 클립을 30~100명분 모아 자동 편집".
- **시장 선정 이유**:
  - 폰 소지자 = 학생 본인 → 1인칭 자기 표현 영상 (외부 관찰자가 찍는 결혼식·어린이집과 본질 다름)
  - 현장성 강함 (당일 흥분 상태에서 즉석 인터뷰)
  - 반복 구매 (매년 졸업식)
  - 가격 저항 낮음 (인당 천원 수준, 졸업앨범 5만원 대비. 분식 1번 값으로 학생 본인이 "내 졸업 영상에 내가 들어간다" 가치를 사는 데 결정 부담 거의 없음)
  - 결제 단위 명확 (반 단위 일괄 또는 졸업앨범 끼워서)
- **포지셔닝**: "여러 명이 같은 사건을 자기 입으로 회고한 영상의 집합체". WeddingMix·Vloggi(행사 다큐멘터리)와 다르고, TikTok·릴스(개인 콘텐츠)와도 다른 자리.
- **확장 시장**: 결혼식, 기업 행사, 동창회 등은 보조 시장. 학교 졸업식에서 검증한 기술의 확장 대상.
- **함의**: 미성년자 영상이라 보안·법적 리스크 우선순위 격상. 학부모·교사 사용자 비율 높아 네이버 메일 도달성 격상. (관련 작업은 ROADMAP 참조.)

## 2026-05-05 — 영상 가치 및 강화 방향 정의 (시장 2 안에서)

- **결정**: congre의 영상 가치는 "현장성 + 실제 얼굴이 나오는 것". AI 영상 생성(시장 1, Sora·Runway 등)으로는 가지 않음.
- **강화 방향 6항목**:
  1. 인물 보정 (촬영자 예쁘게)
  2. 배경 정리
  3. 화면 특수효과
  4. 클립 인아웃 효과 (지루하지 않게)
  5. 완성 영상에 코멘트 자막 (사전 입력받은 톤앤매너 기반, 즉석 사회자 스타일)
  6. 인트로/아웃트로 (행사 주최자 입력)
- **컨텍스트**: 트랙 B 편집 엔진 리서치에서 "AI 영상 생성"과 "프로그래밍 가능한 영상 편집"이 다른 시장임을 확인. congre는 후자(시장 2)에 속함.
- **활용**: 이 6항목은 향후 편집 엔진 평가의 체크리스트. 현재 Shotstack에서 어디까지 되는지, 안 되는 게 있으면 어떤 플랫폼이 가능한지가 평가 기준.
