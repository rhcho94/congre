# 2026-06-21 핸드오프 — 2단계(자동 위임) 정책 박기 + 4명 전원 시운전 완료

## 한 줄 요약
서브에이전트 팀이 라나만이 아니라 lana·scout·axe·eye **4명 모두 구축돼 있음**을
정찰로 확정(코난 기억이 "라나만"으로 낡아 있던 것 정정). CLAUDE.md L243
"에이전트 팀 순서 규칙" 드리프트 2건 정정 + 자동 위임 오케스트레이션 정책 박음
(커밋 afa18e1, push 완료). 이후 scout·eye·axe를 이름으로 직접 호출해 시운전 —
3명 다 떴고 각자 차선 지킴 확인. **2단계(자동 위임) 사실상 완료.**

## 본 세션 커밋 (push 완료)
- afa18e1 docs(claude): 에이전트 팀 순서 규칙 갱신 — 4명 구축 반영 + 실행관
  무커밋 정책 + 자동위임 오케스트레이션
  - origin/main: bdeeec9..afa18e1 push 확인됨.
- (이 핸드오프 커밋이 본 세션 마지막 커밋이 됨)

## 정찰로 확정한 사실 (.claude/agents/)
- 파일 4개 전부 존재: lana.md / scout.md / eye.md / axe.md (모두 model: sonnet,
  2026-06-19 생성).
- **scout.md 무결성 확인됨**: 첫 정찰 출력에서 scout.md 안에 라나 보고 형식이
  섞여 보였으나, 타깃 재확인(grep "표준 해법"·"기성 해법 못 찾음"·"찾은 해법들"
  ·"라나" → 전부 "없음", raw 35줄 일관) 결과 **파일은 깨지지 않음**. 첫 출력의
  겹침은 복붙/스크롤백 아티팩트였음.
- 에이전트별 도구·차선:
  - lana: Read/Grep/Glob/WebSearch/WebFetch (외부 리서치, 읽기 전용)
  - scout: Read/Grep/Glob (내부 코드 정찰, 하드 읽기 전용)
  - eye: Read/Grep/Glob/Bash (검증 — build/lint 검사, 코드 수정 안 함)
  - axe: Read/Grep/Glob/Write/Edit/Bash (실행 — 수정만, git
    commit/add/push/reset/checkout/revert 안 함)

## 해소한 드리프트 (CLAUDE.md L243)
1. "(현재 라나만 구축됨, 나머지 미착수)" → 실제 4명 다 있음. 정정.
2. "실행관(수정+atomic 커밋, push 안 함)" → 실제 axe.md는 **커밋 자체를 안 함**.
   - **결정(Ray 승인, B안)**: 실행관은 커밋하지 않고 작업트리에만 변경을 남기며,
     검증관 통과 후에 커밋한다(push는 Ray 검토용 별도 단계). axe.md 현행 유지,
     L243 문구를 axe.md에 맞춰 정정.
   - 근거: 프로젝트 리듬이 정찰→결정→실행→검증→커밋이라 커밋이 검증 뒤에 옴.
     검증 안 된 커밋이 안 생기고, 실패 시 작업트리만 버리면 돼(비개발자 안전).

## 박은 자동 위임 정책 (L243 NEW 블록 요지)
- 순서: 라나(외부)→정찰관(scout)→Ray 승인→실행관(axe, 수정만)→검증관(eye).
- CC 본체가 순서를 지휘한다 — **서브에이전트끼리는 서로 못 부름**(공식 문서 확인).
- 정찰 후 CC는 멈추고 계획을 Ray에게 올려 승인받는다 — **이 게이트는 자동화 안 함.**
- 자동 위임은 각 에이전트 description으로 발동하나, 신뢰 전 이름을 직접 불러
  (명시 호출) 시운전해 확인한다.

## 공식 문서에서 확인한 메커니즘 (설계 근거)
- CC는 각 서브에이전트의 description 필드로 자동 위임 여부를 판단. 자동 위임은
  이미 기본 동작 — "기능을 새로 만드는" 게 아니라 "순서·게이트를 CLAUDE.md
  정책으로 박는" 게 일이었음.
- 서브에이전트는 다른 서브에이전트를 스스로 호출 못 함 → 체인은 CC 본체가 지휘.
- 권장 절차: 자동 위임을 신뢰하기 전에 명시 호출로 시운전 → description은
  실사용 보고 보고 필요 시 튜닝. (그래서 이번엔 description 손 안 댐.)

## 시운전 결과 (명시 호출, 전원 합격)
- **scout** ✅ — 호출 성공. LANDING_URL="https://congre.kr" (src/lib/constants.ts:1)
  보고. tool 1회(Read만), 파일 안 건드림 = 읽기 전용 차선 지킴.
- **eye** ✅ — 호출 성공. build 통과, lint errors 11 / warnings 3 = baseline
  (11/3) 대비 delta 0. 코드 수정·커밋 안 함 = 검사 전용 차선 지킴.
- **axe** ✅ — 호출 성공. tmp-axe-smoketest.txt 생성(작업트리 untracked).
  핵심 합격선 둘 다 맞음: (1) git log -1 = afa18e1 그대로(HEAD 안 움직임 =
  커밋 안 함), (2) git status에 파일 untracked로 뜸(파일은 씀). B정책 실동작 확인.
- lana는 2026-06-19 시운전에서 기확인이라 이번 생략.
- 'Agent type not found' 한 번도 안 뜸 = 이 세션 명부 정상 등록 상태였음.

## 청소
- 시운전 임시파일 + 전부터 굴러다니던 잔재 모두 삭제:
  tmp-axe-smoketest.txt, tmp-pricing-variants.html.
- 최종 git status --short = 빈 출력(작업트리 클린). 두 파일 다 untracked였어서
  커밋엔 영향 없었음.

## 미완 작업 (다음 세션)
- 없음. 2단계(자동 위임) 정책+시운전 완료. push까지 됨.

## 다음 세션 후보 (우선순위)
1. (高) FGT 실행 결과 대응 — 지인이 막힌 지점 들고 오면 그때 처리. (FGT는 Ray
   운영 행동 영역: 지인에게 카톡 발송. 코드/문서 점검 빈칸은 없음.)
2. (中) 자동 위임 실사용 관측 후 description 튜닝 — 실제 작업에서 CC가 알아서
   맞는 에이전트로 가는지 보고, 안 가거나 과발동하면 그때 description 손봄.
   (지금은 검증 없이 안 건드림.)
3. (中) Gmail SPF alignment 작업 — 침투 마케팅(2단계) 전. 네이버 정상화 작업
   내용 규명도 이때.

## 본 세션 학습
- **최근 빌드/구축 상태는 코난 기억보다 직전 핸드오프가 1차 근거.** 코난 기억은
  background로 늦게 갱신돼 며칠치 뒤처질 수 있음. "라나만 구축"이 그 사례 —
  핸드오프("팀 4명")가 맞았고 정찰로 확정됨. 충돌 시 코드/정찰로 가름.
- **에이전트 출력 이상은 "파일 손상"으로 단정 말고 타깃 grep로 가름.** scout.md
  겹침이 복붙 아티팩트였음 — 서명 문자열 grep 한 방으로 파일 무결성 vs 표시 문제
  즉시 갈림.
- **"자동 위임"은 만드는 기능이 아니라 정책+시운전.** description은 이미 있고,
  일은 순서/게이트를 문서에 박고 명시 호출로 검증하는 것.
