# 세션 핸드오프 — 2026-05-09

---

## ★ 직전 마무리 — 2026-05-09 evening (이 섹션이 최신)

### 세션 요약

오늘 세션은 **필드 테스트 직전 사용성 테스트**에서 발견된 이슈 5건 + 운영 1건 처리. 첫 회차 진입 차단 항목 모두 해결 완료. 코드 측면에서 첫 회차 진입 가능 상태.

---

### 처리 이슈 6건

| # | 이슈 | 처리 방식 | 커밋 |
|---|---|---|---|
| #1 | SMS 미도착 (SOLAPI 잔액 부족) | 운영 절차: SOLAPI 충전 | 코드 변경 0 |
| #2 | render-completed 이메일 "영상 확인하기" → /host 리다이렉트 | `videoUrl`(CDN) 직접 링크로 교체 | `8f8600d` |
| #4 | 갤럭시 S22+ 후면 카메라 ultrawide 고정 | `pickStandardBackCamera` 휴리스틱 + 스트림 선정지 수정 | `c1c834f` + `6f5f0c5` |
| #5 | 랜딩 히어로 영상 무음 고정 (소리 켜는 방법 없음) | 우하단 unmute 토글 버튼 추가 (`LandingHeroVideo` 분리) | `bd65de8` |
| #6 | 완성 영상 클립 역순 회귀 (Phase B-3 부수효과) | render/start uploadedAt 오름차순 sort + shotstack.ts `.reverse()` 제거 | `da89a52` |
| PPT | 필드 테스트용 발표 자료 제작 | 채팅 클로드 영역 | — |

---

### 커밋 이력 (396b99b 이후 시간 순)

| 해시 | 내용 |
|---|---|
| `8f8600d` | fix(email): render-completed 영상 확인하기 버튼 → videoUrl 직접 링크로 변경 |
| `bd65de8` | feat(landing): 히어로 영상 unmute 버튼 추가 |
| `c2a5353` | chore: temp debug logs for camera enumeration (issue #4 reconnaissance) |
| `04a4e21` | chore: add zoom capability logging (issue #4 reconnaissance) |
| `e73f167` | chore: add force-switch buttons for back cameras (issue #4 reconnaissance) |
| `9bad515` | chore: remove debug logs (issue #4 reconnaissance complete) |
| `c1c834f` | feat(camera): auto-select standard wide back camera (issue #4) |
| `fc1eaa2` | chore: add heuristic tracing logs (issue #4 reconnaissance) |
| `751804f` | chore: remove heuristic tracing logs (issue #4) |
| `6f5f0c5` | fix(camera): release current stream before requesting deviceId (issue #4) |
| `da89a52` | fix(render): sort clips by uploadedAt ascending (issue #6) |

---

### 첫 회차 진입 차단 항목 상태

| 항목 | 상태 |
|---|---|
| SMS 발송 가능 (SOLAPI 잔액) | ✅ 충전 완료 |
| 완성 영상 이메일 링크 정상 | ✅ 커밋 8f8600d |
| 후면 카메라 광각 고정 (갤럭시) | ✅ 커밋 c1c834f + 6f5f0c5 |
| 랜딩 히어로 영상 소리 켜기 | ✅ 커밋 bd65de8 |
| 완성 영상 클립 순서 정상 | ✅ 커밋 da89a52 |
| 참가자 닉네임 필수 입력 | ✅ 커밋 a5bec82 (전 세션) |
| 인스타 공유 버튼 제거 | ✅ 커밋 7507b1e (전 세션) |

**코드 측면 첫 회차 진입 가능.**

---

### 운영자 영역 — 첫 회차 진입 전 결정 필요

1. **첫 회차 시나리오 정의**: 행사 종류(결혼식·돌잔치·기업 행사 중 무엇), 규모(참가자 수), 날짜·장소 확정
2. **참가자 모집·안내 자료**: QR 배포 방식, 참가자에게 전달할 업로드 안내문
3. **호스트 계정 생성**: Firebase 콘솔 → Authentication → Users → Add user. 이메일 + 임시 비번을 호스트에게 카톡/문자 전달 (옵션 A 절차, DECISIONS 2026-05-09 참조)
4. **아이폰 #4 검증**: 아이폰에서도 후면 카메라가 ultrawide 아닌 표준 wide로 잡히는지 실측 권장. 이상 시 격상 트리거 발동.

---

### 보류 의제 (별도 트랙)

- **아이폰 이슈 #4 검증 미완**: iOS 휴리스틱("후면 카메라"/"Back Camera" 라벨 매칭)은 아이폰 실측 미완. known-issues 격상 트리거: 첫 회차에서 아이폰 참가자 "광각 찍혔다" 신고 시 → 사용자 카메라 선택 UI(옵션 5-C) 격상 검토.
- **Setlog UX/BM 분석**: 카메라 답은 거기서 안 나왔지만 시장·UX 패턴 참고 가치 있음. 별도 세션 권장.
- **네이티브 앱 진입**: 첫 회차 후 결정 의제. 웹 MVP 필드 테스트 결과 기반 판단.
- **클립 메타데이터 저장 실패 사고** (마감 후 업로드 race): 빈도 측정 후 처리.
- **재렌더 UX 갭** (done 상태 버튼 미노출): 필드 테스트 첫 회차 결과 보고 갈래 5에서 처리.
- **완성본 단일 필드 덮어쓰기 구조**: 갈래 6 (7일 보관 + 삭제 알림) 작업 시 동시 전환.

---

### 자체 학습 (이번 세션 발생)

- **운영자 보고·실측이 검색 결과보다 우선**: 이슈 #4 진단에서 카메라 전문가 의견 5번("facingMode exact 사용" 등) 반복 시도 실패 후, 운영자 갤럭시 S22+ 실측(어떤 deviceId가 표준 wide인지) 기반으로 휴리스틱 확정. 외부 검색 의견보다 운영자 현장 실측이 정합한 케이스. DECISIONS 2026-05-09 카메라 항목에 기록.
- **큰 리팩토링은 호출자 가정 점검 필수**: 이슈 #6 — Phase B-3(de6551b)에서 render/start 리팩토링 시 shotstack.ts의 "내림차순 입력" 가정을 점검하지 않아 회귀 발생. 다음 큰 리팩토링 시 grep으로 호출자 가정 체크를 단계에 포함. DECISIONS 2026-05-09 렌더링 정렬 항목에 기록.

---

### 다음 세션 진입점 후보

- **A — 첫 회차 진행**: 운영자 영역. 시나리오 확정 → 호스트 계정 생성 → 행사 진행 → 결과 보고.
- **B — 첫 회차 결과 인터뷰**: 첫 회차 종료 후 결과 보고 세션. P1·P2 우선순위 재평가, 인증 모델 재결정.
- **C — P1·P2 보강**: Shotstack 비용 모니터링, 클립 길이 안내 강화 등. 첫 회차 데이터 없이 진행 시 비용 큼 → 첫 회차 후 권장.

---

### 다음 세션 시작 시 운영자 작업

CLAUDE.md kickoff 룰 그대로:

1. **5개 셋트** (CLAUDE.md, AGENTS.md, 이번 핸드오프, DECISIONS.md, known-issues.md) + docs/PROJECT.md 컨텍스트에 올림
2. "이 6개 외엔 모르는 것으로 간주. 진단·결정 시 모르는 영역이면 명시해라" 첫 메시지에 포함

---

---

## 이전 세션 내용 (2026-05-09 오전, 하단 보존)

## 세션 요약

직전 세션(2026-05-08 evening)에서 확정된 "필드 테스트 진입" 진입점에 따라, 본 세션은 필드 테스트 직전 단계의 서비스 정체성 확인 + 결정 묶음 + 코드 작업으로 진행됨. 인터뷰를 통해 시장 정의·인증 모델·재렌더 정책·완성본 보존 정책을 확정한 후, 인증 모델 결정은 필드 테스트 후로 보류하고 P0 코드 작업만 진행하는 흐름으로 정리됨.

## 커밋 이력 (시간 순)

- `625ef43` — docs: 인터뷰 결정사항 등록 (시장정의·인증·재렌더·보존·공유)
- `f7c7ce4` — docs: 호스트 인증 결정 폐기 + 이벤트-바운드 모델 채택
- `7507b1e` — feat: 완성 영상 화면에서 인스타그램 공유 버튼 제거
- `a5bec82` — feat: 참가자 닉네임 필수 입력 추가
- (이번 커밋) — docs: 갈래 0/0.5/필드-1/필드-2 결정·작업 이력 등록 + handoff 작성

## 인터뷰 결정 요약

### 시장 정의 보강 (DECISIONS 2026-05-09)
- congre = 단체사진으로 남는 모든 행사의 영상 버전
- 자동 편집은 현장성 보존 필수 조건 (비용 우위 아님)
- 제작자 = 폰 가진 누구나. UI에 특정 역할 가정 없어야 함
- 영상 강화 방향 = 행사 컨텍스트 표시 (가상 포토월, 행사 타이틀). 인물 보정·배경 정리는 "실제 얼굴 + 현장성" 가치와 충돌로 기각

### 호스트 인증 모델 — 보류
- 가입/로그인 분리 결정 → 같은 날 폐기 → 이벤트-바운드 모델 채택 → 필드 테스트 후 재결정으로 보류
- 보류 사유: 작업 영향 범위 7개 갈래 규모 + 첫 회차 결과 보고 모델 재평가가 정합
- 필드 테스트 단계는 옵션 A(Firebase 콘솔 수동) 운영. 코드 변경 0.

### 재렌더 정책 D1 / 완성본 보존 정책 D2
- 두 결정 모두 DECISIONS에 등록됨. 구현은 필드 테스트 후 P2.

### 공유 채널
- 카카오 유지, 인스타 제거. 구현 완료 (커밋 7507b1e).

### 참가자 닉네임 필수 입력
- 별도 nickname stage 신설. verifying → nickname → idle 흐름.
- maxLength 10자, sessionStorage 재사용 ("하나 더 올리기" 시 자동 채움).
- 같은 행사 안에서 중복 불가 (제외된 클립도 중복으로 간주).
- 호스트 대시보드 클립 목록에 닉네임 표시. 구현 완료 (커밋 a5bec82).

## P0 작업 완료

| 항목 | 상태 |
|---|---|
| 환경변수 등록 확인 (CRON_SECRET, NEXT_PUBLIC_APP_URL) | 완료 |
| 인스타 공유 버튼 제거 | 완료 (커밋 7507b1e) |
| 참가자 닉네임 필드 | 완료 (커밋 a5bec82) |
| 호스트 계정 N개 생성 (옵션 A) | 운영자 수동 — 필드 테스트 일정 확정 시 |

## 보류 의제 (이번 세션 발생, 별도 결정 필요)

- **모델 결정** (계정 vs 이벤트-바운드): 필드 테스트 첫 회차 후 재결정
- **클립 메타데이터 저장 실패 사고** (known-issues 등록): 빈도 측정 후 처리
- **호스트 클립 제거 시 영상 미리보기 흐름 검증**: 격상 트리거 대기
- **사용자 닉네임 회상 사고**: 격상 트리거 대기

## 닉네임 구현 세부 (코드 참조용)

변경된 파일 6개 (커밋 a5bec82):

| 파일 | 변경 내용 |
|---|---|
| `src/app/api/clips/check/route.ts` | 신규: GET ?eventId=&name= → { exists: boolean } |
| `src/lib/events.ts` | Clip 인터페이스에 uploaderName?: string 추가 |
| `src/app/api/clips/route.ts` | uploaderName 유효성 검사 + 중복 체크 + Firestore 저장 |
| `src/app/api/host/clips/route.ts` | 응답 맵에 uploaderName 포함 |
| `src/app/upload/[eventId]/page.tsx` | nickname stage 신설, sessionStorage 재사용, 409 처리 |
| `src/app/dashboard/events/[eventId]/page.tsx` | ApiClip에 uploaderName 추가, 클립 행에 닉네임 표시 |

흐름 요약:
- 토큰 검증 성공 → nickname stage (idle 아님)
- sessionStorage `congre-nick-${eventId}` 자동 채움
- "다음" 클릭 → GET /api/clips/check → exists:false → sessionStorage 저장 + idle
- doUpload body: `{ eventId, s3Key, token, uploaderName: nickname }`
- POST /api/clips 409 DUPLICATE_NICKNAME → nickname stage 복귀 + 에러 표시
- reRecord() ("하나 더 올리기") → idle 직행 (nickname 건너뜀, sessionStorage 닉 유지)

## 다음 세션 진입점

운영자 결정 영역. 두 갈래:

### A. 필드 테스트 진입 (P0 완료 → 첫 회차)
1. 운영자가 호스트 계정 N개 사전 생성 (옵션 A: Firebase 콘솔 → Authentication → Users → Add user)
2. 첫 회차 시나리오 정의 + 참가자 모집 + 행사 시뮬레이션
3. 첫 회차 결과 보고 → P1·P2 우선순위 재평가

### B. 필드 테스트 전 추가 보강 (P1 일부 끌어올리기)
- Shotstack 비용 모니터링 설정 (사고 방지)
- 클립 길이 안내 강화 (시나리오 #2 방어)

A를 선택하면 다음 세션은 첫 회차 결과 인터뷰. B를 선택하면 다음 세션은 P1 갈래 정찰/실행.

## 운영자 검증 체크리스트

이번 세션 P0 작업이 의도대로 동작하는지 첫 회차 진입 전 직접 확인 권장:

- [ ] 인스타 버튼이 완성 영상 화면에 더 이상 보이지 않음
- [ ] 카카오톡 공유 + 다운로드 + 링크 복사 버튼만 표시됨 (2개 버튼 그리드)
- [ ] 새 이벤트 만들고 참가자 업로드 페이지 진입 시 닉네임 입력 화면 표시됨
- [ ] 같은 닉네임으로 두 번째 참가자 시도 시 중복 에러 표시됨
- [ ] "하나 더 올리기" 시 닉네임 stage 건너뛰고 idle로 직행
- [ ] 호스트 대시보드 클립 목록에 닉네임 표시됨
- [ ] 같은 브라우저에서 다른 행사 이동 시 sessionStorage 격리 (이전 행사 닉이 새 행사에 미리 채워지지 않음)

## 다음 세션 시작 시 운영자 작업

CLAUDE.md kickoff 룰 그대로:

1. 5개 셋트 + 이번 핸드오프(docs/handoff/2026-05-09.md)를 컨텍스트에 올림
2. docs/PROJECT.md도 함께 올리면 진단 정확도 향상에 도움됨
