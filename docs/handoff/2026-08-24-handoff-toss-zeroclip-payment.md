# 2026-08-24 핸드오프 — 토스 보완 요청 대응 (클립 0개 결제 허용)

## 한 줄 요약
토스가 마감 당일 보낸 보완 요청(결제창 미비)에 대응해 `payment/prepare`의 클립 0개
차단 가드를 제거하고 경고 배너를 추가했다. 실제 결제까지 스모크 테스트로 관통 확인 후
회신 발송 완료. 문서 갱신 4파일 등재. 코드 1커밋 + 문서 1커밋, 둘 다 push 완료.

---

## 이번 세션 커밋 (2건, push 완료)

| 해시 | 내용 |
|---|---|
| `f58281d` | fix(payment): allow prepare with zero clips + warn before payment |
| `a8f5d38` | docs: 클립 0개 결제 허용 기록 + 신규 known-issue 2건 |

`f58281d`: 2파일 `+8 −4`. build 32라우트 통과, lint 12/3 baseline delta-0.
`a8f5d38`: 4파일 `+47 −1`. 문서 전용이라 게이트 미실행.

**숫자 검증**: 두 커밋 모두 `--stat` 파일별 값과 총계가 정합. `a8f5d38`의 파일별 합
10+2+13+23=48이 총계 47+1=48과 맞는다(DECISIONS의 `2`는 추가1+삭제1).

---

## 발단 — 토스 보완 요청 (당일 수신)

> 전달주신 로그인 > 이벤트 생성 > 유료 체크 > 마감하기 로 접속 시 메세지가 뜨며
> '결제하기' 창으로 넘어가지 않습니다. 카드사 심사 송부를 위하여, 영상을 업로드
> 하지 않고도 카드결제창 확인이 가능하도록 개발 수정 부탁드립니다.

반려가 아니라 **카드사 송부 자료를 만들기 위한 보완 요청.** 요구 수준은
"결제 승인"이 아니라 "카드결제창이 뜨는 것"까지다.

화면 문구: `업로드된 영상이 없어 결제할 수 없습니다.` (전체화면 + "이벤트로 돌아가기")

---

## 정찰 결과 (파일:줄 실측)

| 항목 | 위치 | 내용 |
|---|---|---|
| 판정 원본 | `api/payment/prepare/route.ts:52-57` | clips 조회 → `excludedAt` 없는 문서 카운트 → 0이면 400 `NO_CLIPS` |
| 문구 매핑 | `payment/[eventId]/page.tsx:28` | `prepareErrorMessages`의 한 항목(조건문 아님) |
| 화면 분기 | `payment/[eventId]/page.tsx:179` | `if (prepareError)` |
| 위젯 렌더 | `page.tsx:193-195` | `if (!prepared) return null` |
| confirm 가드 | `api/payment/confirm/route.ts:61-77` | 개수 **변화**만 검사. 0 자체를 막는 조건 없음 |
| 금액 계산 | `lib/plans.ts:23-28` | `Math.max(maxClipSeconds × clipCount × 100, MIN_PRICE)` |

**핵심**: 막는 건 클라이언트가 아니라 서버. prepare가 400을 내면 orderId·amount가
생성되지 않아 위젯을 띄울 재료 자체가 없다. 화면 문구나 클라이언트 분기를 고쳐도
결제창은 뜨지 않는다.

---

## 조치 — 3안 중 "가드 제거 + 경고 배너" 채택

| 안 | 내용 | 판정 |
|---|---|---|
| 가 | 가드만 제거 | 클립 없이 결제한 호스트가 4시간 환불 불가 구간에 갇힘 |
| **나** | 가드 제거 + 경고 배너 | **채택.** "알고 눌렀다"가 성립 |
| 다 | 심사용 우회 파라미터 | 심사 통과가 목적인데 우회 경로를 만드는 건 방향이 반대 — 기각 |

배너 문구(`page.tsx:225-232`, `prepared.clipCount === 0` 조건):
> ⚠️ 아직 업로드된 영상이 없습니다. 이 상태로 결제하면 제작할 영상이 없습니다.
> 결제 후 4시간 이내에는 환불이 제한됩니다.

환불 조건 한 줄은 초안에 없던 것을 추가했다. 약관상 4시간 이내 환불 불가를
결제 직전에 고지하는 것이 분쟁 예방이자 심사에도 유리하다는 판단.

`prepare` 응답에 `clipCount`가 **원래 있었다**(`route.ts:105`, 클라이언트
`PrepareResponse` 인터페이스에도 정의). 필드 추가 없이 기존 값을 조건으로 씀 —
API 응답 형태 무변경이라 파급 없음.

---

## 스모크 테스트 (실측)

배포 후 유료 이벤트를 영상 0개로 생성 → 마감 → 결제 페이지에서 전부 확인:

- 경고 배너 정상 렌더
- `영상 길이 15초 × 영상 0개 × 100원 = 0원 → 최소 결제 금액 10,000원 적용` 계산 근거 노출
- 결제수단 위젯 정상, 신용·체크카드 선택 가능, 카드사 목록 표시
- 하나카드 결제창 진입 확인, 가맹점 "토스페이먼츠", 금액 10,000원
- 실제 결제 완료 → 이후 "영상 생성 시작에는 실패했습니다" (예상된 동작)

회신 첨부는 2장(당사 결제 페이지 + 카드사 결제창 진입). 카드번호가 보이는
화면은 제외했다.

---

## 문서 갱신 4파일 (`a8f5d38`)

| 파일 | 내용 |
|---|---|
| `CHANGELOG.md:5` | `## 2026-08-24` 절 신설 (+10) |
| `decisions/market-product.md:5` | 결정 1건, 최상단 (+13). 파일 `:3`에 "최신이 위" 명시 |
| `DECISIONS.md:11` | market-product 18개 → 19개. 실제 `##` 헤더 수 19와 대조 확인 |
| `known-issues.md:775, :789` | 신규 2건 (+23). 773 → 796줄 |

**신규 known-issue 2건**
1. **클립 0개로 결제하면 렌더가 실패한다** — `render/start`의 NO_CLIPS 가드는 그대로
   남아 있어 "돈 받고 렌더 실패" 경로가 열렸다. 배너로 고지하나 코드상 차단은 없다.
   실사용자 0명 + 테스트 키라 현재 실피해 없음. **유료 개시 전 필수 3번째 항목.**
2. **국민카드 위젯 표시가 legal 기록과 어긋난다** — `legal.md` 2026-08-16 결정 4는
   "승인이 나도 국민카드는 열리지 않는다"인데 위젯 목록엔 국민이 표시됨. 표시와
   실제 승인 여부는 별개일 수 있어 판정 보류. 심사 완료 후 실결제로 확인.

---

## Project Knowledge 재업로드 대상

| 파일 | 사유 |
|---|---|
| `known-issues.md` | 신규 2건 |
| `CHANGELOG.md` | 8/24 절 |
| `DECISIONS.md` | 인덱스 19 |
| `decisions/market-product.md` | 결정 1건 |

---

## 정리 필요 — 콩그레 레포에 필드샷 파일 1개

`docs/handoff/2026-08-24-handoff-fieldshot-interview-1.md` 가 untracked 상태로
콩그레 레포 안에 있다. **필드샷 산출물은 콩그레 레포에 커밋하지 않는다**(바이블 선례).
아직 커밋되지 않았으므로 콩그레 폴더 밖으로 옮길 것. 다음 세션에서 `git add`로
딸려 들어가지 않게 주의.

---

## 다음 세션 작업 목록

### 1. 토스 회신 대기 (최우선, 수동)
- 8/24 보완 완료 회신 발송함. 카드사 심사 송부 대기.
- 승인 시 → 결제위젯 전용 키 발급 요청 → 실키 전환
- 보완 요청 가능성 잔여 항목: 통신판매업·유선전화
- **통신판매업 톤 불일치 답변 준비돼 있음**(8/19 핸드오프 참조)
- **미조사(이월)**: 통신판매업 신고에 PG사 발급 구매안전서비스 이용확인증이
  필요하다면 "승인 전 신고 불가 / 신고 없이 승인 불가" 순환이 생긴다. 확인한 바 없음.

### 2. 유료 개시 전 필수 (3건 — 1건 증가)
- 랜딩 `pricing.html` 48시간 기한 추가 (격상 트리거 = 토스 승인 회신)
- **409 재발행 경로** — `payment/confirm` 409 응답에 `eventId` 부재. 미착수.
- **클립 0개 결제 후 렌더 실패** (신규, 이번 세션 발생)

### 3. 격상 트리거 대기 (변동 없음)
클립 영구 잔존 / 48시간 리터럴 2곳 상수 분리 / 재편집 용어 3곳 통일 /
재렌더 결제 서버 강제 / 재렌더 모달 금액 표시 / 사업자 정보 블록 공유화 /
SMS 90바이트 / L14 백업 HTML 공개 접근 / L16 주석 드리프트 / 랜딩 톤 이원화 /
국민카드 표시 불일치(신규, 심사 후 확인)

### 4. 필드샷 트랙 (별도 Project)
오늘 인터뷰로 사양 확정. 산출물 2개(`fieldshot-spec.md` +
`2026-08-24-handoff-fieldshot-interview-1.md`)를 콩그레 폴더 밖에 보관.
**새 Project를 만들고 그 안에서 이어갈 것.**

---

## 이번 세션 학습

- **문서 날짜를 현재 시각으로 읽었다.** 8/19 핸드오프를 첨부받고 "오늘이 8/19,
  마감까지 5일"이라 계산했으나 실제 오늘은 마감 당일 8/24였다. 첨부 문서의
  날짜는 그 문서가 쓰인 날이지 오늘이 아니다.

- **문서를 코드보다 먼저 믿었다 — 랜딩에서 재발.** 히어로 영상을 묻자
  `landing.md`의 2026-05-24 결정을 현재 상태로 읽고 `wedding_1.mp4`라 답했으나
  실제는 `demo.mp4`였고 파일도 4개가 아니라 6개였다. 8/19에 같은 학습을
  했는데 또 밟았다. **랜딩은 git 밖이라 "코드를 보면 된다"가 적용 안 되는
  영역** — 여기선 라이브 페이지를 `web_fetch`로 직접 가져오는 게 1차 출처다.

- **BGM 반영 방식을 일반 패턴으로 앞세웠다.** "곡 목록이 코드에 하드코딩돼
  있을 것"이라 답했으나 실제는 렌더 시점에 S3 폴더를 조회하는 구조라
  파일만 올리면 즉시 반영된다. 일반적인 패턴을 이 코드의 사실로 말했다.

- **`L` 번호를 전역 규칙으로 확대 적용했다.** 8/19 핸드오프의 "전역 일련번호 룰"을
  파일 전체 규칙으로 읽어 L17·L18을 만들려 했으나, 실제로는 **랜딩 섹션 전용**
  번호였다. 앱 영역 50여 건은 번호 없이 `##` 헤더만 쓴다. 문서 표현이 다루는
  대상 범위를 확인하지 않고 확대했다.

- **`calcPrice`의 감싸는 함수를 안 봤다.** "클립 0개면 금액이 0원이 되어 토스가
  거부할 수 있다"고 가설을 세웠으나 `Math.max(계산값, MIN_PRICE)`라 10,000원이
  나온다. 곱셈이 0이 되는 데까지만 따라가고 바깥을 안 봤다.

- **중간 단계 완료를 확인 없이 미완으로 가정했다.** `f58281d` push를 이미
  진행했는데 "아직 안 했을 것"으로 계산해 `ahead 2`를 예상했다. 실제는
  `ahead 1`이 맞았다. 내가 프롬프트를 준 단계라도 실행 여부는 보고로 확인해야 한다.

- **보고 검증 룰을 Ray의 수작업에까지 적용했다.** BGM 업로드 후 "폴더별 개수를
  알려달라"고 요청했으나 그 숫자로 할 작업이 없었다. 룰의 적용 대상(CC 보고)을
  넘겨 확대했다.

- **분리 원칙은 조사 도구에도 적용된다.** 필드샷 브랜드명 조사를 콩그레 세션의
  라나로 돌렸다. 바이블 때 "데이터 조사 하나라도 콩그레 세션·도구를 거치면
  섞인다"고 학습해둔 항목을 그대로 밟았다. 결과물이 콩그레 문서에 들어가지
  않아 실피해는 0.

- **선례를 안 찾고 시작할 뻔했다.** 필드샷 포크 절차를 처음부터 설계하려 했으나
  Ray가 바이블 포크 전력을 언급해 핸드오프 4건을 확보했고, 지뢰 6종
  (`CRON_SECRET` 미설정 / `APP_URL` https 누락 / 새 버킷 CORS / Shotstack
  창고 버킷 이름 환경별 상이 / 통신 placeholder / Import .env의 콩그레 APP_URL)을
  미리 확보했다. 새 트랙을 열 때 "비슷한 걸 전에 했나"부터 물을 것.

- **과거 대화 검색은 현재 Project 범위로만 잡힌다.** 바이블 작업이 별도
  Project라 검색에 안 나왔고, 이 프로젝트엔 6/22(포크 미실행) 시점 기록만
  있었다. "검색해서 없음"을 "존재하지 않음"으로 읽지 말 것.

- **CC가 집계 단위 함정을 스스로 잡았다.** `### L숫자.` grep에서 L10이
  빠지는 것을 발견하고 "L10은 `##` 레벨이라 집계에서 누락됐지만 실제
  사용 중인 번호"라고 먼저 밝혔다. 이 지적이 없었으면 L10을 빈 번호로
  읽고 재사용했을 것이다.

- **CC가 지시에 없는 대조를 했다.** DECISIONS 인덱스 숫자를 고치면서
  선언값이 아니라 실제 `##` 헤더 수를 세어 19와 일치함을 확인했다.
  사양 밖 확인이지만 정확한 방향.
