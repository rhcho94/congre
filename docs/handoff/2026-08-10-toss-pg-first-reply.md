# 2026-08-10 핸드오프 — 토스페이먼츠 PG 계약 1차 회신

## 한 줄 요약
토스페이먼츠 PG 심사 1차 회신 발송 완료. 결제창 미연동이라 2단계 분할
회신을 택했고, 2차 기한은 2026-08-24. 코드 변경 없음(서류·진단 트랙).

## 이번 세션 커밋
- `6580e27` docs(handoff): 2026-08-07 게스트 클립 길이 안내 세션 핸드오프
- (예정) docs: known-issues 유료 플랜 실수치 항목 resolved 이관 + 본 핸드오프

## 처리 내용
토스 계약 담당자(노상미 / yessangmi@tossgrowth.com) 메일의 1번 필수질문
4개에 회신. 2번(홈페이지 준비사항·결제경로 PPT)은 결제창 미연동으로
2차 회신으로 분할.

**분할 근거**: 결제경로 PPT는 카드사 심사용이고, 서류 심사(3~5영업일)가
그보다 앞선다. 1번만 먼저 보내면 서류 심사가 병렬로 돌면서 그동안
결제창을 붙일 수 있다. 1개월 미회신 시 신청 취소 규정도 즉시 해소.

## 토스 신고 확정값
| 항목 | 값 |
|---|---|
| 상호/대표/사업자번호 | 레이네(Rayne) / 조래환 / 412-19-02824 |
| 사업장 주소 | 경기도 성남시 분당구 미금로 36, 201호 A82호 (구미동, 브리엘프라자) |
| 업태/종목 | 정보통신업 / 포털 및 기타 인터넷 정보 매개 서비스업 |
| 과세유형 | 일반과세자 |
| 연락처 | 010-7582-2020 (승인 후 070 전환 명시) |
| 통신판매업 | 미신고 — KB국민카드 제외하고 심사 요청 |
| 단건 최고가 | **2,000,000원** (100초 × 200개 × 100원) |
| 최대 제공기간 | 결제 후 7일 (약관 제11조) |
| 심사 테스트 계정 | rhcho94+tosstest@gmail.com |
| 첨부 | 사업자등록증 사본 1부 |

전대차 계약서는 첨부하지 않음 — 요구받지 않았고 주민번호·임대인
개인정보·계좌번호가 포함됨. 요청받으면 그때 제출.

## 정찰로 확정된 코드 사실 (scout 2회)
- `plans.ts`: `PlanId = "free" | "paid"` 2종.
  `PLAN_CLIP_LIMITS = { free: 5, paid: 200 }`,
  `PLAN_MAX_CLIP_SECONDS = { free: 10, paid: 100 }`
- `calcPrice = Math.max(초 × 개수 × 100, 10000)` — 호출부 1곳
  (`create/page.tsx:441`)뿐, 서버 미사용. `events` 문서에 금액 필드 없음
- `api/events/route.ts:85-94`의 상한 200은 하드코딩 리터럴.
  `getPlanClipLimit`은 정의만 있고 호출부 0건
- 유료 플랜 `comingSoon: true`로 UI 잠김, 라벨 "유료 · 준비 중"
- 무료/유료 실질 차이 4가지: 클립 수 / 클립 길이 / 워터마크 / 렌더 게이트
- 이메일 `+` 별칭 거부 로직 없음 — 실측 가입 성공
- 회원가입 경로는 `/signup` (`/host` 아님)

## 정찰로 뒤집힌 것
**"무료 10개 vs 5개"는 드리프트가 아니었다.** `market-product.md:46`에
"무료 클립 수 10개 → 5개로 변경" 결정 기록이 이미 있고, `landing.md:35`·
`CHANGELOG.md:130`에도 정정이 남아 있다. 결정·이력·코드 전부 정합.

실제 stale은 `known-issues.md:445-451` 한 블록뿐. small/medium/large가
아직 미결 사안인 것처럼 서술돼 있으나 코드에선 구조 자체가 소멸했고,
`market-product.md:48`이 이미 그 사실을 기록하고 있었다.

`PROJECT.md`·`DECISIONS.md`에는 관련 서술이 아예 없다 — 코난이 근거 없이
두 파일을 수정 대상으로 지목했던 것을 정찰이 부정.

## 신규 known-issues 등재 대상
1. **`hello@congre.kr` 수신 불가** — MX 레코드 없음(MXToolbox
   `DNS Record not found`). 푸터에 고객 문의 주소로 공개 중이라 지금까지
   온 문의는 전부 소실. 시작 시점 불명. SPF·DKIM·DMARC(발신)는 정상이므로
   MX 추가 시 기존 발신 설정 파괴 주의 (2026-07-03 도메인 사고 계열)
2. **랜딩 푸터에 사업자정보 0건** — 전자상거래법·카드사 심사 양쪽 미충족
3. **약관이 "무료 베타" 전제** — 제10·14·20조가 유료 도입과 모순.
   환불 조항 부재(제10조 ③이 "별도 고지"로 미룸)
4. **유료 플랜 UI 잠김** — `comingSoon: true` + "준비 중" 라벨.
   심사 요건 "테스트 상품 불가"와 충돌
5. **금액 산출이 클라이언트에만 존재** — 결제 연동 시 서버 계산 필수.
   클라 금액을 신뢰하면 H-2(`unlocked:true` 자기부여)와 같은 계열의 우회
6. **과금 모델 미구현** — 사양(마감 시점, 실제 개수 정산)이 코드에 없음.
   현 `calcPrice`는 천장 표시용

## 2차 트랙 작업 목록 (기한 2026-08-24)
| # | 작업 | 트랙 | 선후 |
|---|---|---|---|
| ⓐ | 결제위젯 연동 + `comingSoon` 해제 + 라벨 교체 + 서버 금액 산출 | 코드 | 최우선 |
| ⓑ | 랜딩 푸터 사업자정보 6종 | 랜딩(git 외부) | 병렬 가능 |
| ⓒ | 유료 약관 + 환불 정책 개정 | 문서→코드 | 병렬 가능 |
| ⓓ | 문의 이메일 수신 경로 | DNS | 단독 사이클 권장 |
| ⓔ | 결제경로 PPT 제작 | 서류 | ⓐⓑⓒ 완료 후 |

ⓑ는 랜딩이 git 외부 트랙이라 CD zip 덮어쓰기 위험(known-issues L8·L9
계열)에 노출된다. 작업 후 백업 필수.

## 결제경로 PPT 제작 요건 (첨부 가이드 1번 기준)
순서: ① 가맹점 정보 표지 → ② 하단정보 캡처 → ③ 환불규정(무형상품)
→ ④ 로그인/회원가입 → ⑤ 상품선택·구매과정 → ⑥ 카드결제경로

유의사항:
- 반드시 PPT 형식
- 북마크바 없이 도메인이 보이도록 캡처
- PC 시계가 함께 찍혀야 함 (시간 흐름으로 조작 여부 판단)
- 테스트용 결제창 연동으로도 심사 가능
- 비씨카드는 카드사 인증 화면까지 캡처 필요

## 미결정
- 070 개설 시점·사업자 (승인 후로 미룸)
- 문의 이메일 방식 (Google Workspace / 가비아 / Gmail 노출)
- 베타 쿠폰칸 — 이번엔 유지 + 메일에 프로모션으로 선제 설명.
  2차에서 재검토 여부 미정

## 이번 세션 학습
- **부분 인용으로 전체를 판정하지 말 것.** Project Knowledge에 자동
  첨부된 문서 일부에서 "무료 10개"를 발견하고 드리프트로 단정했으나,
  다른 문서에 정정 기록이 이미 있었다. 저장소 전수 확인 전에는
  "일부에서 발견됨"까지만 말할 것.
- **stale 문서를 현재 코드로 읽지 말 것.** `large 5000`을 근거로
  "5,000만 원 결제 가능"이라 경고했으나 이미 사라진 값이었다.
  금액·상한처럼 결과가 큰 값은 반드시 코드 실측 후 발언.
- **확립된 워크플로 규범을 매번 재검증하지 말 것.** 커밋이 지시된
  것인지 되물었으나, 커밋은 예외 없이 지시로만 이뤄져 온 규범이다.
  검증은 새 정보나 어긋난 신호가 있을 때만. 정상 동작 재확인은
  진행을 막는다. (2026-08-10 Ray 지적)
- **서류 심사 트랙은 코드 트랙과 선후가 다르다.** "완벽한 답장"을
  한 번에 보내려 하면 코드 완성까지 대기하게 된다. 심사 단계가
  직렬이 아님을 파악하면 분할 회신으로 병렬화할 수 있다.

## 블로커
- 토스 서류 심사 결과 대기 (3~5영업일, 8/17 전후 예상)
- 이후 카드사 심사 7~14영업일
