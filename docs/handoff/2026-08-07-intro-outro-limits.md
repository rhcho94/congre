# 2026-08-07 핸드오프 — 인트로/아웃트로 미디어 상한 교체

## 한 줄 요약
인트로/아웃트로 미디어 상한을 10MB → 100MB·영상 15초로 교체하고 안내 문구를 복원. 커밋 `02015ad`, push·배포·실사용 검증 완료.

## 이번 세션 커밋
- `02015ad` fix(dashboard): 인트로/아웃트로 미디어 상한을 100MB·영상 15초로 교체 + 안내 문구 복원 — 6파일 174↑/20↓

## 처리 내용
- 크기 10MB → 100MB (인트로·아웃트로 양쪽, 브라우저 검사)
- 영상 길이 15초 검사 신설. `measureVideoDuration` 헬퍼를 대시보드 파일 내부에 신설 (게스트 업로드 경로 무접촉 — 가장 중요한 경로를 이번 커밋에 끌어들이지 않기 위함)
- 판정 불가 영상(비표준 코덱 등)은 통과. 기존 동작 유지가 목적
- 안내 문구 2곳 신설 + 아웃트로에만 직렬 배치 안내 1줄

## 정찰로 확정한 사실
- 파일 바이너리는 Next.js 서버를 경유하지 않는다. 브라우저 XHR가 S3 presigned URL로 직접 PUT(`src/lib/s3.ts:67-69`). Vercel 함수 요청 크기 한도의 적용 대상이 아님
- 서버·S3·`next.config.ts` 어디에도 크기 검사 없음. 유일한 관문이 브라우저 검사였음
- 게스트 클립 경로에는 크기 검사가 아예 없음(길이 검사만)
- 10MB의 출처: `5172171`(2026-05-11 초대장 이미지 잠정값) → `4a1e058`(다음 날 영상에 복사) → `09a6d9a`(초대장 삭제, 값만 잔존). 정량 근거 없음

## 실사용 검증 (배포 후 Ray 실측, 전항목 통과)
- 안내 문구 노출
- 폰 영상 인트로 업로드 통과 ← 이번 사이클의 목표
- 15초 초과 영상 거부
- 사진 업로드 통과

## 부수 마감
H-1(2026-08-06) 미완 검증 1건이 함께 닫혔다. 인트로 영상 업로드가 10MB에 막혀 MIME 화이트리스트 실측에 도달하지 못했던 건이, 폰 영상 업로드 성공으로 해소. `video/mp4`가 presign 화이트리스트를 통과함이 실측 확인됨.

## 새로 등재한 것 (known-issues.md, 3건 모두 미처리)
- 재생 시간 측정 로직 중복 2곳 — 의도적 선택
- 게스트 클립 길이 안내 문구와 `maxClipSeconds` 불일치 의심 — 미확인, 정찰 미실시
- `measureVideoDuration` 타임아웃 없음 — 무응답 시 업로드 무반응. 미실현

## 미완 작업
1. iOS `file.type` 실값(HEIC 등) 미실측 — H-1 세션에서 승계, 여전히 미확인
2. S3 버킷 퍼블릭 액세스 차단 토글·CORS 본문 — 콘솔 영역
3. `c3c69e3`의 안내 문구 제거가 의도인지 리디자인 중 실수인지 미확인

## 다음 세션 후보
1. **M-1·M-2 프리픽스 검증** — 남은 최고 등급(MEDIUM). presign 로그 S3 키 평문 건과 묶어 처리
2. 게스트 클립 길이 문구 불일치 정찰 — 이번에 등재한 건
3. Gmail 스팸 재확인 (Toss PG 서류) — 코드 밖
4. Congre Bible 포크
5. lint 11/3 baseline — 이번 세션 delta 0

## 블로커 (변동 없음)
Toss PG 심사 대기

## 이번 세션 학습
- **CHANGELOG의 "추가했다"를 현재 상태로 읽지 말 것.** 코난이 "UI에 10초 안내가 있다"고 단정했으나 그 문구는 이틀 뒤 `c3c69e3`에서 제거된 상태였다. 변경 이력은 그 시점의 사건이지 현재 상태가 아니다. 같은 파일에 나중 커밋이 있는지부터 확인할 것
- **한 줄로 파이프라인 전체를 판정하지 말 것.** `shotstack.ts:111`의 `length:"auto"`만 보고 "서버가 인트로 길이를 모른다"고 결론냈으나, 2026-06-14 probe 도입으로 `render/start`가 미리 재서 넘긴다. 결론(프론트 검사)은 유지됐으나 근거는 틀렸었다
- **제한값을 풀 때는 그 제한이 부수적으로 막아주던 것부터 볼 것.** 10MB 크기 제한이 (1) 길이 제한 (2) 자막 어긋남 진폭을 겸해 막고 있었다
- **운영자의 흐릿한 기억은 정찰 트리거다.** Ray의 "정확치는 않은데" 두 마디가 코난의 오류 2건을 잡았다. 둘 다 문서에 근거가 있었는데 안 찾은 것
- **PowerShell 환경에서 커밋 메시지에 꺾쇠(`<`, `>`)를 쓰지 말 것.** here-string이 리다이렉션으로 해석해 조기 종료된다. 불가피하면 처음부터 `git commit -F` 파일 방식
