# 2026-06-07 (v2) — CC ※recap 절대규칙 격상 + BGM 곡 풀 채움

## 한 줄 요약
⑦ 외부 대기 중 ⑦-독립 작업 2건 처리. CC ※recap 금지를 CLAUDE.md 절대 규칙으로 격상(커밋 9302871). BGM 곡 풀 calm/lively/epic 각 최소 1곡 S3 업로드 완료(운영자, 코드 무관). 나머지는 ⑦ 대기 그대로.

## 본 세션 커밋 (본 앱 git)
- **9302871** `docs: escalate CC meta-comment ban to absolute rules` — CLAUDE.md 2곳 수정 (push 완료, d357f6a..9302871)
  - 1 file changed, 2 insertions(+), 1 deletion(-)

## 외부 작업 (이번 세션 실제로 바꾼 것)
- **BGM 곡 풀 — calm/lively/epic 각 최소 1곡 S3 업로드 완료** (운영자, AWS 콘솔). 키 경로 `audio/{mood}/*.mp3`. 코드 변경 0건 — 코드가 폴더 list로 자동 픽.

## 본 세션 결정·발견

### CC ※recap 금지 — 절대 규칙 격상 (완료)
- CLAUDE.md 편집 2곳:
  - [A] `## 절대 규칙` 맨 끝에 불릿 추가: "**CC 보고 끝 메타 코멘트 전면 금지**" (요약·recap·다음단계·"다음은 X 또는 Y"·자체 합계표 전면 금지).
  - [B] `## 작업 원칙`의 `### 메타 코멘트 금지` 본문을 절대 규칙 참조 포인터 한 줄로 교체 (중복 드리프트 방지).
- **명확화**: per-prompt 지시뿐 아니라 CLAUDE.md(작업 원칙) 레벨 규칙도 이미 무시되고 있었음. 격상은 "다음 레버 시도"지 확정 fix 아님. 또 무시되면 다음 레버는 운영 단계에서 잘라내기.

### BGM 곡 풀 — 라이선스 결정
- 상용 서비스(영상 전달)라 BGM은 **상업적 사용 허용 음원 필수**.
- 현 단계 추천: **Pixabay Music** (무료·출처표기 불필요·상업 OK). 단 기여자 업로드 기반 + 손해배상 보증 $0 리스크는 운영자 부담. 곡 자체 재판매만 금지(영상 임베드는 OK).
- 규모 커지면 유료 구독(Artlist/Epidemic Sound 류)이 표준 — 보증·검색 깔끔. YAGNI로 지금은 보류.
- 분위기 매핑: calm=차분/감성, lively=경쾌, epic=웅장/시네마틱.
- **점검 후보**: 기존 `public/audio/bgm.mp3` 출처/라이선스 미확인. bgm.mp3 정리 작업 때 같이 점검.

### 컨텍스트/세션 운용 (논의, 코드 무관)
- CC: 긴 세션 = context rot(드리프트·비용↑). `/clear`=하드리셋(CLAUDE.md 재로드), `/compact`=요약 후 계속, `--resume/--continue`=옛 컨텍스트 복원(안 비워짐). CLAUDE.md는 세션 시작 시 로드.
- 채팅(claude.ai): 같은 원리 + 200K 창. CC와 달리 메모리 요약 + 지난대화 검색으로 연속성 일부 자동 보존(Project 범위). 단 메모리는 손실/지연 → 핸드오프가 여전히 진짜 다리. 코드실행 켜지면 긴 대화 자동 요약. 사용량은 claude.ai+CC+데스크톱 합산.
- 리셋 리듬: CC=atomic 작업 단위, 채팅=트랙/세션 단위(추론 줄기 보존).

## 미완 / 대기 (다음 세션 우선순위)

1. **⑦ Shotstack raw 로그 답신 대기** (외부, 운영자 액션 없음) — 그대로 대기. 답신 분기: 등록 키가 옛 키면 재등록 실효 / 새 키 맞는데 거부면 Shotstack 버그 확정.
2. **CC recap 격상 검증 미완료** — 9302871 커밋 보고는 깨끗했으나 변수 2개 섞임: (a) 그 CC 프롬프트 최상단에 recap 금지를 또 박았음, (b) 그 CC 세션은 CLAUDE.md 수정 전 시작 = 새 규칙을 규칙서로 안 들고 있었을 수 있음. **진짜 검증**: 새 CC 세션(--resume 말고) + recap 금지 문구 뺀 프롬프트 → 깨끗하면 격상 성공 / 또 붙으면 다음 레버. ※ 이 핸드오프를 커밋한 CC 프롬프트가 그 첫 시험 기회였음(recap 금지 의도적으로 뺐고 새 세션 실행). 그 보고에 recap 붙었는지 채팅 클로드가 확인 예정.
3. **BGM 실제 렌더 검증** — ⑦ 풀리면 즉시. calm/epic에 다른 곡 있으니 소리로 S3/폴백 구분 가능.

## 다음 세션 후보 (우선순위)
- (대기) ⑦ raw 로그 답신 → 분기
- (대기) BGM 렌더 검증 — ⑦과 동시
- (중) CC recap 격상 검증 — 다음 CC 작업 때 recap 금지 빼고 시험
- (중) ② 앱 고착 해소(rendering 박힌 이벤트 6개+) — ⑦과 동시
- (소) BGM 곡 풀 추가 채우기 (운영자 병렬, 분위기당 10곡 목표)
- (소) public/audio/bgm.mp3 정리 + bgm.mp3 출처/라이선스 점검 (BGM S3 동작 확인 후)
- (소) 버킷 Object Ownership 복원 (⑦ 해결 후)

## 본 세션 학습
- **규칙서(CLAUDE.md) 변경은 새 세션부터 로드된다** — 수정 직후 같은 CC 세션에서 검증하면 옛 규칙서 + 자기가 방금 쓴 텍스트가 섞여 신호가 오염됨. 규칙 변경의 효과 검증은 반드시 새 세션에서.
- **채팅의 연속성은 자동이되 손실 있음** — 메모리+지난대화 검색이 CC엔 없는 연속성을 주지만 손실·지연이라, 결정·정밀 추론은 핸드오프로 못박아야 함.
