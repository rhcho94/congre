## 세션 한 줄 요약

PR 2 (참가자 결과 SMS 트리거 연결) 사전 결정 등록·실행·배포 검증까지 완료. 검증 중 renderStartedNotifiedAt null 의제 발견·known-issues 등록. 다음 세션 진입점은 약관·자동 삭제 묶음 작업으로 운영자 결정 보류.

## 본 세션 커밋

- `c70902c` docs(decisions): add participant result SMS channel decision (PR 2 prep) (+14/0, 1 file)
- `b1db07e` feat(notify): wire participant result SMS trigger (PR 2) (+38/-9, 6 files)

## PR 2 실측 변경 범위

### 코드 (3 files)

- `src/app/api/cron/check-rendering/route.ts` (+32) — done 분기에 참가자 SMS 트리거 블록 삽입. import 2개(FieldValue, notifyParticipantResult) + 트리거 본문 30줄(멱등성 가드 → clips JS 필터 → distinct phone Set → for 루프 발송 → 플래그 기록)
- `src/app/api/render/start/route.ts` (+1) — notifications 초기화 객체에 `"notifications.participantNotifiedAt": null` 추가
- `src/lib/notifications/sms-templates.ts` (+1/-1) — participant_result 본문 "준비되었습니다" → "준비됐어요" (DECISIONS 사양 정합)

### 문서 (3 files, PR 2 본 커밋)

- `docs/CHANGELOG.md` — 항목 추가
- `docs/ROADMAP.md` — PR 2 항목 완료 + 진입점 "PR 3 미정"으로 압축
- `docs/PROJECT.md` — "트리거 연결 5건 → 6건", "함수만 구현 2건 → 1건"

### 문서 (사전 등록 단독 커밋)

- `docs/DECISIONS.md` — "참가자 결과 알림 채널: SMS 단독 (PR 2 사전 등록)" 항목 추가

## 사양 확정본 (구현됨)

### 트리거 위치

`src/app/api/cron/check-rendering/route.ts` done 분기 안, `notifyRenderCompleted .catch()` 직후, 분기 닫는 `}` 직전

### 발송 흐름

1. 멱등성 가드: `data.notifications?.participantNotifiedAt == null` 체크
2. clips Firestore 조회 (`where eventId == eventId`)
3. JS 필터: `excludedAt` 없는 클립만 (DECISIONS 2026-05-08 패턴 일관)
4. distinct uploaderPhone: `[...new Set(...)]` (친구 폰 돌려쓰기 케이스 1건만)
5. uploaderPhone 없는 clip 방어 필터 (`filter((p): p is string => !!p`)
6. for 순차 루프 + `.catch()` (Promise.all 아님 — SOLAPI rate limit 회피, 1건 실패가 다른 발송 막지 않게)
7. 플래그 기록: `"notifications.participantNotifiedAt": FieldValue.serverTimestamp()` (부분 성공도 set)

### SMS 본문

`[Congre] '{eventTitle}' 결과 영상이 준비됐어요: {videoUrl}` (이름 호칭 없음 — 한 폰 다중 사용자 케이스 + LMS 전환 회피)

### 멱등성 정책

- 부분 성공도 플래그 set → cron 재진입 시 SMS 추가 발송 차단
- 부분 실패는 known-issues 등록 + 운영자 수동 대응 (빈도 낮음 가정, YAGNI)

## 배포 검증 결과 (1폰 한정 시나리오)

테스트 이벤트 `MclmxNgLQBb8Lb6jexc9` ("멱동성테스트"):

- 같은 폰 + 다른 이름 2개 클립 업로드 → 마감 → 렌더 완료
- SMS 1건 도착 (distinct 처리 OK)
- prefix `[Congre]`, 본문 "준비됐어요" 반영 OK
- 영상 URL 클릭 → 재생 OK
- 5분 대기 추가 SMS 없음 (멱등성 OK)
- Firestore `notifications.participantNotifiedAt` Timestamp 기록 OK (9:50:10)

검증 6/6 통과.

## 별 트랙 의제 — known-issues 등록

배포 검증 중 Firestore 데이터에서 발견:

- `events.notifications.renderStartedNotifiedAt`이 null로 남음 (다른 키 정상 동작)
- 잠재 원인 후보 (정찰 안 됨): notifyRenderStarted 호출 자체 누락 / 호출 후 set 누락 / SMS 실패로 .catch()가 막음
- 영향: 멱등성 가드가 매 cron마다 통과 → SMS 중복 발송 가능성 (수신 기억 있는 경우) / 운영자가 렌더 시작 알림 못 받음 (수신 기억 없는 경우)
- 운영자 수신 기억: 본 세션에서 명확 답 없음 — 다음 정찰에서 확인
- known-issues.md에 OPEN 상태로 등록

## 다음 세션 진입점

**A. 약관·개인정보 처리방침 작성 + 데이터 자동 삭제 코드 묶음 작업** (운영자 결정 보류 영역)

배경: 직전 핸드오프(2026-05-10-pr1.md) 진입점 B(약관)는 채팅 클로드 단독 작업이었으나, 본 세션 사전 정렬에서 다음이 발견됨:

- 보유 기간 사양: 개별 클립 = 완성본 전달 후 24시간, 완성본 = 완성 후 7일
- 자동 삭제 코드 미존재 (clips Firestore + S3 파일 + renders 서브컬렉션 모두)
- DECISIONS 2026-05-09에 이미 "약관 콘텐츠 채움 = PR 1 push 전 필수"로 명시됨 (코드 = 약관 일치 의무)
- 약관에 보유 기간 명시하면서 자동 삭제 코드 없으면 "위법한 거짓 고지" 상태

다음 세션 시작 시 운영자가 결정해야 할 항목:
- 약관 초안 + 자동 삭제 PR을 분리 진행할지 통합 진행할지
- 자동 삭제 PR 작업 분량 정찰 (clips 24시간, videoUrl/S3 7일, renders 서브컬렉션 7일 — 본 세션 정찰 안 됨)

운영자가 본 세션에서 답한 약관 작성 결정 (다음 세션에서 사용):
- 사업자 정보: 자리만 (등록 후 추가)
- 개인정보 보호책임자: "Congre 운영팀 / hello@congre.kr"
- 데이터 보유: 클립 24h, 완성본 7d
- 만 14세 미만: 옵션 3 — 미성년자는 참가자로만 (호스트는 보호자·학교 등 성인 신청, 동의 처리는 호스트 단계)
- CS 채널: 카카오톡 채널 "준비 중" 표기
- 결제·환불: 무료 베타 운영, 유료 전환 시 별도 고지

작업 깊이: 옵션 A (출처 정독 후 항목별 정밀 작성) — 본 세션 사전 합의됨

**B. 별 트랙 진단 — renderStartedNotifiedAt 발화 누락 의제** (known-issues OPEN)

위 A 진입 전후로 처리. 정찰 진입 시:
- `notifyRenderStarted` 함수 위치·시그니처
- render/start/route.ts 안 호출 위치 (있는지)
- set 코드 위치·조건
- 운영자 SMS 수신 기억 답 (정찰 진단 분기점)

A 의 자동 삭제 PR 작업과 묶거나, A 시작 전 단독 짧은 PR로 처리 가능.

## 자체 학습

- **사전 결정 등록 패턴 효과 확인**: PR 2 본 작업 진입 전 "참가자 결과 알림 채널: SMS 단독" 단독 docs 커밋으로 등록 → 본 작업 커밋이 깔끔하게 atomic. 직전 PR 1 자체 학습("ROADMAP·known-issues 자연 합류")의 변형 패턴. 결정 본문이 미리 명확하면 사전 등록, 정찰 결과로 본문이 풍부해질 가능성 있으면 본 커밋 합류 — 이 분기는 운영자가 작업 시작 시 결정 가능.
- **배포 검증 시나리오 B의 Step 6 효과**: 시나리오 작성 시 "Firestore 데이터 확인"을 선택으로 뒀는데, 운영자가 진행한 결과 별 트랙 의제(renderStartedNotifiedAt) 발견. 다음 PR 검증 시 Firestore 직접 확인 단계는 필수로 두는 패턴 검토.
- **운영자 표현의 가벼움 vs 실제 작업 무게 정렬**: "하나만 더 하자"가 별 PR 규모(약관 + 자동 삭제 코드)였음. 작업 진입 전 정렬 단계가 분량 사고 차단. 다음 세션도 "가볍게" 표현 들어오면 즉시 정렬 단계 유지.
- **함수 시그니처 선해석**: PR 2 정찰에서 CC가 `notifyParticipantResult`의 이메일 분기 잔존을 "DECISIONS 결정과 모순"으로 보고했는데, 실제는 양채널 지원 가능 함수 + 호출자에서 SMS만 넘기는 구조 = DECISIONS 결정 준수. 코드 리뷰 시 "함수 본문 형태"와 "호출 경로"를 분리해서 봐야 함. CC 정찰 보고를 그대로 받지 말고 호출 관점 한 번 더 검증하는 패턴.

## 다음 세션 시작 시 운영자 작업 — 컨텍스트 셋트

진입점 A(약관 + 자동 삭제 묶음)일 경우 5개 셋트:

- CLAUDE.md
- AGENTS.md
- 이 핸드오프 (2026-05-10-pr2.md)
- DECISIONS.md (특히 2026-05-07 "비공식 기념품, 즉시 삭제" + 2026-05-09 "약관·처리방침 페이지 신설" + 2026-05-09 "완성본 보존 정책 D2 7일" + 2026-05-10 "참가자 결과 알림 채널" 항목 대조 필수)
- PROJECT.md (디자인 토큰 + 데이터 모델 참조용)

ROADMAP, known-issues, 직전 핸드오프(2026-05-10-pr1.md)는 일단 빼고 시작. 정찰 중 필요해지면 추가.

진입점 B(renderStartedNotifiedAt 진단)일 경우 5개 셋트:

- CLAUDE.md
- AGENTS.md
- 이 핸드오프 (2026-05-10-pr2.md)
- DECISIONS.md (2026-05-04 render_delayed 정책, 2026-05-03 채널 정책 대조)
- known-issues.md (해당 항목)

첫 메시지 한 줄 (진입점 A):
"진입점: 약관·처리방침 + 데이터 자동 삭제 묶음. 컨텍스트 5개 셋트만. 외엔 모르는 것으로 간주, 진단·결정 시 모르는 영역이면 명시. 작업 분리/통합 결정 먼저, 그 후 정찰."

첫 메시지 한 줄 (진입점 B):
"진입점: renderStartedNotifiedAt 발화 누락 진단 (known-issues OPEN). 컨텍스트 5개 셋트만. 정찰만, 코드 변경 금지."
