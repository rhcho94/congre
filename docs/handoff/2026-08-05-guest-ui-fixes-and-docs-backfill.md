# 2026-08-05 핸드오프 — 게스트 UI 버그 트랙 종료 + 문서 backfill

## 한 줄 요약
직전 세션에서 이월된 게스트 화면 UI 버그 4건을 정찰 4회·커밋 5개로 처리 완료. 베타 쿠폰 트랙(b0e7988)의 정식 문서 미반영분도 함께 backfill. 진단이 네 번 뒤집힌 세션이라 그 이력을 아래에 남김.

## 이번 세션 커밋 (전부 push 완료)
- `5da2123` fix(upload): 게스트 화면 문구 정리 — 이벤트 문서 ID 노출 제거, 제목 접미사 "영상입니다" 중복 해소(제목은 h1 단독 노출로 일원화), 이름 입력 안내에 닉네임 허용 문구 추가. 2파일.
- `b950821` fix(create): 에러 코드 키 통일 + 쿠폰 에러 메시지 매핑 + 라벨 표기·자간 정정 — api/events/route.ts의 에러 응답 10건 중 L107만 `code` 키였던 것을 `error`로 통일, 프론트가 읽는 키를 맞추고 INVALID_COUPON·COUPON_ALREADY_USED 사용자 메시지 추가, catch에 console.error 보강(CLAUDE.md 절대 규칙 미준수분 회수), "갯수"→"개수" 3곳 + eyebrow 자간 국소 해제 4곳. 3파일.
- `83bec08` fix(guest): 스크림 농도 상향 + 한글 줄바꿈 정정 — upload·share 공통 scrim 0.55~0.75 → 0.78~0.90, 안내 문구에 break-keep. 3파일.
- `a2344b9` fix(guest): 게스트·공유 화면 제목 색 직접 지정 — 두 h1에 인라인 `color: "#ede8df"`. 3파일.
- `6ed4aaa` docs: 베타 쿠폰 트랙 반영 + 게스트 화면 색 상속 이슈 등재 — CHANGELOG backfill, decisions/market-product.md 결정 추가, known-issues 신규 2건 + 기존 25번 갱신, DECISIONS 인덱스 정정. 4파일.
- `8db1a5d` docs: DECISIONS 인덱스 항목 수 드리프트 정정 — rendering 38→40, legal 2→3, misc 6→7. 2파일.

## 처리 완료 항목 (직전 핸드오프 이월분 기준)
1. "고길동" 사전 입력값 — **버그 아님으로 종결.** Firestore `users/hodzIB8rLxg…`(rhcho94@gmail.com)의 `name` 실값이 "고길동". 게스트 API가 `hostId → users.name` 조인 결과를 정확히 출력 중. src/ 전역 "고길동" grep 0건으로 코드 쪽에서도 닫힘. 마이페이지에서 이름 변경 시 즉시 반영되는 구조(이벤트 문서에 이름 미저장).
2. 배경 위 텍스트 대비 불량 — `a2344b9`로 처치. 근본 원인은 미확정, known-issues 등재.
3. 뭉뚱그린 에러 문구 — `b950821`로 처치. **실호출 검증은 미실시**(known-issues 등재).
4. 닉네임 안내 문구 추가 — `5da2123`으로 처치, 실화면 확인 완료.

## 세션 중 신규 발견·처리
- 제목 접미사 "영상입니다" 중복: 생성 화면이 title을 `"{users.name}씨의 축하영상입니다"`로 자동 세팅하는데(create/page.tsx prefill), 업로드 화면 템플릿이 그 뒤에 고정어 "영상입니다"를 또 붙이던 구조. 접미사 제거로 해소.
- 서버↔프론트 에러 키 불일치: 서버 10건 중 9건 `error` / 1건 `code`, 프론트는 `code`를 읽음. 기존 유일한 매핑(INVALID_CLIP_SECONDS)도 실제로는 안 걸렸을 가능성이 높았음.
- 한글 낱글자 줄바꿈("…사용해 / 요."): break-keep 부재. globals.css 전역에 word-break 계열 규칙 없음.
- DECISIONS 인덱스 항목 수 드리프트 4건: 9개 영역 전수 실측 대조로 발견, 전부 정정 완료. 세는 규칙을 "## 헤더 개수(날짜 헤더 1줄 = 1항목, 괄호 번호 미반영)"로 확정.

## 미검증 / 미확인 (다음 세션 인계)
- **쿠폰 재사용 에러 메시지 실측 미검증** — known-issues 등재됨. 이미 사용된 번호를 쿠폰 칸에 재입력해 "이미 사용된 번호예요…"가 뜨는지 확인 필요. 범용 문구가 뜨면 키 통일이 어딘가에서 안 물린 것. 다음 쿠폰 발급 시 자연 확인 예정.
- **`data-theme="dark"` 미적용 근본 원인** — known-issues 등재됨(CC 가설 + 처치 후보 3갈래 + 판별법). 화면은 인라인 color로 닫혔으므로 급하지 않음.
- **`errBody.code` 전역 잔존 여부** — create/page.tsx 외 다른 파일에서 `.code`로 에러를 읽는 지점이 있는지 미확인. 있다면 그 화면들은 여전히 매핑이 안 걸릴 수 있음.
- **`refundStatus` / `refund50At` / `refund100At` 필드 출처 미확인** — events 문서에 존재하나 CHANGELOG·DECISIONS에서 유래를 확인하지 못함.

## 이번 세션 학습 (중요)
**진단이 네 번 뒤집힘. 공통 패턴은 "간접 증거로 결론을 세우고 실측으로 확인하지 않음".**
1. "고길동" — 쿠폰 문서 ID와 테스트 계정 phone 값이 같은 것을 보고 "그 계정으로 생성된 이벤트"로 이었으나, 쿠폰은 번호 화이트리스트일 뿐 계정 소유권과 무관. → **값 일치를 인과로 읽지 말 것. 같은 문자열이 두 곳에 있으면 둘 다 제3의 출처를 참조했을 가능성부터.**
2. share와 upload 대비 문제를 같은 원인으로 묶음. 실제로는 share는 data-theme를 켜고 upload는 안 켠 상태로 원인이 서로 달랐음. → **화면이 둘이면 각각 정찰하고 각각 진단할 것.**
3. layout.tsx에 `data-theme="dark"`가 있는 것을 보고 "이미 적용됨"으로 판단해 처방 [A]를 폐기했으나, DevTools 계산값은 rgb(26,22,18)로 미적용. → **파일에 코드가 있는 것과 브라우저에서 실제로 먹는 것은 다른 층.**
4. 정찰 F2 표의 `text-foreground` 클래스만 보고 본문도 검정일 것으로 판단했으나 본문은 흰색. → **정찰은 코드를 읽지 실행 결과를 보지 않음. 색·레이아웃 문제는 DevTools 계산값이 최종 판정.**

**부수 학습**
- 사양에 "N을 N+1로" 같은 산술을 못 박지 말 것. 전제 숫자가 이미 드리프트일 수 있음. "실측해서 맞춰라 / 다르면 멈춰라"로 쓸 것. (DECISIONS 인덱스 14→15 지시가 실제로는 15→16이었던 사고)
- 손으로 동기화해야 하는 숫자는 반드시 어긋남. 9개 영역 중 4개가 드리프트였고 전부 "인덱스가 실제보다 적은" 한 방향.
- CC 실행 시간이 간헐적으로 튐(axe 6분 5초 / eye 46분 14초). 결과물은 정확했음. 원인 불명, 관측만.

## 블로커 (변동 없음)
- Toss PG 심사 대기 — 코드 밖. 추가 서류 요청 메일이 스팸함에 있을 수 있으니 확인 권장.

## 다음 세션 후보
1. **보안 감사 사이클** — `/security-review` 1회 (presigned URL, Firestore rules, render/start 결제 가드). 이전 세션에서 "카피 트랙 후"로 결정했으나 미착수. UI 트랙이 닫혔으므로 우선순위 1순위 후보.
2. `errBody.code` 전역 확인 — 짧은 정찰 1회로 닫을 수 있음.
3. Gmail 스팸 분류 재확인 (known-issues 네이버/Gmail 도달성 항목의 후속)
4. Congre Bible 포크 — 별도 인프라, 전용 프로젝트에서 진행

## 잔여 (급하지 않음)
- lint error 11개 / warning 3개 — 기존 baseline, 이번 세션 delta 0. 별도 사이클.
- `videosDeleted` 비-0 확인 — 완성본 7일 만료 도달 후.
- known-issues-resolved.md 이메일 섹션 번호 겹침 — YAGNI 방치 중.
