# 2026-06-19 핸드오프 — 에이전트 팀 1번 직원(라나) 구축 + 문서 책임 합의

## 한 줄 요약
프롬프트 복붙 워크플로를 CC 서브에이전트 팀으로 전환하는 트랙 착수. 첫 직원
"라나"(외부 리서치) 정의·커밋·시운전 성공. 문서 책임자=코난 합의. 다음은 정찰관.

## 본 세션 커밋 (push 안 함, Ray 몫)
- 4467e4a chore(agents): track .claude/agents and add lana subagent
- 2c59313 docs(known-issues): record unverified lana findings on OG proxy (L10)
- (직전) docs(rules): CLAUDE.md에 룰 3건 반영

## 핵심 결정·발견
- 신규 버전 3종(꽁그래/바이블/복지) = 멀티버티컬 아니라 별개 포크(Ray 결정).
- 호칭: 채팅=코난, 리서처 서브에이전트=라나(name: lana), 운영자=Ray.
- 공용 에이전트 위치 = 꽁그래 코어 repo .claude/agents/ (홈 아님). 라나는
  꽁그래 스택 전문가라 만능 아님 + C:\projects\에 pickleball-club-manager가
  따로 있어 홈에 두면 거기까지 샘. 포크 시 lana.md 자동 복사됨.
- 문서 책임자 = 코난. 코드-문서 불일치는 "의도된 지연" 빼고 코난 책임. 전제 =
  모든 코드 변경 커밋이 코난에게 보고됨(보고 다리 = 생명선). 코드/문서 분리는
  업계 보통 아닌 우리 특수설계(비개발자 1인+멀티에이전트), 그래서 보고 다리 필수.
- CC는 세션 시작 시 agent 명부 1회 스캔 → 세션 중 만든 에이전트는 CC 재시작 후 등록.
- git check-ignore -v는 부정(!) 룰도 출력하는 게 정상. 실질은 git ls-files로 확인.
- docs/superpowers/ = 옵시디언 아니라 CC 플러그인(Jesse Vincent). 우리가 손으로
  만드는 계획→실행→2단계검토의 기성품. 지우지 않고 보존 — 손제작 vs 갈아타기
  비교 대상(지금 결정 X, YAGNI).

## 목표 팀 / 진행도
- 라나(리서치, 읽기전용+웹) ← 완료·시운전 성공
- 정찰관(내부 코드 정찰, 읽기전용) ← 미착수 (다음)
- 실행관(코드 수정+atomic 커밋, push 안 함) ← 미착수
- 검증관(사양 대조+빌드 점검, 읽기전용) ← 미착수
- 순서: 라나(바깥)→정찰관(안)→Ray 승인→실행관→검증관

## 라나 시운전 결과 (참고)
- 호출·읽기전용·출처표시·보고형식 전부 통과. 발견 3건은 L10에 미검증 기록.
- 끝맺음에 "다음 액션은 채팅 클로드에서 결정" 한 줄이 남음 → 라나 파일 끝맺음
  룰 한 번 조일 후보(경미, 급하지 않음).

## 미완 작업 (다음 세션 가장 먼저)
1. 새 CC 세션 열기(라나 명부 등록 위해). 이미 한 번 켰으면 등록됨.
2. 정찰관 서브에이전트 설계→생성→CC 재시작→시운전.
   정찰관 첫 시운전 임무 = L10 라나 발견 3건을 코드로 실측.

## 다음 세션 후보 (우선순위)
1. (高) 정찰관 구축 + L10 실측 → 이어서 검증관 = 1단계 팀 완성.
2. (中) 2단계 셋업: CC가 직원 자동 호출(자동 위임) → Ray 복붙 노동 감소 시작.
3. (보류) _shared 전역 문서 골격 + CLAUDE.md 룰을 거기로 이관 정리.
4. (高/이월) FGT 실제 실행 — 코드·보안·랜딩 준비 끝, 운영 결심 영역.

## 본 세션 학습
- 직원 1명으론 Ray 복붙 동작 안 줌. 감소는 2단계(CC 자동 호출)부터. 1단계는
  "되는지" 검증 단계지 "편해지는" 단계 아님 — 기대치 정렬.
- 모르는 폴더는 지우기 전 정체 확인(superpowers를 옵시디언으로 오인할 뻔).
- 코난·CC는 다른 층 → 초기화도 따로. 라나 등록은 CC 재시작에만 반응, 코난
  새 세션과 무관.
