## 세션 한 줄 요약

안드로이드 hotfix 검증 통과 → idle 시간 안내 가독성 사고 발견·정정 →
사양 D 페이지 영역 실행 (게스트·호스트 가이드 + 랜딩 동기화). atomic 3커밋.
PDF 갱신은 다음 세션 이월.

## 본 세션 커밋

| # | 해시 | 메시지 | 핵심 변경 |
|---|---|---|---|
| 1 | 5732ae8 | fix(upload): improve visibility of max clip duration notice on idle screen | line 444 `text-xs + opacity-60` → `text-sm`. 박스 안 시간 안내 가독성 강화. |
| 2 | 2a25295 | docs(guide): merge guest STEP 02·03 to reflect native capture flow | 게스트 가이드 STEP 02·03 통합 (3-STEP 구조). "최대 16초" 하드코딩 제거 + maxClipSeconds 범위 안내 + 갤러리 안내 + 카메라 권한 표현 갱신. 랜딩 GUEST 카드 4 → 3 STEPS 동기화. |
| 3 | 4a52a8a | docs(guide): add max clip duration field to host STEP 02 + sync PROJECT | 호스트 가이드 STEP 02 입력 항목 5개로 (클립 길이 신규). PROJECT.md hotfix + idle 가독성 누락분 보강. |

origin/main 반영 완료.

---

## Track A — 안드로이드 hotfix 검증 (직전 세션 be7cef9)

### 검증 절차

운영자 안드로이드 단말에서 다음 항목 확인:
1. "지금 촬영하기" 큰 박스 → 카메라 앱 직행
2. "갤러리에서 선택" 보조 링크 → 갤러리 직행
3. 촬영 후 자동 복귀 + duration 표시 + 업로드 흐름

### 결과

**3개 항목 모두 통과**. 운영자 추가 코멘트: "업로드된 영상 보는데 화질·화각·
화면 비율이 익숙하고 좋음" — native capture 전환의 부수 이득 호평. 사양 B
전환 의도된 효과 확인.

### 학습

운영자 한 마디 추가 보고("촬영 시간 관련한 안내 메시지는 전혀 없음")로
다음 사고(Track B) 진입. 검증 단계가 사고 발견 트리거로 작동.

---

## Track B — idle 화면 시간 안내 가독성 (5732ae8)

### 사고 발견

운영자 보고: "촬영 시간 관련한 안내 메시지는 전혀 없음".

채팅 클로드 1차 진단: 영역 1(idle 화면 안내) vs 영역 2(카메라 앱 안 제한)
구분 후 영역 1로 확정. 정찰 프롬프트 작성.

### CC 정찰 결과 (정확)

- `src/app/upload/[eventId]/page.tsx` line 444에 "최대 {maxClipSeconds}초 ·
  탭하여 시작" 이미 존재
- 스타일: `text-xs + text-muted + opacity-60` 삼중 약화로 안 보임
- maxClipSeconds 클라이언트 보유 확인 (line 288 파생 상수, GET 응답 포함)

### 채팅 클로드 본인 view 추가 발견

CC 보고 없는 정보:
- line 441 "지금 촬영하기"도 text-muted, group-hover로 accent 전환
- 박스 안 muted 균일성 + opacity-60으로 위계 만든 디자인 의도

### 결정

운영자 G1 선택 (박스 안 강조만, 박스 위치 그대로). 옵션 B 추천 (opacity
제거 + text-sm). 디자인 의도 유지 + 시인성 한 단계 강화.

### 실행

`text-xs + opacity-60` → `text-sm`. 디자인 토큰 변경 없음.

---

## Track C — 사양 D 페이지 영역 (2a25295, 4a52a8a)

### 정찰

운영자 결정 (직전 세션 핸드오프): 게스트 가이드 전면 재작성 + 호스트 가이드
정찰 + PDF 갱신.

본 세션 정찰 결과 (4영역):

| 영역 | 상태 |
|------|------|
| 게스트 가이드 STEP 02 (카메라 켜기) | 전면 stale — "카메라 켜기" 탭 표현, 전·후면 전환 섹션, 미리보기 단계, "최대 16초" 하드코딩 |
| 게스트 가이드 STEP 03 (촬영하고 확인하기) | 전면 stale — "촬영 시작 누르면 녹화" + 자동/수동 정지 표현 모두 native capture 전 흐름 |
| 호스트 가이드 STEP 02 (새 이벤트 만들기) | maxClipSeconds 입력 항목 누락 |
| 랜딩 line 330·337~342 | "GUEST · 4 STEPS" + 4단계 목록 stale |
| 랜딩 "16초" 하드코딩 | **없음** (grep 확인). 신규 손볼 영역 0건 |

PDF 정찰 의외값: 첨부 PDF 3개 (guest 단독 6p, host 단독 8p, 합본 user-guide
15p). 핸드오프 원안은 게스트 PDF 1개만 언급. PDF 갱신 범위 3개로 확장.

### 작업 범위 결정

운영자 결정: **D1c** (페이지만, PDF 분리, 2커밋)
- 커밋 1: 게스트 + 랜딩 (사양 B 영향)
- 커밋 2: 호스트 + PROJECT.md (사양 A 영향)
- PDF: 별개 작업으로 이월

### 게스트 사양 결정 5건 (운영자 영역)

1. STEP 02 제목 → "촬영 시작"
2. STEP 02·03 통합 → 3-STEP 구조 (이름 → 촬영 시작 → 업로드 완료)
3. "갤러리에서 선택" 안내 포함
4. 시간 안내 → "호스트가 정한 최대 시간 (5~30초). 촬영 화면에서 N초 확인.
   넘으면 잘립니다."
5. 카메라 권한 안내 유지 + 표현 변경 ("폰이" 주체)

추가 결정: 중복 영역 처리 (옵션 a — 첫 불릿 짧게 + "촬영이 끝나면" 섹션
유지) + 카메라 권한 위치 (첫 불릿 후 → 시간 전).

### 호스트 사양 결정 4건 (운영자 영역)

1. 신규 항목 위치 → 플랜 다음, 알림 수신 정보 앞
2. 항목명 → "클립 길이 — 참가자가 올릴 수 있는 영상 최대 길이. 5~30초 사이
   선택 (기본 15초)." (풀어 설명)
3. 팁 영역 보강 없음 (YAGNI)
4. PROJECT.md 갱신 동반

### 커밋 1 검증 표 (2a25295)

| 항목 | 결과 |
|------|------|
| guest "네 단계" → "세 단계" | 일치 |
| STEP 02·03 통합 (+16/-20) | 산술 일치 |
| 랜딩 헤더 + 목록 동기화 (+2/-3) | 일치 |
| 빌드 통과 | 통과 |
| atomic 1커밋 | 통과 |

### 커밋 2 검증 표 (4a52a8a) — 의외값 1건

| 항목 | 결과 |
|------|------|
| host 클립 길이 항목 추가 | 일치 |
| **PROJECT.md 사양 일탈** | CC가 정찰에서 사양 A·B·C가 line 119~121에 이미 존재함을 발견하고 자체 판단으로 누락분(hotfix + idle 가독성 2항목)만 추가. 원 사양은 통합 1항목 추가였음. |
| 빌드 통과 | 통과 |
| atomic 1커밋 | 통과 |

**의외값 처리**: 운영자 결정 A (결과 받아들임). CC 자체 판단이 결과적으로
더 정확한 반영을 만들어냄. 본인이 PROJECT.md 정찰 안 한 게 원인.

---

## 본 세션 학습

### 1. 단편 단서 → 전체 단정 패턴 누적 5회 → 본 세션 0회 추가

직전 세션 hotfix 검토 중 5회 도달했고 CLAUDE.md 정식 등재 검토 시점으로
넘어간 상태. 본 세션 신규 발견 0건. 패턴 빈도 줄어듦 관측.

### 2. 검증 표 룰 사고 발견 효과 입증 (커밋 2 사고)

CC가 커밋 2 프롬프트 받지 못한 채 커밋 1 보고를 재출력한 사고에서 검증 표
1행("변경 파일이 host/page.tsx가 아닌 guide/guest/page.tsx") 작성 직후
발견. 검증 표 형식이 의외값 즉시 포착 장치로 작동.

### 3. 사양 정찰 영역 누락 (PROJECT.md "완료된 기능")

채팅 클로드가 사양 D 정찰할 때 PROJECT.md를 정찰 범위에 안 넣음. 사양 A·B·C가
이미 갱신돼 있었음을 모르고 통합 1항목 추가 사양 작성. CC가 자체 정정.

**학습**: PROJECT.md "완료된 기능" 섹션은 CHANGELOG와 다르게 누적 아닌
갱신 영역. 사양 작성 시 정찰 필수.

### 4. CC 자체 판단 영역 확장 관측

이전엔 사양 충돌 시 CC가 멈추고 운영자에게 묻는 패턴이 일반적. 본 세션
커밋 2에서 CC가 사양 일탈을 자체 결정으로 진행 (충돌 아닌 누락 보강).
결과는 정상이지만 패턴 추적 필요. 향후 사양 작성 시 "예상과 다르면 멈춰"
지시 명시 검토.

### 5. 외부 검색 트리거 자체 발동 (본 세션 0회 적용)

직전 세션 학습 항목. 본 세션엔 외부 자료 필요 영역 없었음 (모든 영역이
프로젝트 내부 코드·문서·결정). 다음 외부 자료 필요 영역(예: PDF 디자인
모범 사례, 가이드 페이지 UX 패턴) 발생 시 검증 항목.

### 6. atomic 커밋 원칙 견고 (3/3 통과)

본 세션 3 커밋 모두 atomic. 빌드 깨진 채 중간 커밋 0건. D1c 페이지 영역을
2커밋으로 분리한 결정이 결과적으로 적정 — 사양 B 영향(게스트+랜딩)과 사양 A
영향(호스트+PROJECT)이 다른 사고 영역이라 메시지 명확.

---

## 보정 후보 누적 큐 (다음 세션 첫 작업 후보)

본 세션 처리 없음. 잔여 2건 + 본 세션 추가 1건:

| # | 항목 | 출처 |
|---|------|------|
| 1 | globals.css 전체 재점검 + PROJECT.md 디자인 토큰 섹션 완전 동기화 (`--accent-bright: #e8a038` 추가 + 기타 누락 토큰) | 2026-05-15 이전 세션 |
| 2 | 단편 단서 → 전체 단정 패턴 누적 5회 도달 → CLAUDE.md 정식 등재 검토 | 직전 세션 |
| 3 | CC 자체 판단 영역 확장 패턴 추적 → 사양 작성 시 "예상과 다르면 멈춰" 명시 룰 검토 | 본 세션 |

---

## 미해결 — 다음 세션 진입점

### 다음 세션 작업 후보

| 후보 | 비고 |
|------|------|
| **PDF 갱신** (D1c 분리된 PDF 영역) | 채팅 클로드 본인 작업. 3개 PDF (guest 6p, host 8p, user-guide 15p). 게스트 STEP 02·03 + 호스트 STEP 02 + 스크린샷 갱신. 본 세션 정찰만 완료. |
| **iOS 필드테스트** | 운영자 "다음 날 가능"이라고 본 세션 명시. 사양 A·B·C + hotfix + idle 가독성 + 가이드 페이지 모두 통합 검증. 양쪽 OS 검증 완료 시 사양 시리즈 완전 종료. |
| **보정 후보 #1 globals.css 토큰** | 짧은 단발성 작업 |
| **보정 후보 #2 CLAUDE.md 룰 등재** | 단편 단서 패턴 5회 도달 → 정식 룰 검토 |

### 거론 보류 큐 (이전 세션 계승)

- 재렌더 UX 갭 (done 상태 버튼 미노출)
- 완성본 단일 필드 덮어쓰기 → 서브컬렉션 전환 (D2)
- 클립 메타데이터 저장 실패 (S3 고아 파일)
- 호스트 클립 제거 시 영상 미리보기 흐름 검증
- 영상 호스팅 CDN 이전 (R2 vs Shotstack)
- 미성년자 영상 법적 리스크
- 네이버 메일 도달성
- BGM 다양성 격상
- 야외 환경 음량 검증

### 보류 큐 (운영자 트리거 대기)

- Track 4 작은 버튼 3건 부담 가능 (실전 후 평가)
- Track 5 빈 텍스트 의도적 비우기 불가 (YAGNI 보류)

### 예정된 작업 트리거

- AWS 무료 트라이얼 만료 30일 전 (~2026-09-28)

---

## 다음 세션 시작 시 운영자 작업 — 5개 셋트

진입점: **iOS 필드테스트 결과 회고** 또는 **PDF 갱신**.

- CLAUDE.md (자동 첨부)
- AGENTS.md (자동 첨부)
- 본 핸드오프 (`2026-05-16-android-hotfix-validation-and-guide-refresh.md`) ←
  운영자 직접 첨부
- DECISIONS.md 인덱스 (자동 첨부)
- known-issues.md (자동 첨부)

### 진입 시나리오별 추가 필요 자료

| 진입 시나리오 | 추가 첨부 |
|--------------|----------|
| iOS 필드테스트 결과 회고 | 운영자 관찰 메모 (자유 형식) |
| PDF 갱신 | 3개 PDF 파일 (`congre-guest-guide.pdf`, `congre-host-guide.pdf`, `congre-user-guide.pdf`) + `src/app/guide/guest/page.tsx` + `src/app/guide/host/page.tsx` (PDF와 페이지 텍스트 동기화 확인용) |
| globals.css 토큰 보정 | `src/app/globals.css` |

**첫 메시지 한 줄**:

"본 핸드오프 외엔 모르는 것으로 간주. 진단·결정 시 모르는 영역이면 명시.
CC 보고 받으면 검증 표 먼저. 이번 세션 진입점은 [영역명]."

### 다음 세션 채팅 클로드 본인용 노트

- **PROJECT.md "완료된 기능" 섹션은 정찰 필수**. CHANGELOG와 다르게 갱신
  영역. 본 세션 커밋 2 사양 작성 시 누락으로 CC 자체 정정 발생.
- **CC 자체 판단 영역 확장 관측**. 본 세션 커밋 2에서 사양 일탈을 자체
  결정으로 진행. 결과 정상이었으나 패턴 추적 필요. 향후 사양 명시: "예상과
  다르면 멈추고 보고".
- **PDF 갱신 작업**: 본인 영역. 3개 PDF + 스크린샷 영역 포함. 컨텍스트
  무게 크니 새 세션 시작 시 진입. 운영자가 PDF를 채팅 클로드가 만든 것이라
  명시.
- **검증 표 룰 사고 발견 효과 입증**. 검증 표 첫 행 작성 직후 의외값 포착.
  형식이 사고 발견 장치로 작동. 룰 유지.
- **atomic 1커밋 원칙 견고**. 본 세션 3 커밋 모두 통과. 빌드 깨진 채 중간
  커밋 0건.
