# 2026-05-30 (2) — V5 R10 + 가격 UI 4장 일괄 배포 + 사이클 4 결정

## 본 세션 한 줄 요약

직전 핸드오프(2026-05-30-landing-v5-r4-to-r9-live.md) 이어받음. V5 R10 (8섹션 swap + R4 압축) + 워터마크 사양 확정 (40px / 0.40) + 가격 표시 UI 4장 (덩어리 2) CD 작업 → 사이클 1+3 zip 일괄 배포 → `www.congre.kr` + `www.congre.kr/pricing` V6 + Pricing 라이브 반영. 사이클 4 (본 앱 리뉴얼 + 버튼 연결) 결정만 박음, 다음 세션 작업.

## 본 세션 커밋

본 세션 작업 = CD 작업 + 랜딩 zip 배포 영역. git 커밋 0건 (랜딩 트랙은 git 외부). 본 사이클 docs 정리만 git 커밋 영역. 본 CC 프롬프트 1커밋.

## 본 세션 결정·발견

### 사이클 1+3 라이브 반영 마감

- V5 R10 8섹션 swap (Hero 직후 Showcase) + R4 (Showcase) 17인치 viewport 압축
- 워터마크 사양 확정 (MEDIUM = 40px / 0.40, 무료 플랜만)
- 가격 표시 UI 4장 (무료·소형·중형·라지) — Pricing Section.html이 deploy/pricing.html 통째 교체

라이브 반영:
- `www.congre.kr` (Landing v6.html → index.html)
- `www.congre.kr/pricing` (Pricing Section.html → pricing.html)

### 사이클 4 결정 (다음 세션 진입 전 컨텍스트 박음)

| 결정 영역 | 채택 |
|---|---|
| 본 앱 디자인 정정 범위 | (다) 전 페이지 리뉴얼 |
| 랜딩 ↔ 본 앱 흐름 | (가) 사전예약 모달 제거 + 모든 시작하기 버튼 → 본 앱 직행 |
| 본 앱 리뉴얼 작업 도구 | (가) CD 시안 → CC 본 앱 코드 반영 |
| 사전예약 모달 처리 | (나) UI만 제거 + 백엔드(리드 수집 API) 유지 |
| 가격 카드 CTA | (나) 본 앱 + 플랜 자동 선택 |
| 가격 페이지 진입 경로 | (가) nav "요금"만 유지 |
| CD 사이클 분리 | (가) 단일 CD 사이클 |
| 가입 모델 재검토 트랙 | 외부 검증 후 별도 트랙 (보류) |
| L2 (이미지 41장) | 운영자 결정: 충분, 해소 |
| L5 (모바일 마키 점검) | 운영자 결정: 보류, 이대로 |
| L7 (pricing.html Pretendard) | 운영자 결정: 보류, 이대로 |

### 사이클 4 작업 범위 (다음 세션)

- 랜딩 = 수정 없음, 버튼 연결만
- 본 앱 = 전 페이지 리뉴얼 (V5 R10 톤)

본 앱 리뉴얼 대상 12개 페이지:
- 메인 (`/`)
- 이벤트 생성 (`/dashboard/create`)
- 대시보드 (`/dashboard`)
- 이벤트 상세 (`/dashboard/events/[eventId]`)
- 마이페이지 (`/mypage`)
- 로그인 (`/login`)
- 가입 (`/signup`)
- 비밀번호 재설정 (`/forgot-password`)
- 이메일 인증 (`/verify-email`)
- 참가자 영상 촬영 (`/upload/[eventId]`)
- 약관 (`/terms`)
- 개인정보처리방침 (`/privacy`)

버튼 연결 사양:
- 랜딩 Hero "무료로 시작하기" → 본 앱 이벤트 생성 (현 사전예약 모달 제거)
- 랜딩 CTA 섹션 "무료로 시작하기" → 본 앱 이벤트 생성 (현 사전예약 모달 제거)
- 랜딩 nav "시작하기" → 본 앱 이벤트 생성
- 랜딩 nav "로그인" → 본 앱 로그인
- 랜딩 가격 4장 무료 "지금 시작" → 본 앱 이벤트 생성 + 무료 플랜 자동 선택
- 랜딩 가격 4장 소형 "시작하기" → 본 앱 이벤트 생성 + 소형 플랜 자동 선택
- 랜딩 가격 4장 중형 "시작하기" → 본 앱 이벤트 생성 + 중형 플랜 자동 선택
- 랜딩 가격 4장 라지 "문의하기" → 별도 리드 폼 (사전예약 모달 백엔드 재활용)
- 랜딩 CTA 섹션 "행사 호스트에게 전달" → 변경 없음 (운영자 확인 필요)

다음 세션 진입 전 운영자가 박을 영역:
1. 랜딩 nav "시작하기" + "로그인" 현재 어디로 연결되어 있는지
2. 랜딩 `/pricing` 4장 카드 CTA 현재 어디로 연결되어 있는지
3. 랜딩 CTA 섹션 "행사 호스트에게 전달" 현재 동작 (referral 추정 맞는지)
4. 본 앱 옛 카피 ("이 순간을 영원히" 등) → V5 톤 정정 시 운영자 카피 결정인지 CD 자체 제안인지
5. 본 앱 12개 페이지 캡처 (CD에 현 상태 보여주기 위한 영역)
6. 사전예약 모달 백엔드 API 위치 + 리드 폼 백엔드와 동일한지

## 본 세션 학습 룰 후보 (CLAUDE.md 학습 룰 섹션 격상 보류)

1. **외부 작업 영역 가설 박을 때 캡처 1장으로 진입 X** — 본 세션 R4 압축 + swap 작업 진입 영역에서 캡처 1장으로 가설 박았다가 두 번 깨짐 영역. 다음부터 운영자에게 부족 영역 1줄로 물어 답 받은 후 진입.

2. **CD 캔버스 구조에 대한 본인 모델 부정확** — 운영자 "다른 캔버스" 답 받고 zip 두 개 다르게 추정했으나 실제로는 동일 영역. 운영자 답 즉시 추정 박지 말고 한 단계 더 검증.

3. **사이클 진입 전 기존 deploy 파일 구조 점검 누락 영역** — pricing.html 신규 섹션 추가 의도로 CD 프롬프트 박았으나 실제 pricing.html은 603줄 완전 페이지였음. R 사이클 진입 전 deploy 안 기존 파일 구조 점검 영역.

본 세션 즉시 격상 안 함 (YAGNI — 1회 관측 영역). 다음 세션 검증·격상 영역.

## 미완 작업

사이클 4 (본 앱 리뉴얼 + 버튼 연결) — 다음 세션 작업.

## 다음 세션 후보 (우선순위)

### 우선순위 높음

**1. 사이클 4 — 본 앱 리뉴얼 + 버튼 연결**
- 12개 페이지 V5 R10 톤으로 일관화 (CD 시안 → CC 본 앱 코드 반영)
- 랜딩 버튼 8건 본 앱 직행 연결 (사전예약 모달 UI 제거, 백엔드 유지)
- 가격 카드 CTA → 본 앱 + 플랜 자동 선택
- 진입 전 운영자 박을 영역 6건 (위 사양 참조)
- 코드 변경 영역: 본 앱 12 페이지 + plans.ts (PLAN_MAX_CLIP_SECONDS 갱신 트리거)

### 우선순위 중간

**2. 워터마크 본 앱 코드 구현 트랙** (사양 확정 후속)
- shotstack.ts createRender 시그니처에 plan 인자 추가
- render/start route에서 eventData.plan 전달
- 무료 플랜에만 워터마크 전용 트랙 추가
- public/fonts/에 Cormorant Garamond italic ttf 추가 + timeline.fonts 등록

**3. 가입 모델 재검토 트랙** (외부 검증 후)
- 본 세션 보류 결정. 외부 검증 결과 호스트 가입 단계 이탈률 높을 시 격상.

### 우선순위 낮음 (외부 작업 의존)

**4. 회사 메일함 구축** — KI 리드 폼 수신지 복원 트리거
**5. 라이브 실측 검증** — V6 + Pricing 라이브 반영 후 운영자 실측

## 다음 세션 진입 컨텍스트

본 핸드오프 첨부 시 새 채팅 클로드가 이어받을 정보:
- V5 R1~R10 마감 + V6 + Pricing 라이브 반영 완료
- 사이클 4 결정 11건 박음 (본 핸드오프)
- 학습 룰 후보 3건 (격상 보류, 본 핸드오프에 박음)
- 본 앱 12 페이지 리뉴얼 대상 명시
- 버튼 연결 사양 9건 명시

운영자 다음 세션 첫 메시지에 포함:
- 본 핸드오프 파일 첨부
- 작업 영역 명시 (사이클 4 / 워터마크 코드 / 가입 모델 중 택1)
- 사이클 4 진입이면 위 "운영자가 박을 영역 6건" 답 박을 영역

## 작업 요약 (한 줄)

V5 R10 8섹션 swap + 가격 UI 4장 + 워터마크 사양 일괄 배포 마감 (`www.congre.kr` + `www.congre.kr/pricing` 라이브). 사이클 4 (본 앱 리뉴얼 + 버튼 연결) 결정 11건 박음, 다음 세션 작업.
