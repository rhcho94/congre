# 2026-07-12 핸드오프 — D2 완성본 이력 보존 + cleanup S3 삭제 복구

## 한 줄 요약
재렌더 시 이전 완성본이 소실되던 D2를 배열 필드(`events.videos[]`)로 해소하고, 그 과정에서 발견한 더 심각한 드리프트(Track ⑦ 이후 cleanup이 S3 완성본을 한 번도 안 지우고 있었음 = 개인정보처리방침 위반 상태)를 함께 정정. 고아 파일 11개 일회성 삭제까지 완료. 코드 3커밋 전부 push·배포.

## 이 세션 커밋 (전부 origin/main 반영)
- 33430b6 fix(cleanup): 완성본 7일 만료 시 S3 객체 실제 삭제 (+6/-2, 1파일)
- a315b05 feat(video-history): 완성본 이력 배열 videos[] 도입 (D2) (4파일)
- 3060532 feat(ui): done 화면에 이전 완성본 목록 + 다운로드 (D2) (+57, 2파일)
- (코드 아님) S3 고아 완성본 11개 / 0.322GB 일회성 삭제 — 성공 11 / 실패 0, 보존 1건

## 이 세션 결정
- **저장 구조: 서브컬렉션 → 배열 필드로 변경** (2026-05-21 D2 결정 폐기). 서브컬렉션은 Firestore 규칙 신규 match + 콘솔 수동 게시가 필수인데(과거 사고 원인), 배열은 기존 events 규칙에 얹혀 그 단계가 없다. 이벤트당 재렌더는 한 자릿수라 무제한 개수 이점이 값어치 없음(YAGNI). decisions/data-flow.md 2026-07-12.
- **`videoS3Key`는 제거하지 않고 유지**. 읽는 곳 5군데 회귀 위험. `videos[]`는 덧붙이는 이력, 현재 완성본 경로는 기존 그대로.
- **이전 완성본 UI는 A안(목록 + 다운로드만)**. 재생 버튼·인라인 플레이어 없음 — 이전 버전 수요의 본질은 "다시 보기"가 아니라 "사라지기 전에 확보"라 판단. YAGNI.
- **삭제 실패 시 포인터 보존** 원칙 확립. S3 삭제가 실패했는데 Firestore 포인터를 지우면 그 파일은 영구 고아가 된다(= 이번에 정리한 11개가 생긴 메커니즘). 성공했을 때만 포인터를 지워, 실패분은 다음 cron이 자동 재시도하게 한다.

## 이 세션 발견 (중요)
- **Track ⑦ 드리프트**: 2026-06-11에 완성본 출력지를 Shotstack CDN → 자체 S3로 옮겼는데, cleanup cron의 삭제 대상은 옛 위치(Shotstack)에 그대로 남아 있었다. 즉 **S3 완성본은 한 번도 만료된 적이 없다.** 개인정보처리방침 제3조가 "완성본 7일 후 삭제"를 이용자에게 약속하고 있어, 코드 버그를 넘어 **약관 위반 상태**였다.
- 2026-06-02 정찰이 "삭제 코드 있음"으로 결론낸 건 그 시점엔 옳았다(당시 완성본은 Shotstack CDN에 있었고 삭제 코드도 있었다). ⑦이 그 전제를 무효화한 것.
- **S3 라이프사이클 규칙 0개** 확인(AWS 콘솔). 즉 AWS가 자동으로 지워주지도 않았다.
- 실측: S3 루트 완성본 12개 / 0.337GB 중 Firestore 참조는 1개. 나머지 11개 고아.
- **Firestore 제약**: `arrayUnion` 원소 안에 `serverTimestamp()`를 넣을 수 없다. `Timestamp.now()`를 써야 한다. 사양에 명시하지 않았으면 렌더 완료 처리가 통째로 깨졌을 것.

## 미완 (다음 세션 최우선)
1. **★ 재렌더 실전 검증 (라이브)** — 코드는 배포됐으나 `videos[]`에 실제 데이터가 쌓이는 걸 아직 못 봤다. done 이벤트에서 "영상 다시 만들기" 1회 → 완료 후 (a) "이전 완성본" 섹션이 뜨는가 (b) 백필로 기존 완성본이 배열에 들어갔는가 (c) 다운로드가 되는가. **백필 로직이 실전에서 도는 첫 순간**이라 여기서 깨지면 즉시 잡아야 함.
2. 오늘 밤 KST 03:00 cron 후 Vercel 로그(Functions → /api/cron/cleanup)에서 `[cleanup] video s3 delete failed` 미발생 확인.

## 다음 세션 후보 (우선순위)
1. 위 재렌더 실전 검증 (필수 선행)
2. 결제 트랙 (Toss v2) — 재렌더 결제 게이트(③). 선행 조건이 코드 밖(사업자등록·Toss 심사)이라 진행 상황 확인 필요
3. (별도 트랙) 보안 감사 1회 (/security-review)
4. (별도 트랙) 이메일 도달성 Gmail 스팸 (SPF alignment)

## 이 세션 학습
- **저장 위치를 바꾸는 작업은 쓰기 경로뿐 아니라 삭제 경로도 짝이다.** ⑦은 "쓰기"만 옮기고 "지우기"를 안 옮겨, 한 달간 약관 위반 상태를 만들었다. 데이터 이사 시 그 데이터를 지우는 코드의 목적지도 반드시 확인.
- **스키마를 바꿀 땐 "전환 시점에 반쯤 걸친 데이터"를 따로 본다.** 새 구조로 새 데이터만 쌓으면, 옛 구조로 만들어진 데이터가 사각지대에 빠진다(백필 누락 → 고아 생성).
- **TS 타입이 필수라고 런타임이 보장되는 게 아니다.** API 응답에서 온 배열은 UI에서 항상 optional로 다룬다(5초 폴링 중 `undefined.length` 크래시).
