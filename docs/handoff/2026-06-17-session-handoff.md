# 2026-06-17 세션 핸드오프

## 한 줄 요약
랜딩 2건(가격 페이지 홈 복귀 로고 + OG 메타 신설) 완료·배포. FGT 준비
정찰(축 A/B/C) 완료 및 결정 확정 — 실행은 다음 세션으로. FGT 절차는
별도 영구 문서(`2026-06-17-fgt-prep.md`)로 분리.

---

## 이번 세션 완료 (랜딩 = git 외부 트랙, deploy/)

### 1. 가격 페이지 홈 복귀 로고
- 문제: `deploy/pricing.html`에 홈으로 빠져나갈 수단 전무(nav·로고·footer
  복귀 링크 0). 정찰로 확인.
- 처방: `<body>` 직후 `.pricing-nav` 헤더 삽입 + `<a href="/">` "Congre"
  워드마크(C만 골드). 폰트는 index 톤 맞추려 Cormorant Garamond 직접 지정
  (pricing의 `--font-display`가 Pretendard라 변수 쓰면 톤 어긋남 — 함정 회피).
- 결과: HTML +3 / CSS +18 = +21줄. 배포 후 라이브 확인 OK(로고 표시·
  클릭→홈 이동). 백업 `pricing_pre_homelogo_backup.html`.

### 2. OG 메타 신설 (카톡 미리보기)
- 발견: index.html·pricing.html에 OG 메타 **전무**였음("정해진 이미지"가
  아니라 아예 없었음 — 카톡이 본문 이미지를 임의 추출하던 상태).
- 이미지: 업로드된 세로 행사 사진을 1200×630 카드형으로 재구성
  (사진 위아래 크롭해 비중 키움 + "Congre" 로고 + 태그라인). 다크+골드
  브랜드 톤. → `deploy/images/og-image.png`.
- 메타: index.html `<title>` 직후 og:* + twitter:* 12줄 삽입.
  - og:title "Congre — 다 같이 만드는 행사 영상"
  - og:description "한 사람씩 찍으면, 하나의 영상으로. 졸업식·행사 영상을
    다 같이 만드세요."
  - og:image https://congre.kr/images/og-image.png (절대경로)
- 검증: 배포 후 curl -L로 og:image 메타 노출 + 이미지 http 200 확인.
  카카오 공유 디버거에서 새 이미지 정상 스크랩 확인.
- **남은 것(코드 아님)**: 카톡 앱 캐시 지연으로 실기기 미반영. 디버거는
  통과했으므로 시간 문제(모바일 수십분~2h, PC 더 느림). 새 대화방에서 확인.
- 백업 `index_pre_og_backup.html`.

### 알아둘 점 (랜딩)
- `og:url`이 `https://congre.kr/`인데 라이브는 www로 307 리다이렉트.
  현재 정상 작동하나, 카톡 미반영 지속 시 og:url을 www로 바꾸는 게 1차 점검.
- 백업 HTML들(`*_backup.html`)이 deploy 폴더에 누적 중. 폴더 통째 배포 시
  같이 올라갈 수 있음. 나중에 일괄 정리 권장.
- 랜딩은 git 외부라 이 변경들이 git/문서에 자동 안 남음(드리프트 주의).

---

## 다음 세션 시작점 = FGT 실행

- **FGT 영구 문서 `2026-06-17-fgt-prep.md`를 먼저 읽고 시작.**
  정찰 사실·결정·실행 목록이 거기 있음.
- 실행 ①②③(유료 비활성화+안내 / render/start 가드 / 19세 자가신고)을
  **하나의 atomic 커밋**으로. 위치는 FGT 문서 §5 참조.
- 실행 전 정할 것: ② 가드가 막을 때 사용자에게 보일 문구.
- ④ cron 실제 가동 확인(코드 외, Vercel cron) — **FGT 최우선 점검**.
  안 돌면 모든 영상이 "rendering"에서 멈춤.

---

## 이번 세션 학습 (한 줄씩)
- **stale 문서 사고**: landing.md의 "다음 사이클 분리"(계획)를 "현재 상태"로
  오독 → pricing이 계산기 모델로 이미 배포됐는데 "아직 4카드"라 단정.
  근본: 2차 소스(문서)를 1차 소스(라이브)보다 신뢰. "확인"을 라이브 fetch
  아닌 문서 읽기로 수행. → 소스 권위 우선순위(라이브>문서>메모리) 룰 메모됨.
- **추측 메우기 재발**: OG가 "정해져 있다"고 단정 → 실제론 메타 전무.
  "보통 그렇다"를 "이 코드도 그렇다"로 확장한 것. 정찰로 깨짐.
- **이미지 공유**: view는 Claude만 봄. 사용자에게 보이려면 present_files
  필수(중간에 누락해 사용자가 설명만 보던 순간 있었음).

---

## 미해결·이월
- FGT 실행 ①②③④ (다음 세션, FGT 문서 기준)
- OG 카톡 실기기 반영 확인(시간 경과 후)
- 레거시 이벤트(옛 plan 값) 삭제 — Ray가 나중에 일괄 예정
- 소스 권위 우선순위 룰 등 메모리 반영(이전 세션 제안분) — Ray 보류 중
