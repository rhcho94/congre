# 핸드오프 — 2026-05-18 v1 문서 정상화 + Kickoff 룰 외화

## 세션 한 줄 요약

본 세션 진입점은 사양 D 본문 회수였으나, 1차 정찰 단계에서 자동 첨부
자료(PROJECT.md / DECISIONS.md / known-issues.md)가 stale 상태임이
밝혀지며 진로 변경. 코드·산출물·문서 전체 정찰 후 12개 갭을 식별하고
문서 정상화 작업 1 atomic 커밋(b24adc4) + 정정 1 커밋(794664b) +
Kickoff 룰 6번 신설로 마무리. 사양 D는 작업 범위가 stale 정의였음이
밝혀져 종결 처리.

## 본 세션 커밋 (2건, 모두 CC)

| # | 해시 | 메시지 | 핵심 변경 |
|---|---|---|---|
| 1 | b24adc4 | docs: sync PROJECT.md / DECISIONS.md / known-issues.md to current code state + add handoff cross-check rule | 6 files / +57 / -43 |
| 2 | 794664b | docs: fix omissions in b24adc4 (handoff rule rationale + changelog version tag) | 2 files / +2 / -2 |

origin/main 반영 완료.

본 세션 채팅 클로드 본인 산출물 (커밋 외):
- 본 핸드오프 — present_files로 운영자 공유

---

## Track A — 사양 D 진입 → 정의 충돌 발견

### 진입점

직전 5/17 핸드오프(`2026-05-17-spec-c-validation.md`)에서 다음 세션
진입점으로 지정된 "사양 D 본문 회수". 본인이 첫 답변에 자동 첨부 자료
의존으로 정찰 프롬프트 작성, CC에 전달.

### CC 정찰 결과 (1차)

| 영역 | 결과 |
|---|---|
| rendering.md 사양 D 본문 | 사양 D 전용 결정 항목 **없음**. 사양 C 항목 끝줄에 "사양 D(구 MediaRecorder 코드 정리)만 남음" 한 줄만 |
| MediaRecorder / getUserMedia grep | 둘 다 **0건** (src/ 전체) |
| handoff 최근 3개에서 "사양 D" | 5/16 핸드오프 2개에서 사양 D = **"게스트 가이드 텍스트 재작성 + 호스트 가이드 maxClipSeconds 안내 + PDF 재생성"**으로 정의됨 |

### 발견 — 사양 D 정의 충돌

| 출처 | 사양 D 정의 |
|---|---|
| rendering.md 한 줄 메모 | "구 MediaRecorder 코드 정리" |
| 5/17 핸드오프 (본인 직전 추정) | 위 메모 그대로 계승 |
| 5/16 핸드오프 2개 | "가이드 텍스트 + maxClipSeconds + PDF 재생성" |

MediaRecorder 코드는 사양 B 실행 시 이미 제거 완료. "코드 정리"는 작업
항목으로 더 이상 존재하지 않음.

### 운영자 단서로 사양 D 종결 확정

운영자 "PDF 작업도 이미 끝난 것 같은데?" 한 마디로 본인이 직접
`congreguestguide.pdf` 본문 확인 → 2026-05-14 생성, STEP 02·03 모두
native capture 흐름으로 갱신됨 → **사양 D 잔여 작업 없음** 확정.

호스트 가이드 영역도 본 세션 영역 2 정찰에서 `/guide/host` 페이지
실재 확인됨.

---

## Track B — 컨텍스트 재정렬 정찰 (12개 갭 식별)

### 진입 트리거

운영자 "컨텍스트가 완전히 틀어진 것 같다. 현 개발상태와 개발 문서
전체를 다시 점검해서, 현상태 앞으로 해야할 개발 순서와 프로세스를
다시 잡자" 트리거.

### Phase 1 정찰 — 6개 영역 전체

| 영역 | 핵심 발견 |
|---|---|
| 1. 코드 인벤토리 | src/app/ 페이지·API 31개, src/lib/ 24개. 가이드 페이지 3종, share 페이지, render API 2종 등 PROJECT.md 미등재 영역 다수 식별 |
| 2. PROJECT.md vs 코드 | 28개 항목 중 일치 19, stale 5 (#4·#6 등), 확인 불가 4. 가이드·cron·render API 5건 미등재 |
| 3. DECISIONS 인덱스 vs 실측 | rendering 21→26 (+5), market-product 5→7 (+2) 불일치 |
| 4. known-issues.md vs 코드 | 12개 중 2건(getUserMedia·1a784d0) 이미 해결 — resolved.md 이동 후보 |
| 5. handoff 최근 7개 잔여 작업 | 15건 통합. 합본 PDF·iOS 회고·잔여 발견 다수 |
| 6. public/ 산출물 | 10개 파일. PDF 0건 (저장소 외 운영자 PC 보관) |

### 갭 처리 분류

| 영역 | 갭 | 처리 |
|---|---|---|
| A. 자동 첨부 자료 stale (메타) | A1·A2·A3 (PROJECT/DECISIONS/known-issues 자동 첨부본이 신버전과 다름) | **본 세션 처리 영역 밖**. 시스템 영역 |
| B. PROJECT.md 본문 갱신 | B1·B2·B3·B4·B5 | b24adc4 처리 |
| C. DECISIONS 인덱스 정상화 | C1·C2 | b24adc4 처리 |
| D. known-issues.md 정리 | D1·D2 (resolved.md 이동) | b24adc4 처리 |
| E. 잔여 개발 작업 | E1 합본 PDF, E2 globals.css, E3 SVG 잔존 | 미처리 — 다음 세션 후보 |

---

## Track C — 문서 정상화 실행 (b24adc4)

### 작업 1. known-issues.md 정리

- "getUserMedia constraints 처방 (5Mbps)" 항목 → resolved.md 이동
- "1a784d0 회귀 의심 → 미재현 확인" 항목 → resolved.md 이동
- 두 항목 모두 종결 일자(2026-05-18) + 종결 사유 한 줄 추가

### 작업 2. DECISIONS.md 인덱스 정정

- rendering 영역: 21개 → **26개**
- market-product 영역: 5개 → **7개**

### 작업 3. PROJECT.md "완료된 기능" 갱신

| # | 처리 |
|---|---|
| B1 | "참가자 영상 촬영 (카메라 미리보기 → 촬영 → 업로드)" → "참가자 영상 업로드 (이름·전화번호 입력 → OS 네이티브 카메라 → 미리보기 → 업로드)" |
| B2 | "카메라 전/후면 전환 (standby에서만)" 항목 **삭제** (코드 0건) |
| B3 | 가이드 페이지 3종 (`/guide`, `/guide/guest`, `/guide/host`) 등재 |
| B4 | 렌더링 polling cron + 렌더 지연·환불 cron 등재 |
| B5 | render/status + render/complete 등재 |

### 작업 4. CLAUDE.md Kickoff 룰 외화

**4-A**: 핵심 5개 셋트 3번 항목 — "최신 파일" → "최신 2개 파일 — 직전
+ 직직전 세션 인계 (단일 핸드오프 misremember·누락 사고 차단용 이중
채널)"

**4-B**: Kickoff 룰 6번 신설:

> 핸드오프 2개 교차 검증 의무: 새 세션 시작 시 첨부된 직전 + 직직전
> 핸드오프 2개를 첫 답변 전에 비교한다. 출력 의무: (a) 두 핸드오프
> 사이 불일치/시점별 모순/미처리 영역 검출 결과, (b) 본 세션 진입점
> 후보 제시. 한 핸드오프만 보고 진입점 단정 금지. 본 룰은 2026-05-18
> 본 세션에서 자동 첨부 자료 stale + 직전 핸드오프 단일 의존이 사양
> D 정의 충돌 사고를 일으킨 직후 외화.

**4-C**: 핸드오프 파일 명명 규칙 신설:

> 형식: `YYYY-MM-DD-v{N}-{한줄요약}.md`. 예:
> `2026-05-18-v1-docs-sync.md`. 같은 날 여러 세션 시 v1·v2·v3 순차.
> 한줄요약은 영문 kebab-case로 핵심 작업 1개 압축.

### 작업 5. CHANGELOG.md

`## 2026-05-18 v1` 헤더 신설 + 본 세션 작업 2줄.

---

## Track D — 정정 (794664b)

검증 표 단계에서 본인이 의외값 2건 포착:

| # | 의외값 | 정정 |
|---|---|---|
| 1 | 4-A 문안 일부 누락 — 괄호 안 "사고 차단용 이중 채널" 문구 빠짐 | CLAUDE.md 1줄 교체 |
| 2 | 5번 v1 표기 누락 — `## 2026-05-18`로만 적용됨 | CHANGELOG.md 1줄 교체 |

본 세션 본인이 명시적으로 추천한 문안이 CC 적용 단계에서 일부 누락된
패턴. 검증 표 룰이 또 효과 입증.

---

## Track E — 의외값 3 본문 확인 (정찰만, 커밋 없음)

검증 표 단계에서 본인이 의외값 1건 추가 포착:

- B5 render API 등재 문구 중 `/api/render/complete`의 "잠김 (401),
  향후 Shotstack webhook 수신용으로 보존" 표현. 본인 시야 코드 본문
  없어 검증 불가 영역.

운영자 결정으로 CC 본문 확인 정찰 진행. 결과:
- `/api/render/status`: GET + renderId param + Shotstack 상태 조회.
  등재 문구 정합.
- `/api/render/complete`: POST + 무조건 401 USE_CRON. 코드 주석에
  "Shotstack webhook 수신용으로 보존" 명시. 등재 문구 정합.

추가 정정 불필요. 종결.

### 부수 발견 (잠재 잔여)

CC가 정찰 중 짚은 부수 영역:
- `/api/render/complete`의 401 응답이 HTTP 의미 위반 (인증과 무관하나
  401 반환).
- 본 세션 작업 범위 밖. 해당 라우트는 webhook 수신 시점까지 보존용,
  실제 호출되는 외부 클라이언트 없음.
- 향후 Shotstack webhook 연결 작업 시 HTTP 상태 재설계 단계에서
  자연스럽게 정정될 영역.
- YAGNI — 지금 처리 필요 없음. 운영자 결정 영역.

---

## 본 세션 사고 패턴

### 사고 #1 — 자동 첨부 자료 신뢰 단정

본인이 본 세션 진입 시점부터 자동 첨부된 PROJECT.md / DECISIONS.md /
known-issues.md를 현재 저장소 상태로 단정. 실제로는 **3개 모두 stale**.
이 단정이 본 세션 가장 큰 어긋남의 근원.

발견 트리거: CC 정찰 보고에서 본인 시야 PROJECT.md 항목 수(22)와 CC
실측(28)이 다르게 나오면서 즉시 인지.

학습: 자동 첨부 자료는 "현재 상태"가 아니라 "어느 시점 스냅샷"임을
가정해야 함. 정찰 단계에서 매번 CC 실측과 대조 필요.

### 사고 #2 — 사양 D 정의 단편 계승

본인이 5/17 핸드오프의 "사양 D = MediaRecorder 코드 정리" 정의를
그대로 계승. 5/16 핸드오프 2개 본 적 없는 상태에서 단정. CLAUDE.md 룰
"단편 단서로 전체 단정 금지"(f68b4c0) 또 적용 실패.

발견 트리거: 운영자 "PDF 작업도 이미 끝난 것 같은데?" 한 마디.

학습: 룰 등재 자체가 적용 보장 아님. 본 세션 사양 D 진입 시 5/16
핸드오프 2개를 자동 첨부 영역 외로 인지하고 운영자에 요청했어야 함.
**본 세션 등재한 Kickoff 룰 6번(핸드오프 2개 교차 검증)이 이 사고
재발 차단 룰**.

### 사고 #3 — 첨부 PDF 본문 확인 누락

본 세션 첫 답변에서 `congreguestguide.pdf` 본문이 텍스트로 안 보이는
것을 "빈 파일/첨부 오류"로 단정. 사용자 한 마디로 잡힘.

학습: PDF는 본 인터페이스에서 텍스트로 안 보이는 게 정상. file-reading
스킬 영역. 직접 검증 안 한 채 단정한 패턴.

### 공통 패턴

세 사고 모두 **본 시야 없는 영역에서 단편 단서로 단정**. 직접 검증
누락. 본 세션 1답변마다 반복된 패턴.

### CC 작업 결과 의외값 2건 포착 — 검증 표 룰 효과

검증 표가 본 세션에서도 효과 입증:
- 4-A 문안 누락 — CC 보고와 본인 추천 문안 한 줄 한 줄 대조에서 포착
- CHANGELOG v1 누락 — 같은 단계에서 포착
- render/complete 본문 본인 시야 없음 — 본인이 명시적으로 검증 불가
  영역 표기 후 사용자 결정 위임

검증 표 룰 유지 가치 입증.

---

## 운영자 책임 분담

본 세션 어긋남의 근원이 자동 첨부 자료 stale인데, 이는 운영자 책임
영역 아님. 시스템 영역(채팅 인터페이스의 자동 첨부 채널이 어느 시점
스냅샷인지 보장 안 됨)에서 비롯됨. 운영자가 "컨텍스트 완전 틀어졌다"
조기 트리거 줘서 본 세션 갈래 잡힘. 운영자 영역 정상.

본 세션 본인 사고 #1·#2·#3은 모두 본인 단정 영역. 자책 영역 아닌
패턴 외화 영역.

---

## 보정 큐 (다음 세션 작업 후보)

| # | 항목 | 출처 | 상태 |
|---|---|---|---|
| ~~D1·D2~~ | ~~known-issues 정리 (getUserMedia·1a784d0)~~ | 본 세션 | **본 세션 처리 (b24adc4)** |
| ~~C1·C2~~ | ~~DECISIONS 인덱스 정정~~ | 본 세션 | **본 세션 처리 (b24adc4)** |
| ~~B1~B5~~ | ~~PROJECT.md 갱신~~ | 본 세션 | **본 세션 처리 (b24adc4)** |
| ~~Kickoff 룰 6번~~ | ~~핸드오프 2개 교차 검증~~ | 본 세션 | **본 세션 처리 (b24adc4 + 794664b)** |
| **E1** | **합본 PDF (15p) — 게스트+호스트 합본** | 5/16 이전~ | **잔여 (저장소 외 PDF 영역, 운영자 PC 작업)** |
| **E2** | **globals.css 토큰 동기화 잔여 여부 확인** | 5/14~5/17 | **잔여 (5/16 91ac5e9에 PROJECT.md 표 처리됐으나 globals.css 본문 잔여 영역 미확인)** |
| **E3** | **public/ Next.js 기본 SVG 5개 실제 사용 여부 확인** | 본 세션 | **잔여 (file.svg / globe.svg / next.svg / vercel.svg / window.svg)** |
| **F1** | **/api/render/complete 401 응답 HTTP 의미 위반 (잠재)** | 본 세션 의외값 3 부수 | **잠재 잔여 — webhook 연결 시점까지 보류, YAGNI** |
| **F2** | **자동 첨부 채널 stale 사고 재발 방지 룰** | 본 세션 메타 | **잠재 잔여 — Kickoff 룰 6번 작동 관찰 후 검토. 시스템 영역이라 룰화로 차단 가능한지 불명** |

---

## 거론 보류 큐 (이전 세션 계승)

- 재렌더 UX 갭 (done 상태 버튼 미노출)
- 완성본 단일 필드 덮어쓰기 → 서브컬렉션 전환 (D2)
- 클립 메타데이터 저장 실패 (S3 고아 파일)
- 호스트 클립 제거 시 영상 미리보기 흐름 검증
- 영상 호스팅 CDN 이전 (R2 vs Shotstack)
- 미성년자 영상 법적 리스크
- 네이버 메일 도달성
- BGM 다양성 격상
- 야외 환경 음량 검증
- iOS 필드테스트 결과 회고

### 예정된 작업 트리거

- AWS 무료 트라이얼 만료 30일 전 (~2026-09-28)

---

## 다음 세션 진입점 (본인 추천 우선순위)

| 순위 | 후보 | 비고 |
|---|---|---|
| 1 | **iOS 필드테스트 결과 회고** | 운영자 트리거 대기. 직전 핸드오프부터 계승 |
| 2 | **합본 PDF (15p) 생성** | E1. 저장소 외 영역이라 운영자 PC 작업, 본 세션 본인이 작성 가능한 영역 |
| 3 | **잔여 보정 #1·#2·#3 (globals.css 잔여 / SVG 정리)** | E2·E3. 가벼운 정리 작업 |
| 4 | **Kickoff 룰 6번 작동 관찰** | 다음 세션 본인이 첫 답변에 핸드오프 2개 교차 검증 의무 수행. 룰 효과 확인 |

---

## 다음 세션 시작 시 운영자 작업 — 5개 셋트

진입점: **운영자 트리거에 따라 결정** (위 후보 1~3 중 또는 다른 영역)

- CLAUDE.md (자동 첨부) — **본 세션 등재 룰 6번 + 핸드오프 명명 규칙
  포함**
- AGENTS.md (자동 첨부)
- 직전 핸드오프 (본 파일, `2026-05-18-v1-docs-sync.md`) ← 운영자 직접
  첨부
- 직직전 핸드오프 (`2026-05-17-spec-c-validation.md`) ← **운영자 직접
  첨부 (본 세션 신설 룰 6번 의무 영역)**
- DECISIONS.md 인덱스 (자동 첨부)
- known-issues.md (자동 첨부)

### 자동 첨부 stale 가능성 경고

본 세션 발견대로 자동 첨부된 PROJECT.md / DECISIONS.md / known-issues.md
는 어느 시점 스냅샷일 수 있음. **현재 저장소 상태로 단정 금지**. 본
핸드오프 또는 CC 실측 결과를 우선 참조.

### 첫 메시지 한 줄 (Kickoff 룰 6번 의무)

> "본 핸드오프 2개 외엔 모르는 것으로 간주. 진단·결정 시 모르는
> 영역이면 명시. CC 보고 받으면 검증 표 먼저. **첫 답변에 두 핸드오프
> 교차 검증 결과(불일치/시점별 모순/미처리 영역) 출력 의무**
> (CLAUDE.md Kickoff 룰 6번). 이번 세션 진입점은 [영역명]."

### 다음 세션 채팅 클로드 본인용 노트

- **Kickoff 룰 6번 본 세션 등재 (b24adc4 + 794664b)**. 첫 답변에 두
  핸드오프 교차 검증 의무. 한 핸드오프만 보고 진입점 단정 금지. 본
  룰의 등재 트리거가 본 세션 사양 D 정의 충돌 사고이므로 같은 패턴
  재발 차단이 핵심.
- **자동 첨부 자료 stale 가능성 인지**. 첨부된 PROJECT.md / DECISIONS.md
  / known-issues.md를 "현재 상태"로 단정하지 말 것. CC 정찰로 매번 실측
  대조 필요.
- **검증 표 룰 본 세션도 효과 입증 (의외값 2건 포착)**. 룰 유지.
- **단편 단서로 전체 단정 금지 룰(f68b4c0) 본 세션 또 적용 실패**.
  사양 D 정의 단편 계승 + 첨부 PDF 본문 미확인. 본인 의식 모드 유지
  필요.
- **컨테이너 경로 표기 사고 재발 방지 (계승)**: 운영자 안내에
  `/home/claude/...` 내부 경로 노출 금지.
- **PROJECT.md "완료된 기능" 섹션이 현재 진실에 가까운 상태**(b24adc4
  처리됨). 다음 세션 정찰 시 첫 참조 영역.
