## 세션 한 줄 요약

PR 1 (refactor uploader: 닉네임 → 이름+전번) 정찰·실행·배포 완료. 사양 충돌(닉네임 재사용 정책) 사전 발견·정합화. ROADMAP·known-issues 직전 누락분 같은 커밋에 합류.

## 본 세션 커밋

- `49c08c2` refactor(uploader): 닉네임 → 이름+전번 사양 전환 (PR 1) (+151/-57, 11 files)

## PR 1 실측 변경 범위

### 코드 (7 files)

- `src/lib/events.ts` — Clip.uploaderPhone?: string 추가
- `src/app/api/events/route.ts` — organizerPhone /^010\d{8}$/ 검증, 위반 시 400 INVALID_PHONE
- `src/app/api/clips/route.ts` — uploaderPhone 받기·정제·검증, 3조건 복합 중복 체크, DUPLICATE_NICKNAME → DUPLICATE_UPLOADER, name length 1-10 → 1-20
- `src/app/api/clips/check/route.ts` — phone 파라미터 추가, 정제·검증, 3조건 쿼리
- `src/app/api/host/clips/route.ts` — 응답에 uploaderPhone 추가
- `src/app/upload/[eventId]/page.tsx` — Stage rename, 인풋 2개, sessionStorage JSON, reRecord uploader 직행, 에러 코드·문구 변경
- `src/app/dashboard/events/[eventId]/page.tsx` — ApiClip.uploaderPhone, 클립 행 전번 노출(마스킹 없음), fallback "(닉네임 없음)" → "(이름 없음)"

### 문서 (4 files)

- `docs/DECISIONS.md` — 닉네임 재사용 정책 폐기 + 식별 키 변경 결정 등록
- `docs/CHANGELOG.md` — 항목 추가
- `docs/ROADMAP.md` — PR 1 완료 표기 + PR 2 진입점 추가
- `docs/known-issues.md` — Firestore composite index 이슈 추가, "닉네임 회상 사고" RESOLVED 처리

## 사양 확정본 (구현됨)

### 데이터 모델

- Clip: `uploaderName: string` (1~20자, required), `uploaderPhone: string` (정제 후 010+8자리, required)
- events: `organizerPhone` /^010\d{8}$/ 검증 (숫자만, 하이픈 시 400)
- 식별 키: `eventId + uploaderPhone + uploaderName` 복합

### 중복 정책

- 다른 전번 → 통과
- 같은 전번 + 다른 이름 → 통과 (친구 폰 돌려쓰기)
- 같은 전번 + 같은 이름 → 차단 ("이전 영상과 다른 이름을 입력해주세요")
- 에러 코드: `DUPLICATE_UPLOADER`

### 입력 UX 비대칭 (의도됨)

- uploaderPhone: 사용자 자유 입력 (하이픈 등 허용) → 클라 정제 후 검증. 학생·참가자 마찰 최소화.
- organizerPhone: 숫자만 (하이픈 시 400). 운영자 1회 입력 + placeholder 안내로 학습 비용 수용.

### sessionStorage

- 키: `congre-uploader-${eventId}` (구 `congre-nick-${eventId}`)
- 값: JSON `{name, phone}` (구 plain string)
- reRecord: uploader stage 직행, useEffect가 sessionStorage에서 자동 채움

## 운영자 처리 완료 (push 직전)

1. ✅ phone 없는 clips 전부 삭제 (Firebase 콘솔)
2. ✅ events organizerPhone 하이픈 점검 완료

## 다음 세션 진입점

**A. PR 2 — feat(notify): 참가자 결과 SMS 트리거 연결**

- 트리거 위치: `src/app/api/cron/check-rendering/route.ts` Shotstack done 직후
- 흐름: clips Firestore 조회 (excludedAt: null) → distinct uploaderPhone → 각 phone에 notifyParticipantResult
- 본문: `[Congre] '{eventTitle}' 결과 영상이 준비됐어요: {videoUrl}` (이름 없음)
- 멱등성: `events.notifications.participantNotifiedAt` (기존 notifications.* nested 패턴, 2026-05-04 결정 일관성)
- 함수 위치 정찰됨: notifyParticipantResult — 정찰 결과 위치 핸드오프에 별도 기록 없음, PR 2 정찰 시 재확인

**B. 약관·처리방침 콘텐츠 작성** (운영자 영역, 법무)

- 현재 `/privacy`, `/terms` 페이지는 골격만 있고 임시 배너 표시
- PR 2 배포 전 본문 채움 필수 (전번 수집 + SMS 발송 = 개인정보 활용 = 처리방침 본문 필수)

A·B 병렬 가능. A 코드 작업과 B 콘텐츠 작업은 독립.

**C. PR 1 배포 후 첫 업로드 테스트 (필수, A 시작 전)**

- Firestore composite index 자동 생성 확인 — 첫 업로드 시 Vercel 함수 로그에 인덱스 생성 링크 나오면 운영자 클릭, 1~2분 후 정상화
- 메뉴 트리: Vercel 대시보드 → 프로젝트 congre → Deployments → 최신 배포 → Functions 탭 → /api/clips 또는 /api/clips/check 로그
- 이슈 발생 시 `docs/known-issues.md`의 "Firestore composite index" 항목 절차 따라가면 됨

## 운영자 잔여 작업

1. PR 2 시작 전 PR 1 배포 검증 (위 진입점 C)
2. 약관·처리방침 본문 콘텐츠 작성 (PR 2 push 전 필수)
3. PR 2 작업 시 SOLAPI 발신번호 등록·잔여 한도 점검 (현재 상태 모름, 운영자 영역)

## 자체 학습

- **사양 충돌 사전 점검 패턴 효과 확인**: 정찰 진입 전 DECISIONS.md 대조에서 "닉네임 재사용 정책"이 폐기 표기 안 된 채 남아 있는 걸 발견. 핸드오프엔 함의로만 들어 있던 걸 명시 폐기로 격상. 다음 PR도 정찰 전 DECISIONS 대조 단계 유지.
- **5개 셋트 단순화 + 누락분 자연 합류**: 직전 세션 운영자 제안대로 5개로 시작 → ROADMAP·known-issues는 PR 1 커밋에 자연 합류. 별도 "갱신 작업" 단계 안 만든 게 정합. 다음 세션도 같은 리듬.
- **CC 자율 판단의 적절성**: known-issues "닉네임 회상 사고" RESOLVED 처리를 사양 명시 없이 CC가 자율 판단. 사양에 직접 안 적힌 자명한 결과 갱신은 CC 자율로 둬도 됨.
- **atomic 커밋 + 운영자 작업 분리**: 코드 작업 ↔ 데이터 정리 작업 경계가 자연스럽게 push 직전에 분리됨. 다음 PR도 데이터 변경 동반 시 같은 패턴 유지.
- **세션 종료 리듬**: PR 1 단독 한 세션. 직전 세션 자체 학습("PR 단위로 세션 끊기")이 이번에도 적용됨.

## 다음 세션 시작 시 운영자 작업 — 컨텍스트 셋트

진입점 A(PR 2)일 경우 5개 셋트:

- CLAUDE.md
- AGENTS.md
- 이 핸드오프 (2026-05-10-pr1.md)
- DECISIONS.md
- PROJECT.md (디자인 토큰 참조용)

ROADMAP, known-issues, 직전 핸드오프(2026-05-10.md)는 일단 빼고 시작. 정찰 중 필요해지면 추가.

첫 메시지 한 줄:
"진입점: PR 2 (feat notify SMS). 컨텍스트 5개 셋트만. 외엔 모르는 것으로 간주, 진단·결정 시 모르는 영역이면 명시. 사전 점검: DECISIONS.md에 PR 2와 충돌하는 결정 있는지 대조."

진입점 B(약관 콘텐츠)는 코드 작업 아니므로 채팅 클로드 단독, 컨텍스트 셋트 별도 (DECISIONS의 약관 관련 결정 + 처리방침 표준 양식).
