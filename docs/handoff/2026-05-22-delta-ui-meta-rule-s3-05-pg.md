# Handoff — 2026-05-22: δ UI 분기 + CC 메타 룰 격상 + S3-05 PG 정찰 (토스페이먼츠 선정)

## 본 세션 완료 항목

### 1. δ — 이벤트 생성 폼 플랜별 영상 길이 분기 UI (커밋 ebe4f27 + 853e4fd)

**ebe4f27 — feat(ui): 이벤트 생성 폼 플랜별 영상 길이 분기 + 자물쇠 잠금 표시**

- `src/app/dashboard/create/page.tsx` 4영역 변경:
  - **영역 1**: 영상 길이 라디오에 `isLocked = sec > getPlanMaxClipSeconds(form.plan)` 분기 추가. disabled + opacity-40 + cursor-not-allowed + lucide-react `Lock` 아이콘 (size=12, absolute top-1.5 right-1.5, text-muted)
  - **영역 2**: plan 변경 onChange 핸들러에서 현재 maxClipSeconds가 새 플랜 한도 초과 시 자동 재설정. 마운트 시점 발동 없음 (onChange 내부에서만)
  - **영역 3**: handleSubmit non-ok 분기에서 res.json() 본문 읽고 `code === "INVALID_CLIP_SECONDS"`이면 alert("무료 플랜은 최대 10초까지 가능해요...") + return. 그 외는 기존 throw 유지
  - **영역 4**: planOptions desc에 영상 길이 정보 추가 ("최대 10클립 · 10초" 형식 4개 모두)
- `docs/CHANGELOG.md` 2026-05-22 영역 신설 + 한 줄 추가
- TypeScript 빌드 0 errors

**853e4fd — fix(ui): 이벤트 생성 폼 초기 maxClipSeconds 10으로 변경 (free 기본값 정합)**

- form useState 초기값 `maxClipSeconds: 15 → 10` 1줄 변경
- 이유: 초기 free + 15초 상태에서 15초 자물쇠 잠긴 상태로 떠 어색. 본인 사양 자체 모순 (영역 사고 1 참조)
- `docs/CHANGELOG.md` 한 줄 추가

### 2. 격상 처리 — CC 보고 메타 코멘트 금지 룰 (커밋 80b033d)

**80b033d — docs(claude): CC 보고 메타 코멘트 금지 룰 추가**

- 직전 핸드오프(2026-05-21) "본 세션 사고 학습 — 사고 2 (잠재)" 격상 트리거 발동
- 본 세션 δ 후속 보고에서 CC가 또 `※ recap` 추가하여 재발 확인
- `CLAUDE.md` "협업 스타일" 영역 "응답 톤" 직후에 1항목 추가:
  - **보고 형식 — 메타 코멘트 금지**: 작업 완료 보고 마지막에 "※ recap", "다음 단계 제안", "다음은 X 또는 Y" 등 메타 코멘트 추가 금지. 보고는 작업 결과(변경 파일·동작 흐름·빌드 결과)로만 끝낸다. 다음 단계는 사용자(운영자 / 채팅 클로드)가 결정한다.
- `docs/CHANGELOG.md` 한 줄 추가
- **즉시 위반 발견**: CC가 본 룰 추가 작업의 보고 끝에 또 `※ recap` 추가. 같은 세션 컨텍스트 영향. **다음 세션 첫 보고에서 룰 효과 검증 영역**

### 3. S3-05 정찰 진입 — PG 토스페이먼츠 선정 (코드 변경 없음)

- 결제 흐름 결정: **β** (마감 후 렌더 트리거 시 결제) — B5 결정과 정합
- 결제 형태: **단건 결제** 확정 (B5 "첫 렌더 유료 + 재렌더 매번 유료" 정합)
- 사업자 등록 상태: 아직 미등록 → 등록 절차 본인 정찰 포함
- PG 선택: **토스페이먼츠** (운영자 결정)
  - 기본 카드 수수료 3.4%, 영세 가맹점(연매출 3억 이하) 자동 우대 0.4%
  - 가입비 33만원
  - 개발 문서 한국 PG 중 최상위, Next.js / TypeScript 친화적
  - 가입: 사업자등록증 + 통장사본 + 신분증 온라인 신청

## 본 세션 사고 학습 (본인 영역)

### 사고 1: 본인 사양 자체 모순 — form 초기값 영역

- δ 프롬프트 영역 [3]에 "초기값 15는 그대로 유지 (자동 조정으로 즉시 10이 됨)" + "단, 자동 조정 로직이 초기 마운트 시점에 발동되면 안 됨" 모순 적음
- CC는 후자(마운트 발동 금지) 합법 따름 → 초기 화면이 free + 15초 (15초 자물쇠 잠긴 상태) 어색
- δ 후속(853e4fd) 1줄로 정정
- **학습**: 사양 작성 시 초기 상태 영역 한 번 더 점검. "마운트 시 사이드 이펙트" 영역을 보수적으로 막을 게 아니라 form 초기값 자체를 바꾸는 단순 해결 영역 있었음. 본인이 "두 가지 해결 영역 중 어느 게 단순한가" 점검 안 하고 한쪽만 사양에 적음

### 사고 2: CC 메타 코멘트 패턴 — 격상 후 즉시 위반

- 직전 핸드오프 격상 트리거 발동 → 본 세션에서 격상 액션 A (CLAUDE.md 룰 추가) 처리
- 그러나 CC가 본 룰 추가 작업의 보고 끝에 또 `※ recap` 추가
- **현재 처리**: 본 룰은 CLAUDE.md에 들어갔으므로 다음 세션부터 적용됨. 본 세션 컨텍스트는 룰 추가 이전부터 시작됐으므로 즉시 적용 어려움이 자연스러움
- **격상 트리거**: 다음 세션 첫 CC 보고에서 `※ recap` / "다음 단계 제안" 등 메타 코멘트 미포함 여부 확인. 재발 시 known-issues 등록 + 추가 격상 액션 검토

### 사고 3: 본인 첫 메시지에서 첨부 후보 헷갈림

- δ 영역 진입 시 본인이 첨부 후보 명단에 `upload/[eventId]/page.tsx` 포함시킴
- 실제로는 `INVALID_CLIP_SECONDS`가 `api/events POST`에서 발생 → 이벤트 생성 폼 영역. upload 페이지 무관
- 정찰 단계에서 본인이 정정 (`dashboard/create/page.tsx` 하나만 필요)
- **학습**: 첨부 후보 명단 작성 시 응답 분기 영역의 발생 지점을 한 번 더 추적. 본인이 "응답 분기 = upload 페이지"로 자동 매핑하지 말 것

## 다음 세션 우선 영역

### 1. S3-05 본격 진행 — 결제 흐름 사양 + 토스 SDK 통합

본인이 다음 세션에 진입할 정찰 영역:

| 영역 | 본인이 정찰할 내용 |
|---|---|
| **결제 흐름 사양 확정** | 마감 → 플랜 선택 화면 → 토스 결제창 → 콜백 → 렌더 트리거 흐름 설계. 기존 close 흐름과 어떻게 합쳐지는지 |
| **토스페이먼츠 SDK 통합** | `@tosspayments/payment-sdk` 또는 `@tosspayments/payment-widget-sdk` 영역. Next.js App Router 친화도 |
| **결제 완료 후 webhook 영역** | 토스가 결제 성공 알림을 어떻게 보내는지. 본인 미정찰 |
| **결제 실패 / 환불 영역** | 환불 정책 사양. B5 결정에 환불 영역 없음 → 약관 v0.2와 함께 결정 |
| **약관 v0.2 트리거** | 결제 약관 추가, 환불 정책, 미성년자 동의 영역 등. legal CHANGELOG 갱신 |

### 2. 운영자 외부 작업 — 사업자 등록 + 통신판매업 신고

본 세션 운영자 미답 영역 2건:
- **사업자 등록 진행 시기** — 본인 추천: 이번 주 안 시작 (영업 한 달 안 영역에 빠듯)
- **사업장 주소** — 자택 / 비상주 사무실 / 안내 필요 중 미답. 다음 세션에 정찰 영역으로 가져갈지 결정

#### 전체 절차 (한 달 안 영업 시점 기준)

```
[1] 사업자 등록 (홈택스 온라인)        3영업일
        ↓
[2] 토스페이먼츠 가입 신청 + 카드사 심사  1~2주
        ↓
[3] 구매안전서비스 이용확인증 발급 (토스 관리자 페이지)
        ↓
[4] 통신판매업 신고 (정부24)          3~7영업일
        ↓
[5] 토스에 통신판매번호 입력 + 실서비스 개통
```

총 약 2~3주 영역. 운영자 외부 작업 + 본인 + CC 코드 작업 동시 진행 영역.

#### 운영자 단계별 안내

**[1] 사업자 등록 — 홈택스 온라인**

```
홈택스 (hometax.go.kr) 접속
  → 로그인 (공동인증서 / 간편인증)
  → 상단 메뉴: 신청·제출
  → 사업자등록 신청·정정 등
  → 사업자등록 신청(개인)
```

준비물:
- 공동인증서 또는 간편인증
- 사업장 주소 (자택 또는 비상주 사무실)
- 업종코드 (본인 영역에서 정찰 필요. "정보통신서비스 / 영상편집 서비스" 영역으로 보임. 다음 세션에 본인이 정확한 코드 정찰)
- 임대차계약서 사본 (사업장 임차 시)

평일 09:00~18:00만 접수. 3영업일 이내 발급.

**[2] 토스페이먼츠 가입 신청**

```
토스페이먼츠 (tosspayments.com) 접속
  → 가맹점 신청
  → 개인사업자 선택
  → 사업자등록증 + 통장사본 + 신분증 업로드
```

가입비 33만원. 카드사 심사 1~2주.

**[3] 구매안전서비스 이용확인증 발급**

토스페이먼츠 관리자 페이지에서 직접 발급 가능.

**[4] 통신판매업 신고 — 정부24**

```
정부24 (gov.kr) 접속
  → 로그인 (간편인증 / 공동인증서)
  → 검색: "통신판매업신고"
  → 통신판매업신고 - 시.군.구 → 발급하기
```

준비물:
- 구매안전서비스 이용확인증 ([3]에서 받은 것)
- 사업자등록증

신고 후 등록면허세 납부 (위택스/이택스) → 정부24에서 신고증 직접 출력.

**[5] 토스에 통신판매번호 입력**

토스 관리자 페이지 → 가맹점 정보 → 통신판매번호 입력 → 실서비스 개통.

#### 본인이 잘 모르는 영역 (운영자 PG 가입 시 확인 영역)

- 영세 가맹점 우대 자동 적용 시기 (초기 매출 0원 가맹점 영역)
- 카드사 심사에서 Congre 영역(영상 자동 편집)이 어떻게 분류되는지
- 자택 주소 사용 시 비상주 사무실 필요 영역 (운영자 결정)

## B5 결정 후속 미해결 영역

### 응답 형식 혼재 (직전 격상 트리거 대기 유지)

- 기존: HTTP 4xx + `{ error: "..." }`
- 신규: HTTP 4xx + `{ code: "...", ...meta }`
- 본 세션도 그대로 유지. 본 세션 신규 추가 없음
- 격상 트리거: 다른 검증 신규 추가 시 또는 클라이언트 분기 통일 필요할 때

### 기존 events 문서 마이그레이션 (안 함, 유지)

- 본 세션 이전 생성된 events 문서에 무료 + 15·30초 저장된 데이터 있을 수 있음
- 격상 트리거: 기존 이벤트에서 동작 이상 보고 발생 시

### B5 가격 시작점 임의값 영역 (다음 세션부터 짚을 영역)

- 직전 세션 본인이 임의로 정한 가격 (소형 2만 / 중형 7만 / 대형 별도)
- 영업 개시 후 시장 데이터 들어오면 다시 짚어야 할 영역
- 격상 트리거: 첫 결제 받은 후 또는 운영자 가격 결정 요청 시

### 사업장 주소 영역 — 운영자 미답 (다음 세션 재청구)

- 자택 / 비상주 사무실 중 미답
- 본인 추천: 영업 한 달 안 시점이면 자택부터 시작 (속도). 매출 영역 커지면 비상주 사무실로 전환
- 다만 통신판매업 신고증에 주소가 공개됨 → 자택 주소 공개 영역 트레이드오프
- 다음 세션에 운영자 결정 영역 재청구

### 사업자 등록 진행 시기 — 운영자 미답

- 본 세션 단답 미답. 본인 추천: 이번 주 안 시작
- 다음 세션에 운영자 답 받고 본인 진행 영역 정리

## 현재 커밋 상태

본 세션 신규 = 3개 커밋:
- `80b033d` — docs(claude): CC 보고 메타 코멘트 금지 룰 추가
- `853e4fd` — fix(ui): 이벤트 생성 폼 초기 영상 길이 10초 (free 플랜 정합)
- `ebe4f27` — feat(ui): 이벤트 생성 폼 플랜별 영상 길이 분기 + 자물쇠 잠금 표시

origin/main 동기화 완료.

## 다음 세션 진입 시 5개 셋트 + 추가

### 핵심 5개 (Kickoff 매번 동일)
1. CLAUDE.md
2. AGENTS.md
3. **본 핸드오프** (`2026-05-22-delta-ui-meta-rule-s3-05-pg.md`)
4. docs/DECISIONS.md (인덱스)
5. docs/known-issues.md

### 추가 첨부 후보 (다음 세션 진입 영역에 따라)
- **S3-05 본격 진행 시**:
  - `src/lib/events.ts` — Event 타입 + 결제 관련 필드 추가 영역
  - `src/app/dashboard/events/[eventId]/page.tsx` — 마감 후 결제 UI 진입 지점
  - `src/lib/notifications/` — 결제 완료/실패 알림 영역 추가
  - `docs/legal/CHANGELOG.md` — 약관 v0.2 트리거 영역
- **사업자 등록 진행 영역 도움 필요 시**:
  - 업종코드 정찰 결과 (본인이 다음 세션에 web_search)

### 운영자 결정 영역 (다음 세션 진입 전)

1. **사업자 등록 시기** — 이번 주 / 다음 주 / 다음 세션에 다시 정찰
2. **사업장 주소** — 자택 / 비상주 사무실 / 안내 필요
3. **다음 세션 본인 작업 영역 진입 영역**:
   - 옵션 A: 결제 흐름 사양 정찰부터 (운영자 외부 작업과 병행)
   - 옵션 B: 운영자 외부 작업 진행 상태 받고 결정 (등록 완료 후 진행)

## 본인 메모 (다음 세션 본인용)

- **CC 메타 코멘트 룰 효과 검증**: 다음 세션 첫 CC 보고 받으면 `※ recap`, "다음 단계 제안" 등 미포함 여부 확인. 재발 시 known-issues 등록 + 추가 격상 액션 검토
- **사고 1 영역 학습 유지**: 사양 작성 시 초기 상태 영역 한 번 더 점검. 두 가지 해결 영역 중 단순한 것 선택
- **B5 가격 시작점 임의값 영역**: 시장 데이터 들어오는 시점에 다시 짚어줄 영역. 잊지 말 것
- **PG 토스 + 결제 흐름 β + 단건 결제 확정**: 본 영역 사양 확정됐으므로 다음 세션 진입 시 정찰 영역 명확
- **S3-05는 큰 영역**: 결제 흐름 사양 / SDK 통합 / webhook / 환불 / 약관 v0.2 — 한 세션에 다 안 함. 세부 분해 후 단계별 진행
- **영업 시점 한 달 안 + 절차 2~3주**: 운영자 외부 작업 시기 영역이 본 영역 일정 핵심. 본인이 코드 진행보다 운영자 외부 진행 영역에 더 짚어줄 영역
- **사업장 주소 영역 운영자 미답** + **사업자 등록 시기 영역 운영자 미답** — 다음 세션 첫 메시지에 다시 청구

## 본 세션 본인 평가 정리

본 세션 영역 다음 3개:
1. δ UI 분기 (γ 후속) — 완료
2. CC 메타 코멘트 격상 처리 — 완료 (룰 추가). 효과 검증 다음 세션
3. S3-05 정찰 진입 — PG 토스 선정 + 절차 정찰 완료. 본격 진행 다음 세션

본 세션 신규 3개 커밋. 본인 정찰 영역 (사업자 등록 / 통신판매업 신고 / PG 비교) 정리됨.

다음 세션 진입 시 운영자 외부 작업 영역 진행 상태와 본인 코드 영역 영역을 같이 가져갈 것.
