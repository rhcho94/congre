# 2026-07-03 핸드오프 — 완주율 개선 카피 오딧(A~F) + 화면 재설계·기능 백로그

## 한 줄 요약
가입~첫테스트 완주율 개선 작업. 운영자가 실제 첫-테스트 흐름을 화면 순서대로 훑으며 마찰을 뽑아냄. 나온 항목을 3개 바구니(1차 카피 / 2차 화면 재설계 / 3차 기능)로 분류. 1차 카피(A~F)는 scout 정찰로 파일·줄번호까지 실측 완료 → CC 실행(4커밋, push 전 멈춤)까지 완료.

## 1차 카피 실행 완료 — 미푸시 커밋 4개
- 0ad7b12 feat(signup) / 01fcd59 feat(dashboard) / 70ff236 feat(create) / 117b11d feat(event-dashboard)
- build 통과, lint 11/3 (baseline delta 0), 4개 파일만 변경, eye 검증 합격.
- **다음 세션 최우선: 이 커밋들 push → Vercel 자동배포(app.congre.kr) → 운영자 실화면 확인.**
- push 시 하위 미푸시 커밋(8cef24b 이메일 핸드오프)도 함께 올라감.

### push 후 실화면 확인 포인트 (사양 외 변경·주의)
- signup 제목 h1 마진이 mb-10→mb-2로 좁혀짐(axe 판단, 부제를 제목에 붙이려는 것). 부제가 제목에 너무 붙었는지 눈으로.
- dashboard 여정 스트립 숫자뱃지 색이 [var(--accent)] 임의값 유틸로 들어감(사양은 "기존 토큰"이었음). 주황 뱃지 정상 표시되는지 확인.
- phone helper "문자로 알려드려요"가 실제 SMS 동작과 맞는지(문서 근거=렌더완료 알림 이메일+SMS, 코드로 phone 용도 직접 재확인은 안 함).

## 이 세션 실측 확정 — 재정찰 불필요 (2026-07-03 scout, 줄번호까지)
> 코드 실측값. 커밋/드리프트로 줄번호 밀릴 수 있으니 착수 시 head 확인 권장.

### src/app/signup/page.tsx (219줄)
- 필드 4개 email/password/name/phone, 마크업 L93–152. eyebrow L80 "Host"(대문자H+소문자, "HOST" 아님). 제목 h1 L81. glass-panel L79 안, form L92 바깥. 제출버튼 L203–205 삼항 {loading?"가입 중...":"가입하기"}. 만19세 체크박스 L193–195 ageAgreed. canSubmit L40 = termsAgreed&&privacyAgreed&&ageAgreed&&!loading (필드값은 HTML required로만 제어, 결함 아님).
- ※이 세션에서 이미 수정됨(커밋 0ad7b12): Host 삭제 / 부제 / 버튼 "무료로 시작하기" / helper 3개 / 체크박스 맥락.

### src/app/dashboard/page.tsx
- 빈상태 분기 L169 (events.length===0), 전용 UI L170–175. 미인증 사용자에게도 동일 노출(헤더 "+새 이벤트"만 disable).
- ※이 세션에서 이미 수정됨(커밋 01fcd59): 긍정 제목 + 여정 4단계 + 보조문구.

### src/app/dashboard/create/page.tsx
- E(QR 완료)는 별도 라우트 아님 — 같은 페이지 state 뷰 전환 type View="form"|"created"(L83), setView("created")(L161), 분기 L240/L411, router.push 없음(URL 안 바뀜). 제출버튼 L405–407 {submitting?"생성 중...":"이벤트 만들기"}. E QR 문구 L423. QR 렌더 L429, 공유링크+복사 L436–445.
- "이벤트 만들기" 누르면 바로 QR 초대 가능 상태로 열림(생성만+나중에 여는 구조 아님) → 버튼 "이벤트 시작하기"가 동작에 부합.
- ※이 세션에서 이미 수정됨(커밋 70ff236): 버튼 "이벤트 시작하기" + QR 문구 통일.

### src/app/dashboard/events/[eventId]/page.tsx
- 참가자 초대 섹션 L1020 (event.status==="open"&&shareUrl). eyebrow "참가자 초대" L1022. F QR 문구 L1032. QR 렌더 L1025, 공유링크+복사 L1034–1043.
- **QR 박스 공용 컴포넌트 없음**(src/components 아래 Qr/QR/Invite/Share 없음). E·F 각자 qrcode.react QRCodeSVG 페이지에 인라인 → QR 문구 통일은 두 파일 각각 수정해야 함(이 세션 이미 처리).
- 선택옵션: 래퍼 L1051 panel, eyebrow 헤더 L1052, 기존 설명 L1053. 유도문구 자리=L1052 뒤 L1053 앞.
- 클립 목록: 개수 이미 셈 "업로드된 클립 (N개)"(화면 확인). 각 클립 = 번호#N+이름+전번+시각+눈/재생. ★목록·완성영상 블록 정확한 줄번호는 scout 미확인 → 2차 착수 시 정찰 필요.
- ※이 세션에서 이미 수정됨(커밋 117b11d): QR 문구 통일 + 선택옵션 유도문구.

### src/components/FlowStrip.tsx
- props 없음. 4단계 STEPS 배열 하드코딩 L5–10(name/camera/upload/link). Icon 함수 L16–64. 사용처 1곳: upload/[eventId]/page.tsx:7,:462 (!isReturning). 게스트 전용 하드코딩 → 호스트 여정과 개념 달라 재활용 안 함(빈상태는 인라인 별도 구현).

## 2차 백로그 — 이벤트 대시보드 화면 재설계 (CD 시안 먼저, 미착수)
카피 아님. 레이아웃·정보구조·시각위계 → CD 시안 → scout → CC. 회귀 위험 있어 시안 합의 후 구현.
대상 = dashboard/events/[eventId]/page.tsx 상태별 재구성:
- 수집중: "클립 N개 올라옴"을 최상단 크게(개수 이미 셈, 표시 위치 이동).
- 순서 재배치: [선택옵션 → 클립목록 → 참가자 초대 QR] (QR을 하단으로).
- 클립 목록 조밀 UI(수십 개 한눈에, 이름으로 고름). 현 카드형 세로로 길어 몇 개만 보임.
- 시각 위계: 섹션 제목 크게, 옵션 select·버튼 작게.
- 마감(편집완료) 상태: 완성 영상 맨 위(현 위치 미확인 → 정찰 필요).
- 선행: 클립목록·완성영상 줄번호 scout 정찰.

## 3차 백로그 — 새 이벤트 폼 스마트 기본값 (기능, 미착수)
카피 아님. DB 값 프리필 = 동작 변경. scout(create 데이터흐름) → CC.
- 이메일·전번: 로그인 호스트 users 문서에서 기본값(수정 가능).
- 날짜: 당일 기본 입력. 이벤트 이름: 이름 넣은 예시("○○의 결혼식에 축하영상 보내세요") — placeholder냐 실입력값이냐 결정 필요.
- 근거: 라나 리서치 스마트 기본값(활성화 에너지↓) + 아는 정보 다시 묻지 않기.

## 아직 안 본 화면 (다음에 마저 훑기)
운영자가 첫-테스트 흐름을 이벤트 대시보드(F)에서 멈춤. 미확인:
- 참가자 업로드 화면(upload/[eventId], FlowStrip 그 화면).
- 완성본 결과 화면(렌더 후 호스트/게스트).
- 1차 push·검증 후 이어서 훑기 → 카피/레이아웃/기능 3바구니 분류.

## 외부 리서치 요약 (라나 — 완주율/온보딩 표준)
- 활성화=핵심가치 첫 체험(우리=셀프테스트 완주). SaaS 평균 37.5%, 2/3 미도달. 가입수 아닌 활성화수 최적화.
- 필드 줄이기가 최강 레버(7→3필드 이탈 44.7%↓). 우리 4필드=이미 린 → 카피 보강이 맞는 수.
- helper text="왜 필요?"에 답, 필드 바로 아래. 구체성>간결성. 신뢰문구는 불안 지점에.
- 빈 상태=teachable moment. 긍정 제목+다음행동+이점+단일CTA. "빈 종이 공포" 낮추기. 여정지도=getting-started 체크리스트.
- 약속-경로 일치: 랜딩 "무료로 시작하기"→가입 버튼도 같은 문구.
- 한 번에 하나만(전술 쌓으면 상쇄)=1/2/3차 분리 근거.
- 주의: 자료 대부분 B2B·장기리텐션. 우리는 일회성 소비자(웨딩) → "한 세션 완주·단일CTA·활성화에너지↓"에 무게.

## 다음 세션 후보 (우선순위)
1. [미완 최우선] 1차 카피 커밋 4개 push → 실화면 확인 (위 확인 포인트 3개).
2. 첫-테스트 흐름 마저 훑기(참가자 업로드·완성본) → 3바구니 분류.
3. 2차 이벤트 대시보드 재설계 — CD 시안 먼저, 줄번호 scout 선행.
4. 3차 새 이벤트 폼 프리필 — create 데이터흐름 scout 선행.
5. (별도 트랙) 결제 스파이크(Toss v2 test키) / 이메일 도달성 A층 재확인 / 미인증 계정 전수 정찰 — 핸드오프 2026-07-03-email-verification-domain-fix.md 참조.

## 이 세션 학습 한 줄
- 운영자가 실제 흐름을 화면 순서로 훑으면 마찰이 쏟아지되 성격(카피/레이아웃/기능)이 뒤섞인다. 나오는 즉시 3바구니 분류 + "카피만 먼저 실행"으로 끊어야 1차가 안 무거워짐 — 안 그러면 재설계·기능이 카피 실행을 계속 뒤로 민다.
