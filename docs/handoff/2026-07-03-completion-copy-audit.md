# 2026-07-03 핸드오프 — 완주율 카피(A~F) + 대시보드 빈상태 시각 완성 + 백로그

## 한 줄 요약
가입~첫테스트 완주율 개선. 운영자가 실제 첫-테스트 흐름을 화면 순서로 훑으며 마찰을 뽑아냄 → 3바구니(1차 카피 / 2차 화면 재설계 / 3차 기능) 분류. **1차 카피(A~F) + 대시보드 빈상태 시각 완성(여정 스트립+화살표+반투명 결혼식 사진 배경)까지 실행·push 완료.** 2차·3차는 백로그. 첫-테스트 흐름 후반부(참가자 업로드·완성본) 미확인.

---

## 이 세션 push 완료 — origin/main 반영된 커밋 (최신순)
- 3532382 fix(dashboard): 빈상태 사진 배경 blur 제거 — 인물 형체 노출(오버레이 유지)
- 6166291 feat(dashboard): 빈상태 시각 완성 — 반투명 결혼식 사진 배경 + 오버레이 + 라벨 줄바뀜 수정
- 68d59c2 fix(dashboard): 빈상태 여정 스트립 시각 보강 — 세로 정렬 + 단계 사이 화살표(인라인)
- 8867df1 docs: 완주율 개선 카피 오딧 세션 핸드오프
- 117b11d feat(event-dashboard): 카피 — QR 문구 통일 + 선택옵션 대기중 유도 문구
- 70ff236 feat(create): 이벤트 생성 카피 — 버튼 "이벤트 시작하기" + QR 문구 통일
- 01fcd59 feat(dashboard): 빈상태 셀프 테스트 여정 안내 확장 — 긍정 제목 + 4단계 스트립 + 보조문구
- 0ad7b12 feat(signup): 가입 폼 완주율 카피 — helper 3개 + 만19세 맥락 + 제목정리 + 버튼 "무료로 시작하기"
- (하위) 8cef24b 이메일 인증 도메인 사고 핸드오프
- **전부 origin/main push 완료, ahead 0.** Vercel 자동배포 → app.congre.kr 라이브.

## 대시보드 빈상태 최종 상태 (완성)
파일: src/app/dashboard/page.tsx, events.length===0 분기.
- 제목 "5분이면 완성본까지 직접 볼 수 있어요"
- 여정 4단계 스트립: 세로 정렬(숫자 뱃지 위·라벨 아래) + 단계 사이 인라인 SVG 화살표(color var(--accent), opacity 0.55). 라벨 break-keep(어절 중간 절단 방지), 고정폭 w-16 제거.
- 라벨: 이벤트 만들기 / QR을 내 폰으로 스캔 / 하객인 척 짧게 촬영 / 완성본 영상 확인
- 보조문구 "하객이 없어도 혼자 한 바퀴 돌려볼 수 있어요", CTA "첫 이벤트 만들기 →"(Link → /dashboard/create)
- **배경**: 반투명 결혼식 사진(public/images/empty-state-wedding.png, AI 생성, 1.6MB). 3레이어 구조 = 배경사진(cover/center, blur 없음) → 흰색 오버레이 rgba(255,255,255,0.80) → 콘텐츠(z-10). blur는 얼굴 뭉개져 제거함(운영자 결정), 오버레이 80%는 운영자가 "이대로 좋다" 확정.

## CTA "첫 이벤트 만들기" 먹통 건 — 해결(버그 아님)
- 증상: 미인증 계정에서 "첫 이벤트 만들기 →" 눌러도 반응 없음 + "+ 새 이벤트"는 disabled.
- **원인 = 이메일 미인증 상태**. 운영자가 이메일 인증하니 정상 작동. 코드 변경 없음, 커밋 없음.
- ⚠️ 백로그(YAGNI, 미착수): 미인증 시 "+ 새 이벤트"는 disabled 표시가 있는데 "첫 이벤트 만들기 →" CTA는 아무 표시 없이 안 눌림 → UX 비일관. 미인증 유저가 "고장났나?" 오해 소지. 나중에 CTA도 disabled 표시/인증 유도 문구 붙이는 것 후보.

---

## 1차 카피 실행 완료 — 파일별 최종 문안 (전부 push됨)
**signup/page.tsx** (0ad7b12): Host eyebrow 삭제 / 부제 "가입하면 바로 무료로 테스트해 볼 수 있어요" / 버튼 "무료로 시작하기" / helper(email "완성본 링크와 알림을 이 주소로 보내드려요" · name "완성본과 하객 초대 화면에 표시돼요" · phone "완성본이 준비되면 문자로 알려드려요") / 체크박스 아래 "서비스는 성인(만 19세 이상) 대상이에요"
**dashboard/page.tsx** (01fcd59 + 위 시각 커밋들): 빈상태 여정 (위 "최종 상태" 참조)
**dashboard/create/page.tsx** (70ff236): 버튼 "이벤트 시작하기" / QR 완료 문구 "아래 QR이나 링크를 참가자에게 공유해 영상을 모으세요"
**dashboard/events/[eventId]/page.tsx** (117b11d): QR 문구 통일(위와 동일) + 선택옵션 유도 "참가자 영상을 기다리는 동안, 인트로·아웃트로·음악·색감을 골라 완성본을 더 멋지게 꾸며보세요"

### push 후 실화면 확인 잔여 (다음 세션에서 눈으로)
- signup 제목 마진 mb-10→mb-2 좁혀짐(axe 판단). 부제가 제목에 너무 붙었는지.
- 빈상태 얼굴 보이고 글자 읽히는지(blur 제거 + 오버레이 80% 최종).
- phone helper "문자로 알려드려요"가 실제 SMS 동작과 맞는지(문서 근거=렌더완료 알림 이메일+SMS, phone 용도 코드 직접 재확인 안 함).

---

## 실측 확정 — 재정찰 불필요 (2026-07-03 scout, 줄번호까지)
> 커밋/드리프트로 줄번호 밀릴 수 있으니 착수 시 head 확인 권장. 아래 파일 다수는 이 세션에서 이미 수정됨.

### src/app/signup/page.tsx (219줄)
필드 4개 email/password/name/phone(마크업 L93–152). eyebrow(원래 L80 "Host")·제목 h1 L81, glass-panel L79 안 form L92 밖. 제출버튼 L203–205 삼항. 만19세 L193–195 ageAgreed. canSubmit L40=terms&&privacy&&age&&!loading(필드값은 HTML required로만).

### src/app/dashboard/page.tsx
빈상태 분기 L169. 미인증 사용자에게도 빈상태 노출(헤더 "+새 이벤트"만 disable).

### src/app/dashboard/create/page.tsx
E(QR 완료)=별도 라우트 아님, 같은 페이지 state 뷰 전환 View="form"|"created"(L83), setView L161, 분기 L240/L411, router.push 없음. 제출버튼 L405–407. E QR 문구 L423. "이벤트 만들기" 누르면 바로 QR 초대 가능 상태로 열림(→"이벤트 시작하기"가 동작 부합).

### src/app/dashboard/events/[eventId]/page.tsx
참가자 초대 섹션 L1020(status==="open"&&shareUrl). eyebrow L1022. F QR 문구 L1032. QR 렌더 L1025, 공유링크+복사 L1034–1043. **QR 박스 공용 컴포넌트 없음** — E·F 각자 qrcode.react QRCodeSVG 인라인(문구 통일은 두 파일 각각 수정, 이 세션 처리됨). 선택옵션 래퍼 L1051, eyebrow L1052, 기존 설명 L1053. 클립 목록 "업로드된 클립 (N개)" 개수 이미 셈. ★클립목록·완성영상 블록 줄번호는 scout 미확인(2차 착수 시 정찰 필요).

### src/components/FlowStrip.tsx
props 없음, STEPS L5–10 하드코딩, Icon L16–64(인라인 SVG), Arrow L66–87(선+화살촉, accent 0.55). 각 단계 세로 컨테이너 L94 고정폭 w-14. 사용처 1곳 upload/[eventId]/page.tsx:7,:462(!isReturning). 게스트 전용 → 호스트 여정과 개념 달라 코드 재활용 안 함(빈상태는 인라인 별도 구현, Arrow 시각만 참고).

---

## 2차 백로그 — 이벤트 대시보드 화면 재설계 (CD 시안 먼저, 미착수)
카피 아님. 레이아웃·정보구조·시각위계 → CD 시안 → scout → CC. 회귀 위험 있어 시안 합의 후.
대상 = dashboard/events/[eventId]/page.tsx 상태별 재구성:
- 수집중: "클립 N개 올라옴"을 최상단 크게(개수 이미 셈, 표시 위치 이동).
- 순서 재배치: [선택옵션 → 클립목록 → 참가자 초대 QR] (QR을 하단으로).
- 클립 목록 조밀 UI(수십 개 한눈에, 이름으로 고름). 현 카드형 세로로 길어 몇 개만 보임.
- 시각 위계: 섹션 제목 크게, 옵션 select·버튼 작게.
- 마감(편집완료): 완성 영상 맨 위(현 위치 미확인 → 정찰 필요).
- 선행: 클립목록·완성영상 줄번호 scout 정찰.

## 3차 백로그 — 새 이벤트 폼 스마트 기본값 (기능, 미착수)
카피 아님. DB 값 프리필=동작 변경. scout(create 데이터흐름) → CC.
- 이메일·전번: 로그인 호스트 users 문서에서 기본값(수정 가능). 날짜: 당일 기본. 이벤트 이름: 이름 넣은 예시("○○의 결혼식에 축하영상 보내세요") placeholder냐 실입력이냐 결정 필요.
- 근거: 라나 리서치 스마트 기본값(활성화 에너지↓) + 아는 정보 다시 묻지 않기.

## 기타 백로그 (YAGNI, 미착수)
- 미인증 시 "첫 이벤트 만들기" CTA에 disabled 표시/인증 유도 문구(위 CTA 건 참조).

## 아직 안 본 화면 (다음에 마저 훑기)
운영자가 첫-테스트 흐름을 이벤트 대시보드(F)에서 멈춤. 미확인:
- 참가자 업로드 화면(upload/[eventId], FlowStrip 그 화면).
- 완성본 결과 화면(렌더 후 호스트/게스트).
- 이어서 훑기 → 카피/레이아웃/기능 3바구니 분류.

---

## 외부 리서치 요약 (라나 — 완주율/온보딩 표준)
- 활성화=핵심가치 첫 체험(우리=셀프테스트 완주). SaaS 평균 37.5%, 2/3 미도달. 가입수 아닌 활성화수 최적화.
- 필드 줄이기가 최강 레버(7→3필드 이탈 44.7%↓). 우리 4필드=이미 린 → 카피 보강이 맞는 수.
- helper text="왜 필요?"에 답, 필드 바로 아래. 구체성>간결성.
- 빈 상태=teachable moment. 긍정 제목+다음행동+이점+단일CTA. "빈 종이 공포" 낮추기. 여정지도=getting-started 체크리스트. (이 세션 빈상태 작업의 근거)
- 약속-경로 일치: 랜딩 "무료로 시작하기"→가입 버튼 같은 문구.
- 한 번에 하나만(전술 쌓으면 상쇄)=1/2/3차 분리 근거.
- 주의: 자료 대부분 B2B·장기리텐션. 우리는 일회성 소비자(웨딩) → "한 세션 완주·단일CTA·활성화에너지↓"에 무게.

## 다음 세션 후보 (우선순위)
1. push 후 실화면 잔여 확인(위 3개) — signup 제목 마진 / 빈상태 얼굴·가독성 / phone helper 사실성.
2. 첫-테스트 흐름 마저 훑기(참가자 업로드·완성본) → 3바구니 분류.
3. 2차 이벤트 대시보드 재설계 — CD 시안 먼저, 줄번호 scout 선행.
4. 3차 새 이벤트 폼 프리필 — create 데이터흐름 scout 선행.
5. (별도 트랙) 결제 스파이크(Toss v2 test키) / 이메일 도달성 A층 재확인 / 미인증 계정 전수 정찰 — 핸드오프 2026-07-03-email-verification-domain-fix.md 참조.

## 이 세션 학습 한 줄
- 운영자가 실제 흐름을 화면 순서로 훑으면 마찰이 쏟아지되 성격(카피/레이아웃/기능)이 섞인다. 나오는 즉시 3바구니 분류 + "카피만 먼저 실행"으로 끊어야 1차가 안 무거워짐. 그리고 화면 "먹통" 신고는 코드 버그 의심 전에 계정 상태(미인증 등) 먼저 확인 — 이번 CTA 먹통이 미인증이 원인이었음.
