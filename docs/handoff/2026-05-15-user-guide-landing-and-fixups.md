## 세션 한 줄 요약

호스트(5단계) + 게스트(4단계) 사용 가이드 페이지 3종 신설 + 랜딩 미리보기 섹션 +
푸터 링크 (atomic 1커밋). 이어서 보정 후보 6건 묶음 atomic 1커밋. 두 커밋 모두
빌드 통과·실전 검증 완료.

## 본 세션 커밋

| # | 해시 | 메시지 | 핵심 변경 |
|---|---|---|---|
| 1 | 1a278f4 | feat(guide): 사용 가이드 페이지 3종 신설 + 랜딩 미리보기 섹션 + 푸터 링크 | guide/page.tsx (87) + guide/host/page.tsx (254) + guide/guest/page.tsx (229) + page.tsx 수정 + CHANGELOG. 5 files, 648 insertions. |
| 2 | 9e6e76a | docs: 보정 후보 6건 묶음 처리 | PROJECT.md 2건 + known-issues→resolved 이동 + .gitignore 예외 + CLAUDE.md 학습+토큰 + .env.local.example 신규 추적. 6 files, 50 insertions, 9 deletions. |

origin/main 반영 + Vercel 재배포 완료. 운영자 실전 검증 통과 ("문제없슴").

---

## Track A — 사용 가이드 랜딩 통합

### 결정 과정

1. **진입점 확정**: "사용설명서 랜딩 섹션 적용 (Track A 마무리)"
2. **본문 입수**: 운영자 첨부 `congre-user-guide.pdf` (합본 15p = 표지 + 호스트 8p + 게스트 6p). pdftotext로 깨끗하게 추출 — PDF에 본문 텍스트가 다 들어 있었음
3. **랜딩 통합 방식**: **옵션 C (하이브리드)** — 랜딩 미리보기 카드 + 전용 `/guide` 페이지 3종 (`/guide`, `/guide/host`, `/guide/guest`)
4. **이미지 처리**: 일단 텍스트만 (YAGNI). 스크롤 캡처 3장(05, 08, 09)은 다음 세션 후보로 보존

### 작업 단계 분해

1. **CC 정찰** — 랜딩 page.tsx 구조 + terms/privacy 라우팅 패턴 + 공통 컴포넌트 + 디자인 토큰 컨벤션
2. **docs/guide-content.md 생성** (채팅 클로드 직접, 364줄) — PDF 본문 마크다운 정리, CC가 view로 한 번에 읽도록
3. **CC 실행** — 4 파일 생성 + 2 파일 수정 + 빌드 통과 + 커밋
4. **운영자 실전 검증** — 모바일·PC 확인, "문제없슴"

### 정찰에서 드러난 사실 (PROJECT.md와 코드 불일치 4건)

CC 정찰 보고로 globals.css 실측값이 PROJECT.md 기록과 다름:

| 토큰 | PROJECT.md (구) | globals.css (실) |
|---|---|---|
| `--bg` | #0c0b09 | #1f1c18 |
| `--surface` | #151310 | #2a261f |
| `--surface-2` | #1e1a13 | #34302a |
| `--accent-bright` | (없음) | #e8a038 (추가됨) |

→ **PROJECT.md는 이미 올바른 값으로 갱신돼 있었고**, 구 값이 남아 있던 곳은 CLAUDE.md였음
→ Track B 보정 후보 #6, #8로 처리 (#7 `--accent-bright`는 globals.css 전체 재점검 차원에서 다음 세션으로 미룸)

### 디자인 사양 (구현된 것)

**랜딩 신규 섹션**: Use Cases 다음 `.rule mt-16` 직후. eyebrow `USER GUIDE` + h2 italic + 2-column 카드 (호스트 / 참가자) + `.rule mt-16` 마무리. 모바일 stack, md 이상 2-column.

**/guide 페이지 (개요)**: terms/privacy 패턴 그대로 (nav 인라인 + max-w + .rule + eyebrow + h1 italic) + 카드 2개 (호스트/참가자 선택).

**/guide/host 페이지**: 시작하기 전에 + STEP 01~05 + FAQ 6문항 + 카카오톡 채널 안내 푸터.
**/guide/guest 페이지**: 시작하기 전에 + STEP 01~03 + 완료&FAQ 5문항 + 카카오톡 채널 안내 푸터.

**푸터**: 이용약관 옆에 "사용 가이드" 링크 추가 (→ /guide).

### 작업 중 발견된 사고 + 학습

**사고 1 — 채팅 클로드 ask_user_input_v0 한글 옵션 깨짐**

- 도구 호출 시 한글 옵션이 유니코드 이스케이프(`\uXXXX`)로 변환되는데, 어딘가에서 글자가 깨져 의미 없는 단어("좌라 조사 후 추가", "타사니 타니 검토 후" 등)가 사용자에게 표시됨
- 운영자 지적 후 발견 ("선택지 안의 한글 단어가 이상한게 많아")
- 채팅 클로드 본인은 도구 출력의 raw 형태를 보지 못해 사고 인지 불가
- **재발 방지 처리**: 별도 메모로만 (CLAUDE.md 추가 안 함). 패턴 더 관찰 후 재결정. 다음 세션 초반 채팅 클로드가 이 핸드오프 보고 인지하도록.
- **운영용 임시 가이드**: ask_user_input_v0 옵션은 짧게(2~3단어), 분명한 단어로만. 의심스러우면 평문 질문으로 갈음.

**사고 2 — 보정 후보 #6 PROJECT.md 추측 오류**

- 핸드오프(2026-05-14)에 보정 후보로 적힌 "PROJECT.md 디자인 토큰 정정"을 채팅 클로드가 view 직접 확인 없이 사양에 그대로 박음
- CC가 정찰 단계에서 PROJECT.md는 이미 올바른 값임을 발견, CLAUDE.md가 진짜 정정 대상이라고 정정 보고
- **학습**: 핸드오프 보정 후보를 받을 때 항목 그대로 믿지 말고 채팅 클로드가 직접 view로 현재 상태 확인 후 사양 확정.

**사고 3 — 화면 매핑 추측 (방지 성공 사례)**

- 21장 이미지 한꺼번에 받았을 때 직전 핸드오프(Track A) 사고 학습 적용
- 직접 매핑 추정하지 않고 21장 격자 이미지 생성 → 시각 검증 후 정확한 매핑 표 작성
- 이번엔 사고 없이 작업 진행됨
- **CLAUDE.md에 학습 정식 등재**: "화면·이미지 매핑은 추정 말고 직접 view로 확인" 룰 추가 (커밋 9e6e76a)

### Track A 학습 한 줄

> CC 정찰 단계가 채팅 클로드 추측을 정정하는 사고 조기 발견 장치로 작동한다. 채팅 클로드가 핸드오프 문구만 믿고 사양을 짜면, CC가 view로 실측해서 사양 자체의 오류를 잡아낸다. 본 세션 #6 보정은 그 사례.

---

## Track B — 보정 후보 6건 묶음 처리

### 처리 결과

| # | 항목 | 결과 |
|---|------|------|
| 1 | PROJECT.md 참가자 입력 정정 (닉네임 → 이름+전화번호) | 처리. "이름 + 전화번호 입력 →" 흐름 앞에 추가 |
| 2 | PROJECT.md 플랜 선택 항목 추가 | 처리. "이벤트 생성" 바로 아래 줄 |
| 3 | known-issues GitHub Actions cron throttling → resolved 이동 | 처리. Vercel Cron Jobs 이전 완료로 확정 |
| 4 | .gitignore `!.env.local.example` 예외 추가 | 처리. CC가 .env.local.example 전체 정독, placeholder만 확인 후 안전 추가. 신규 staged |
| 5 | CLAUDE.md 화면 매핑 학습 추가 | 처리. "보고 시 숫자 검증" 다음 줄에 추가 |
| 6/8 | 디자인 토큰 구 값 정정 | CC 정찰로 정정 대상이 PROJECT.md가 아닌 **CLAUDE.md**임을 발견 → CLAUDE.md 토큰 3건 정정으로 처리 |

### 다음 세션으로 미룬 항목 (1건)

| # | 항목 | 비고 |
|---|------|------|
| 7 | globals.css 전체 재점검 + 누락 토큰 모두 PROJECT.md 보정 | `--accent-bright` 외에도 누락 가능성. CC가 globals.css 전체 view 후 PROJECT.md 디자인 토큰 섹션 완전 동기화 |

### Track B 학습 한 줄

> 보정 후보 묶음 처리는 atomic 1커밋이 정답이지만, "어느 파일에 구 값이 남아 있는지"는 CC 정찰로 실측 확인 필수. 본 세션 #6은 PROJECT.md 아닌 CLAUDE.md였음.

---

## 본 세션 학습 요약

1. **CC 정찰 단계의 검증 가치** — 채팅 클로드 사양 짤 때 핸드오프·코드를 view로 직접 확인 안 한 부분이 있으면, CC 정찰이 사양 단계에서 잡아낸다. 본 세션 사고 #6이 그 사례. **추측이 사고로 진행되기 전에 정찰로 정정.**

2. **ask_user_input_v0 한글 옵션 깨짐 패턴** — 매 세션 잠재 위험. 채팅 클로드 본인은 깨진 출력을 못 봄. **운영자가 지적해줘야 발견되는 구조.** 별도 메모로 기록, CLAUDE.md 룰 추가는 보류 (패턴 관찰).

3. **B 옵션의 효과 (docs/guide-content.md 먼저 만들기)** — 본문 분량 큰 작업에서 별도 md 파일로 본문 정리하고 CC가 view로 읽게 하면 프롬프트 가벼움 + 본문 누락·왜곡 위험 감소. 364줄 본문이 CC 프롬프트에 박혔으면 사고 위험이 컸을 것.

4. **검증 표 룰 정착** — CC 보고 받을 때마다 검증 표 먼저 작성 (수치 일관성, 사양 일치, 자체 합계 표 패턴). 본 세션 두 번 다 적용, 사고 없음.

---

## 보정 후보 누적 큐 (다음 세션 첫 작업 후보)

본 세션에서 6건 처리, 1건 미룸. **잔여 1건**:

| # | 항목 | 출처 |
|---|------|------|
| 1 | globals.css 전체 재점검 + PROJECT.md 디자인 토큰 섹션 완전 동기화 (`--accent-bright: #e8a038` 추가 + 기타 누락 토큰 확인) | 본 세션 CC 정찰 |

---

## 미해결 — 다음 세션 진입점

### 운영자 의사 — 실전 테스트 + 그 사이 고도화 병행

다음 세션도 두 갈래:
- (a) 실전 테스트 결과 회고
- (b) 테스트 기간 중 고도화 작업

### 고도화 작업 후보

**(본 세션 완료)**:
- ✅ 사용 가이드 페이지 3종 + 랜딩 미리보기 + 푸터 링크
- ✅ 보정 후보 6건 묶음

**(다음 세션 우선순위 후보)**:

| 항목 | 출처 | 비고 |
|------|------|------|
| **사용 가이드 스크린샷 추가** | Track A 보류 분 | 본 세션은 텍스트만으로 진행 (YAGNI). 스크롤 캡처 3장(05, 08, 09) + 일반 캡처 일부 활용 사양 핸드오프에 보존됨 |
| **globals.css 토큰 완전 보정** | 보정 후보 #1 | CC가 globals.css 전체 view 후 PROJECT.md 동기화 |
| **카메라 제어 기능 확장** | 운영자 거론 | 화각·모드 다양화. "어린 친구들에게 답답 + 폰 기종별 화각 세팅 부담". 정찰부터 필요 |

**(거론 보류 큐 — 실전 테스트 결과 후 결정)** — 이전 세션 계승:
- 재렌더 UX 갭 (done 상태 버튼 미노출)
- 완성본 단일 필드 덮어쓰기 → 서브컬렉션 전환 (D2)
- 클립 메타데이터 저장 실패 (S3 고아 파일)
- 호스트 클립 제거 시 영상 미리보기 흐름 검증
- 영상 호스팅 CDN 이전 (R2 vs Shotstack)
- 미성년자 영상 법적 리스크
- 네이버 메일 도달성
- BGM 다양성 격상
- 야외 환경 음량 검증

**(보류 큐 — 운영자 트리거 대기)**:
- Track 1-B 카메라 광각 픽스 (iPhone 실기기 디버그)
- Track 4 작은 버튼 3건 부담 가능 (실전 후 평가)
- Track 5 빈 텍스트 의도적 비우기 불가 (YAGNI 보류)

**(예정된 작업 트리거)**:
- AWS 무료 트라이얼 만료 30일 전 (~2026-09-28) — 결제 전환 또는 이전 결정

---

## 사용 가이드 스크린샷 작업 — 다음 세션 진입 시 필요 정보

다음 세션에서 가이드 페이지에 이미지 추가하기로 결정하면 다음 자료가 필요:

**21장 카톡 첨부 이미지 + 매핑 표** (본 세션 시각 검증으로 작성됨):

| 번호 | 화면 | 용도 후보 |
|---|---|---|
| 02, 03, 04, 07 | 게스트 입력·카메라 화면 | guest 가이드 STEP 01~02 |
| 01 | 게스트 촬영 후 미리보기 | guest STEP 03 |
| 00 | 게스트 업로드 완료 (전달됐어요!) | guest 완료 화면 |
| 05 | 호스트 완성본 (스크롤 캡처) | host STEP 05 핵심 |
| 08 | 호스트 수집중 (스크롤 캡처) | host STEP 03 |
| 09 | 호스트 마감 직후 (스크롤 캡처) | host STEP 03 + STEP 04 |
| 10, 11 | 새 이벤트 만들기 + 로그인 | host STEP 01, STEP 02 |
| 12, 13 | 주최자 로그인 (블루투스 오버레이 있음) | 사용 X 후보 (오버레이) |
| 14~20 | 랜딩 페이지 캡처 | 사용 X (중복) |
| 06 | QR 클로즈업 가로 사진 | 사용 X 후보 (품질 낮음) |
| 16 | 호스트 대시보드 (실 데이터) | 사용 X 후보 (실 데이터) |

**스크린샷 호스팅 사양**:
- `public/guide/` 폴더에 PNG/JPG 저장
- Next.js Image 컴포넌트로 lazy load
- 모바일 mockup 프레임 합성 검토 (브라우저 UI 부분 잘라낼지)

---

## 다음 세션 시작 시 운영자 작업 — 5개 셋트

진입점: **운영자가 가져오는 고도화 영역 또는 실전 테스트 결과 회고**.

- CLAUDE.md (자동 첨부)
- AGENTS.md (자동 첨부)
- 본 핸드오프 (`2026-05-15-user-guide-landing-and-fixups.md`) ← 운영자 직접 첨부
- DECISIONS.md 인덱스 (자동 첨부)
- known-issues.md (자동 첨부)

작업 영역 파일은 진입 결정 후 영역에 맞춰 첨부.

### 진입 시나리오별 추가 필요 자료

| 진입 시나리오 | 추가 첨부 |
|--------------|----------|
| 실전 테스트 결과 회고 | 운영자가 관찰한 사고·패턴 메모 (자유 형식) |
| 사용 가이드 스크린샷 추가 | 21장 카톡 이미지 묶음 (본 핸드오프 매핑 표 참조) |
| globals.css 토큰 완전 보정 | `src/app/globals.css` |
| 카메라 제어 기능 확장 | `src/app/upload/[eventId]/page.tsx` + 카메라 관련 lib |
| 시장·BM 영역 진입 | decisions/market-product.md |
| 인프라·결제 영역 진입 | decisions/infra.md |

**첫 메시지 한 줄**:

"본 핸드오프 외엔 모르는 것으로 간주, 진단·결정 시 모르는 영역이면 명시. CC 보고 받으면 검증 표 먼저. 이번 세션 진입점은 [영역명]."

### 다음 세션 채팅 클로드 본인용 노트

- ask_user_input_v0 한글 옵션은 짧고 분명한 단어로만 (2~3단어, 모호한 한자어 X). 의심스러우면 평문 질문으로.
- 핸드오프 보정 후보 받을 때 view로 현재 상태 직접 확인 후 사양 짠다. 핸드오프 문구만 믿지 말 것.
- CC 정찰이 채팅 클로드 추측을 정정하는 장치라는 점 인지. 정찰 결과 의외면 받아들이고 사양 정정.
