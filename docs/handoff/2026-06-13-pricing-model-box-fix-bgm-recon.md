# 2026-06-13 — #3 박스 흐림 정정 + 가격 모델 전환 + BGM loop 정찰 (멀티 작업 세션)

## 한 줄 요약
이벤트 상세 옵션 카드 배경 비침(#3) `.panel` 면 복원으로 종료 + 가격 모델을 고정 플랜(소/중/대)
폐지 → 계산식 단가로 전환(docs + 무료 5개 코드 + 랜딩 표기) + BGM loop 원인 확정·해법 경로 정찰
후 등재. 본 앱 커밋 4개 + 랜딩 배포 1회.

## 본 세션 커밋 (본 앱, 시간순)
- `6a4bfc7` refactor(ui): restore panel surface on eventId option cards + drop double-dim
- `a55a434` docs(market): 가격 모델 전환 — 고정 플랜 폐지, 계산식 단가 + 무료 5개
- `1134285` feat(plans): 무료 플랜 클립 수 10→5 + 관련 화면 문구 정정
- `73a1ec8` docs(landing): 랜딩 pricing 무료 5개 표기 + 배포 ID 기록
- `c1313ed` docs(known-issues): BGM loop 미지원 등재 — soundtrack 한계 + probe 해법 경로

## 랜딩 배포 (git 외부 트랙)
- deploy/pricing.html L283 + Pricing Card.html L162 "클립 10개까지" → "클립 5개까지"
- Deployment: dpl_A8FiJUH9MfnHHnKGAuC4pYXaQCUi (production, READY)
- 푸터 약관·개인정보 절대경로 4건(app.congre.kr) 보존 확인 (known-issue L8).

## 본 세션 결정·발견

### 1. #3 옵션 카드 흐림 = 배경 비침 (글자 색 아님) — 종료
- 증상 "글자가 흐리다"는 실제로는 직전 `0fd28eb`의 `.card` 투명화로 페이지 배경 장식
  (PageBackdrop pattern="c", bg-stage-c.png 가족사진)이 폼 사이로 비친 것. 글자 색(text-muted)
  문제 아님. 화면 1장으로 가설 갈림.
- 처방: `.panel` 클래스 신설(globals.css — surface-1 면 + 약한 보더 --hairline + radius +
  padding 24px). 이벤트 상세 옵션 카드 2곳(L1045 인트로/아웃트로, L1205 영상 스타일)만
  `card`→`panel`. 안내문 2줄(L1047·L1207) inline `opacity: 0.7` 제거(이중 흐림).
- 미접촉: 같은 페이지 QR 카드(L1017)·완성영상 카드(L1327)는 운영자 b안 투명화 유지.
- **운영자 폰 확인 완료 — "해결" 종료.**

### 2. 가격 모델 전환 — 고정 플랜 폐지 → 계산식 단가 (오늘 = docs + 무료 5개만)
운영자 결정. market-product.md 2026-06-13 (7)에 전문 박음.

- **무료**: 길이 10초 / 수 5개(기존 10개 축소) / 워터마크 박힘(유지).
- **유료(소·중·대 폐지, 단일 계산식)**:
  - 결제액 = (호스트 설정 최대 클립 길이 초) × (마감 시 실제 업로드 클립 수) × 100원
  - 금액 하한선 1만원(계산값 미만이면 1만원). 길이는 호스트 설정 최대값으로 계산(개별
    클립 짧아도 최대값). 클립 수는 마감 시 실제 업로드 수. 결제 시점 = 마감 시.
  - 클립 길이 상한 120초(시스템 하드 상한). 워터마크 없음.
- **재렌더**: 매번 유료, 첫 렌더 80% (변경 없음).
- **별도 문의**: 기업 행사만. 기존 길이·총시간 한도(30초·15분) 폐기.

**오늘 코드 반영(가 옵션)**: plans.ts `PLAN_CLIP_LIMITS.free` 10→5 + 화면 문구 3곳
(create 폼 desc, guide/host L89 소중대 줄→계산식 안내, 랜딩 pricing 2파일).
서버 강제는 plans.ts 단일 출처라 한 줄로 따라옴. 화면 문구는 전부 하드코딩이라 개별 수정.

**결제 트랙(S3-05)으로 이월 — 오늘 안 함**:
- 계산식 결제 UI + PG(토스페이먼츠, 사업자등록 선행) 연결
- plans.ts 등급 구조 개편(소/중/대 PlanId·PLAN_CLIP_LIMITS·PLAN_MAX_CLIP_SECONDS 정리)
- create 폼에 여전히 살아있는 소/중/대 옵션(L17~19) 정리
- "마감 시 결제" 화면 문구(결제 UI와 한 몸 — 지금 넣으면 결제 안 떠서 혼란)
- 랜딩 4카드 → 계산식 모델 재설계 (**CD 트랙**, 다음 랜딩 사이클 #1·#5와 묶기)

**경계 확인(운영자 합의)**: 오늘은 정책 박기 + 무료 5개까지. 결제 구현은 별도 트랙.
"마감 시 결제 = 결제 먼저 → 렌더" 순서이나 PG 미구현이라 코드 미반영.

### 3. BGM loop — 원인 확정 + 해법 경로 정찰 (실행은 다음 세션)
- soundtrack loop 미지원(Shotstack 공식 직원 답변 확인). 3분 BGM도 100클립서 끊김(실측).
- 해법 B(정석): probe로 전체 길이 측정 → loop 횟수 계산 → audio clip N개 직렬 배치.
  공식 코드 패턴 존재. 단 renderer 파이프라인(⑦ 민감 영역) 구조 변경 + probe 2026-05-12
  폐기 이력 재확인(mp4에 duration 주는지) 선결.
- known-issues.md 등재 완료(c1313ed). **세션 끝물 회귀 위험 회피 위해 실행 보류.**

## 미완 작업 (다음 세션 시작 시 먼저 처리)
1. **가격 전환 폰 확인** (코드·배포 됨, 운영자 육안 대기): 본 앱 create 폼 무료 desc
   "최대 5클립" + 랜딩 pricing "클립 5개까지". 확인되면 오늘 가격 작업 완전 종료.
2. (선택) create 폼에 소/중/대 옛 플랜이 아직 선택 가능 — 폐기됐으나 결제 트랙 이월이라
   당장 사고는 아님. 인지만.

## 다음 세션 후보 (우선순위)
- **BGM loop 실행 (해법 B)**: 선결 정찰부터 — probe가 native capture mp4에 duration 주는지 +
  2026-05-12 probe 폐기 사유(WebM/비용) 현 시점 유효성. 통과 시 audio clip 동적 loop 구현.
  렌더 파이프라인 민감 영역이라 정찰→결정→실행→실측 게이트 엄수. (known-issues 등재됨)
- **#4 done 화면 재편집 버튼**: 재렌더 버튼이 status==="closed"에만 노출, done엔 없음.
  B5 결제 게이트(재렌더 매번 유료, 첫 렌더 80%)와 묶임 — 단순 버튼 아님. 결제 트랙과
  연동되므로 PG 구현 시점과 함께 보는 게 자연스러움. (known-issues "재렌더 UX 갭")
- **🔴 재렌더 완성품 품질 결함**(2026-06-11 미해결, known-issues): done 도달하나 결과 영상
  비정상. 운영자 화면 확인 후 어느 층(합성/순서/인트로·아웃트로/자막/BGM)인지 切り分け.
- **결제 트랙(S3-05) 본격**: 계산식 UI + PG + plans.ts 개편 + 랜딩 4카드 재설계(CD).
- **#1·#5 랜딩 히어로 CTA**(CD): 이전 핸드오프 잔여. 랜딩 4카드 재설계와 묶어 CD 한 번에.

## 본 세션 학습 한 줄
- 시각 "흐림" 보고는 글자 색 vs 배경 비침이 헷갈림 — 화면 1장이 코드 정찰 여러 번보다 빨랐다.
- API loop류는 이름값/통념 추론 금지(soundtrack에 loop 있을 거라 기대했으나 미지원). 공식
  문서+커뮤니티 직원 답변으로 확정. 실측(3분도 끊김)이 "자산이 짧아서" 1차 가설을 깸.
- 정책 전환은 docs→코드→랜딩 순서 분리 실행이 깔끔했음. 랜딩 git 외부라 landing.md 갱신을
  같은 흐름에 넣어 누락 방지(이전 세션 학습 룰 반영).
