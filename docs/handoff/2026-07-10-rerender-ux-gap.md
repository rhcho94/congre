# 2026-07-10 핸드오프 — 재렌더 UX 갭 (done 버튼 + 확인 모달)

## 한 줄 요약
재렌더 UX 갭 ①②(done 상태 재렌더 버튼 노출 + 재렌더 직전 확인 모달) 구현·push·라이브 확인 완료. 방향 C(모달은 안내+확인, 클립 재선택은 화면 인라인 목록, 인트로/아웃트로는 대시보드에서). ③ 결제 게이트는 결제 트랙(Toss v2)으로 분리. 대상 파일 1개: src/app/dashboard/events/[eventId]/page.tsx.

## 이 세션 push 완료 — origin/main 반영
- 3c648c6 feat(rerender): done 상태 재렌더 버튼 + 재렌더 확인 모달 (+54/-6, 코드1+문서3)
- Vercel 자동배포 → app.congre.kr 라이브. Ray 실화면 확인 완료(모달 노출·취소·M=0 비활성·closed 공용 모달 전부 정상).

## 한 일 상세 (정찰 3회 → axe → eye → 커밋)
- 스코프: ①done 버튼 ②확인 모달만. ③재렌더 결제 게이트는 FGT로 결제 전면 비활성이라 지금 켤 수 없는 게이트 → 결제 트랙으로 분리.
- 구현: includedCount = clips.filter(c=>!c.excludedAt).length 신규 파생값 / showRerenderModal state / 확인 모달(마감 확인 모달 L796-815 패턴 복제, .notice max-w-sm) / done glass-panel 내부 "영상 다시 만들기" 버튼 / closed 버튼 onClick도 모달 공유로 변경.
- 모달 = 방향 C: "전체 N개 중 M개 포함" 요약 + "아래 목록에서 클립 재선택, 대시보드에서 인트로/아웃트로 변경" 안내. 편집 UI를 모달에 복제하지 않음(기존 화면 UI가 이미 있음).
- M=0(전부 제외) 시 모달에서 [다시 만들기] 비활성 + 안내(서버 NO_CLIPS_AFTER_EXCLUSION 도달 전 차단).
- 무변경 확인(eye): handleRestartRender/callRenderStart 본문, FGT 유료 가드(PAID_NOT_AVAILABLE), 클립 토글 API, /api/render/start. handleRestartRender는 excludedAt을 안 건드림 → 화면에서 골라둔 제외 상태 그대로 재렌더(방향 A/C 전제 코드로 확인).
- build 통과, lint 11/3 baseline delta 0. axe·eye 독립 검증 동일 결론.

## 이 세션 결정
- 방향 C 채택: 모달은 안내+확인만. 클립 재선택/인트로·아웃트로 조정은 이미 있는 화면 UI로. 편집기능 모달 복제(방향 B)는 중복+YAGNI로 기각.
- 결제 게이트는 이번 스코프 제외 → 결제 트랙에 남김.
- done·closed 재렌더 버튼이 같은 확인 모달을 공유(실수 방지 + 코드 단순).

## 라이브 버그 소동 기록 (원인=캐시, 코드 정상)
- 증상: done "영상 다시 만들기"가 모달 없이 바로 재렌더로 직행(최초 라이브 확인 시).
- 진단: scout 코드 정찰 + git 확인 → 소스 정상(onClick=setShowRerenderModal, 모달 마운트 정상, 재렌더 직행 경로 없음, 3c648c6 origin 반영). 배포/캐시 층 문제로 판정.
- 해소: 시크릿 창/하드 리프레시 후 모달 정상 노출. 코드 수정 없이 해결. 관찰 오차 가능성도 있었음.
- 학습: 라이브 동작이 검증된 코드와 어긋나면 axe부터 부르지 말 것. 배포 Ready 상태 + 캐시(하드 리프레시/시크릿) 먼저 걷어낸 뒤 재현되면 그때 코드 정찰.

## 미착수 (다음 세션 후보, 우선순위)
1. **이전 완성본 보존** (Ray 요구, 이번 세션 발생) — 재렌더해도 이전 완성본을 보이고 다운로드 가능하게("돈 낸 결과물"). 현 구조는 events.videoS3Key 단일 필드 덮어쓰기라 재렌더 시 이전 영상 소실. known-issues "완성본 단일 필드 덮어쓰기 (D2)" 항목 = 서브컬렉션 전환 + 7일 보관 + 목록 UI. 결제 트랙과 묶임("돈 낸 결과물" 논리). → 결제 트랙(Toss v2)에서 재렌더 결제 게이트(③) + 이전 완성본 보존을 한 묶음으로.
2. 첫-테스트 흐름 후반부(참가자 업로드 upload/[eventId]·완성본 결과 화면) 카피/레이아웃/기능 3바구니 분류 — Ray가 별도로 이미 훑음(A 트랙, 완료 보고). 잔여 있으면 재개.
3. (별도 트랙) 보안 감사 1회(/security-review) — UI 트랙 종료 조건 충족. presigned·Firestore 규칙·render/start 결제 가드 묶음.
4. (별도 트랙) 이메일 도달성 A층(Gmail 스팸) 재확인 / 결제 스파이크(Toss v2).

## 이 세션 학습 한 줄
- "재렌더 버튼 하나 추가"가 정찰 들어가니 3요소(done 노출·클립 모달·결제 게이트)로 갈렸고, 그중 결제는 FGT로 지금 켤 수 없는 게이트였다. 요구를 코드로 옮기기 전에 "지금 이게 켜지긴 하나"부터 확인하는 게 죽은 코드를 막았다.
