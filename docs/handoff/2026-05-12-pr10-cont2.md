## 세션 한 줄 요약

트랙 2-A 항목 6 라운드 ② (커밋 3) 실행 완료. 대시보드 인트로/아웃트로
업로드 UI + 텍스트 편집 + GET/PATCH/invite-urls API 확장 + s3.ts 타입
동기화. 5파일 1커밋 push. 다음 세션은 라운드 ③ (커밋 4 — shotstack
multi-track + render/start 통합).

## 본 세션 커밋

| # | 해시 | 메시지 | 변경 |
|---|---|---|---|
| 1 | 4a1e058 | feat: add intro/outro upload UI + text edit in dashboard | 5파일 (아래 분해) |

push 완료, origin/main 반영.

### 파일별 변경

| 파일 | 변경 라인 |
|---|---|
| src/app/dashboard/events/[eventId]/page.tsx | +293, -1 |
| src/app/api/host/events/[eventId]/route.ts | +58, -2 |
| src/app/api/host/events/[eventId]/invite-urls/route.ts | +9, -1 |
| docs/CHANGELOG.md | +4 |
| src/lib/s3.ts | +1, -1 |

(합계는 의도적으로 생략 — 본 세션 검증 사례 참조. 항목별 숫자만 권위로 둠.)

## 본 세션 결과

### route.ts (host/events/[eventId])

GET 응답: 9필드 → 15필드. 추가 6필드 모두 null 가능.
- introText, introMediaKey, introMediaType
- outroText, outroMediaKey, outroMediaType

PATCH 화이트리스트: 3필드 → 9필드. 동일 6필드 추가.

검증 분기 추가:
- introText/outroText 60자 상한 → INVALID_INTRO_TEXT / INVALID_OUTRO_TEXT
- mediaType "image"|"video"|null 외 → INVALID_INTRO_MEDIA_TYPE / INVALID_OUTRO_MEDIA_TYPE
- Key·Type 정합성 (정확히 한쪽만 보내면 거절) → INVALID_INTRO_MEDIA / INVALID_OUTRO_MEDIA

값 처리: 기존 hasWelcomeText 패턴 그대로. null 또는 빈 값 → FieldValue.delete().

### invite-urls/route.ts

응답 2필드 추가: introMediaUrl, outroMediaUrl.
기존 keyToPresignedUrl + Promise.all 배열에 2항목 추가, 패턴 그대로.
S3 미설정 fallback도 4필드 객체 반환으로 갱신.

### page.tsx (dashboard)

배치: 초대장 작성 섹션 직후, QR 코드 섹션 직전 (line 898 부근).

UI 구조:
- 인트로 블록 + 아웃트로 블록 (대칭)
- 각 블록: 텍스트(textarea + 60자 카운트 + 저장 버튼) + 미디어(단일 슬롯)
- 미디어 슬롯: accept="image/*,video/*", 10MB 상한
- mediaType 자동 판단: file.type.startsWith("video/")
- 미리보기: introMediaType === "video" ? <video controls playsInline> : <img>
- 삭제 시 introMediaKey + introMediaType 동시 null 처리

상태 변수 14개 추가 (인트로 7 + 아웃트로 7). 평면 useState 패턴 유지.
컴포넌트 분리 안 함 (기존 page.tsx 인라인 JSX 일관성).

초기화: fetchEvent 응답에서 6필드 + introMediaKey || outroMediaKey
조건에 invite-urls 호출 추가.

### s3.ts

getPresignedUrl 함수 kind 타입 1줄 확장:
- 변경 전: `"clip" | "invite"`
- 변경 후: `"clip" | "invite" | "intro" | "outro"`

라운드 ① 커밋 2에서 presign **서버** 화이트리스트는 확장됐으나
**클라이언트** 헬퍼 타입 동기화 누락된 상태였음. 본 세션 빌드 실패로
발견 → 운영자 승인 후 커밋 3에 포함.

## 환경 이슈

`.next` 캐시 EPERM 재발 (1회). 라운드 ① 동일 패턴.
- 트리거: s3.ts 수정 후 빌드 시도
- 해결: .next 삭제 후 재빌드 → 통과
- 빈도 누적: 라운드 ①·② 각 1회 = 2회. 본 환경(OneDrive 동기화) 고질적
  가능성 시사. 3회 이상 누적 시 known-issues 등록 검토.

## 사양 vs 핸드오프 정찰 어긋남

라운드 ② 진입 직전 사전 정찰(채팅 클로드)에서 발견·정정된 어긋남.

| 항목 | 라운드 ① 핸드오프(cont) 추정 | 실제 |
|---|---|---|
| 작업 영역 파일 수 | 사양표 원본 "page.tsx + route.ts" 2파일, 라운드 ① cont 추가 표기 "4파일 영역"(출처 불명) | 실제 5파일. invite-urls/route.ts, s3.ts, CHANGELOG.md 누락 |
| line 802 부근 배치 후보 | "인트로/아웃트로 섹션 자연스러운 배치" | line 802는 렌더 결과 표시 영역. 인트로/아웃트로 편집은 line 898 (초대장 직후) 배치가 자연스러움 |
| invite-urls 확장 필요 여부 | 미언급 | D7 "GET route 확장" 보강으로 필요. 사전 정찰 결과 패턴 복붙 가능 |
| s3.ts 클라이언트 헬퍼 타입 | 미언급 | 빌드 실패로 발견. 커밋 2 작업 시 동기화 누락 |

라운드 ② 직전 사전 정찰(invite-urls 1회 CC 위임)이 본 라운드 사양 보강
(D7 확장: invite-urls 포함)에 결정적이었음. s3.ts는 그 정찰로도 못 잡힘 —
정찰 영역이 "현재 코드 + 사양 영향 파일"이었지 "타입 의존 그래프"까지는
아니었음.

## 미해결 — 다음 세션 후보

### 본 의제 (트랙 2-A 항목 6)

- **라운드 ③ — 커밋 4**: shotstack multi-track + render/start 통합
  - 영역: src/lib/shotstack.ts, src/app/api/render/start/route.ts
  - 회귀 위험 최대 — 사양표 명시
  - 4케이스 회귀 테스트 필수:
    1. 미디어 있음 + 텍스트 있음 → overlay 동작
    2. 미디어 없음 + 텍스트 있음 → 기존 검은 배경 호환
    3. 미디어 있음 + 텍스트 없음 → 미디어만 표시
    4. 둘 다 없음 → 참가자 영상만
  - 문서 갱신 동시 커밋:
    - docs/decisions/rendering.md에 multi-track 결정 추가
    - known-issues "한글 인트로/아웃트로 기능 미구현" → RESOLVED 이동
    - known-issues "인트로/아웃트로 편집 UI 미존재" → RESOLVED 이동
    - docs/CHANGELOG.md 항목 추가
  - 사양 참조: 직전 핸드오프(2026-05-12-pr10.md) Multi-track 구조 섹션

### 본 의제 외 (직전 핸드오프에서 이월)

- 트랙 2-A 항목 2 (filter 단일 적용)
- 트랙 2-A 항목 3 (Ken Burns effect 다양성, 인접 회피 알고리즘)
- 트랙 2-A 항목 1·2·5 (정의 미확정)
- 카카오톡 공유 SDK 분기 검증 (cont3 미해결 4번)
- fetchEvent line 126 안전망 (cont3 미해결 1번, 트리거 없음)
- 5초 폴링 운영 비용 메모 (cont3 미해결 2번, 트리거 없음)

## 현재 시스템 상태 (라운드 ② 종료 시점)

- 호스트가 대시보드에서 인트로/아웃트로 텍스트·미디어 편집 가능
- Firestore에 저장됨 (introText, introMediaKey, introMediaType, outro 동일)
- **렌더 결과엔 아직 미반영** — multi-track 적용은 커밋 4 영역
- 사양 호환 분기 유지: 인트로/아웃트로 없는 기존 이벤트도 정상 동작

## 자체 학습

- **클라이언트 헬퍼 타입 ↔ 서버 화이트리스트 동기화**: 라운드 ① 커밋 2
  작업 시 presign 서버 화이트리스트만 확장하고 src/lib/s3.ts의
  getPresignedUrl kind 타입 동기화 누락. 라운드 ②에서 page.tsx가
  새 kind("intro"/"outro") 호출 시 TypeScript 빌드 실패로 발견. **다음
  부터 서버 화이트리스트(또는 enum) 확장 시 클라이언트 헬퍼 타입
  동기화를 셋트 작업으로 처리**. 정찰 단계에서 "kind를 쓰는 클라이언트
  함수 시그니처 확인"이 체크리스트.

- **보고 숫자 자체 검증 룰의 작동 사례**: CC가 합계 +361/-9로 보고했으나
  실제 항목별 합산은 +365/-5. 사용자 룰 7번(보고 숫자 자체 검증)이
  정확히 이런 케이스 잡으라고 있는 거. **다음부터 핸드오프엔 항목별
  숫자만 두고 합계 자체를 생략**. CC가 합계 필요하면 git diff --stat을
  권위로 출력해 그대로 인용.

- **사전 정찰 1회의 가치 — 라운드 ② 검증 사례**: 라운드 ② 진입 직전
  사전 정찰(채팅 클로드 page.tsx 정찰 + CC invite-urls 정찰)에서 cont
  요약의 어긋남 3개 발견·정정 (line 802 위치 오해, invite-urls 누락,
  사양표 보강 필요). 라운드 ① 학습 "사전 정찰 1라운드 옵션"이 실제로
  사양 확정 단계에서 작동. **다음 라운드(③) 진입 시에도 동일 패턴
  유지**. 라운드 ③은 회귀 위험 최대 영역이라 정찰 필요도 가장 높음.

- **OneDrive 환경 .next EPERM 빈도 누적 2회**: 라운드 ①·② 각 1회. 1회만
  관측 시 보류했으나 2회 누적. 라운드 ③에서 1회 더 발생 시 known-issues
  등록 권장. 현재는 코드 영향 없음 — .next 삭제 후 재빌드로 매번 해결.

- **임의 수정 금지 원칙 작동**: CC가 s3.ts kind 타입 확장이 필요한 빌드
  실패 발견 시 임의로 고치지 않고 작업 중단 + 보고 + 승인 요청. 이건
  사용자 룰 9번(정찰→결정→실행→검증) + CLAUDE.md "확인과 실행 분리"가
  정확히 작동한 케이스. **다음 라운드에서도 사양 외 변경 발견 시 동일
  패턴 유지**.

## 다음 세션 시작 시 운영자 작업 — 컨텍스트 셋트

진입점: 트랙 2-A 항목 6 라운드 ③ (커밋 4 — shotstack multi-track +
render/start 통합)

5개 셋트 (CLAUDE.md "핵심 5개 셋트"):
- CLAUDE.md
- AGENTS.md
- 직전 핸드오프 사양 원본 (2026-05-12-pr10.md) — Q1~Q6, D1~D7 확정표 + Multi-track 구조
- 본 핸드오프 (2026-05-12-pr10-cont2.md) — 라운드 ② 결과 + 라운드 ③ 4케이스 회귀 테스트 사양
- DECISIONS.md (인덱스) + docs/decisions/rendering.md 본문 (커밋 4 영역 직접 관련)
- known-issues.md

작업 영역 파일 (운영자 첨부, 라운드 ③ 사전 정찰용):
- src/lib/shotstack.ts (커밋 4 — multi-track 구조 영역)
- src/app/api/render/start/route.ts (커밋 4 — createRender 호출자, eventData에서 미디어 키 읽기)

추가 권장 첨부 (사전 정찰 정확도↑):
- src/lib/events.ts (라운드 ① 결과 — 미디어 필드 4개 확정 상태 확인용)
- src/app/api/upload/presign/route.ts (라운드 ① 결과 — kind 화이트리스트 4값 상태)

첫 메시지 한 줄:

"진입점: 트랙 2-A 항목 6 라운드 ③ (커밋 4 — shotstack multi-track +
render/start 통합). 컨텍스트 5개 셋트 + 라운드 ② 종료 핸드오프 + 작업
영역 2~4파일. 외엔 모르는 것으로 간주, 진단·결정 시 모르는 영역이면
명시. 사양은 2026-05-12-pr10.md 확정표 그대로 (Multi-track 구조 섹션).
라운드 ② 학습대로 사전 정찰 1라운드 거친 후 실행 프롬프트 작성. 회귀
위험 최대 영역이라 사전 정찰 깊이는 라운드 ②보다 한 단계 더."
