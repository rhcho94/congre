## 세션 한 줄 요약

PDF 갱신 본인 영역 실행 (게스트 5p + 호스트 8p, native capture 흐름 반영) →
호스트 FAQ 자체 판단 영역 본인 정정 → CLAUDE.md 룰 2건 등재 (사양 외 자체
판단 명시 + 단편 단서로 전체 단정 금지). 합본 PDF + 보정 #1 globals.css는
다음 세션 이월.

## 본 세션 커밋 (2건, 모두 CC)

| # | 해시 | 메시지 | 핵심 변경 |
|---|---|---|---|
| 1 | ab59ab1 | chore(rules): add "사양 외 자체 판단 명시" rule to CLAUDE.md | CLAUDE.md +1 / CHANGELOG.md +1 |
| 2 | f68b4c0 | chore(rules): add "단편 단서로 전체 단정 금지" rule to CLAUDE.md | CLAUDE.md +1 / CHANGELOG.md +1 |

origin/main 반영 완료. 운영자 GitHub에서 양 커밋 모두 확인.

본 세션 채팅 클로드 본인 산출물 (커밋 외):
- `congre-guest-guide.pdf` (5p, 89.6 KB) — present_files로 운영자 공유 완료
- `congre-host-guide.pdf` (8p, 95.1 KB) — present_files로 운영자 공유 완료

PDF 산출물은 본 세션 컨테이너 임시 디렉토리. 프로젝트 저장소에 들어가지 않음.
운영자가 다운로드해 본인 PC에 보관하는 영역.

---

## Track A — PDF 갱신 본인 영역 실행

### 진입점

직전 세션 핸드오프 (2026-05-16-android-hotfix-validation-and-guide-refresh)
D1c 사양 분리분. 페이지 영역(`.tsx`)은 직전 세션에서 처리됐고 PDF 영역만 이월.

운영자 진입 시 선언: "3개 PDF + 가이드 페이지 영역으로 진입". 운영자는 게스트
PDF + guest/host 페이지 .tsx 첨부.

### 정찰 — 첨부 자료 확인 (1차)

| 자료 | 상태 |
|------|------|
| 게스트 PDF (`congreguestguide.pdf`) | 자동 첨부 (PROJECT 파일 영역). 6페이지, A4 가로, ReportLab 생성 (2026-05-14 13:30:33 UTC). |
| 호스트 PDF | 미첨부. 운영자에 추가 요청 후 8페이지 PDF 받음. |
| 합본 PDF (15p) | 미첨부. 본 세션 작업 범위 밖. |
| 가이드 페이지 .tsx 2개 | 첨부됨 (guest + host). |

### 게스트 PDF 정찰 결과 (시각 + 텍스트)

ffprobe(pdfinfo)·pdftotext·pdftoppm 100dpi 래스터화·pdffonts로 정찰.

| 페이지 | 갱신 필요 | 비고 |
|--------|----------|------|
| 1 표지 | ○ 작음 | "네 단계" → "세 단계" |
| 2 START | × | 그대로 유효 |
| 3 STEP 01 | × | 텍스트·스크린샷 모두 유효 |
| 4 STEP 02 (구 "카메라 켜기") | ● 전면 재작성 + 스크린샷 교체 | 인앱 카메라 UI stale |
| 5 STEP 03 (구 "촬영하고 확인하기") | ● 전면 재작성 | 미리보기 스크린샷은 유효 |
| 6 DONE & FAQ | × | 그대로 유효 |

폰트 식별: Noto Sans KR Thin (Regular/Medium/Bold) + DM Sans (Regular/Medium
/Bold) + Cormorant Garamond (Italic/BoldItalic) 임베디드.

### 호스트 PDF 정찰 결과

| 페이지 | 갱신 필요 | 비고 |
|--------|----------|------|
| 1 표지 | × | "다섯 단계" 유지 |
| 2 START | × | 그대로 유효 |
| 3 STEP 01 로그인 | × | 그대로 유효 |
| 4 STEP 02 새 이벤트 만들기 | ○ 입력 항목 1불릿 추가 | 클립 길이 항목 신규 |
| 5·6·7 STEP 03·04·05 | × | 그대로 유효 |
| 8 FAQ | × | 그대로 유효 |

### 결정 갈래 4건 (운영자 영역)

| 결정 | 옵션 | 운영자 선택 |
|------|------|------------|
| 1. 게스트 STEP 02·03 통합 | 3-STEP 통합 / 분리 유지 | **3-STEP 통합 (5페이지)** |
| 2. 작업 범위 | 게스트만 / 게스트+호스트 / 셋 다 | **게스트+호스트 (합본 이월)** |
| 3. 호스트 STEP 02 스크린샷 | 갱신 없이 텍스트만 / 새 캡처 | **갱신 없이 텍스트만** |
| 4. ReportLab 소스 코드 | 운영자 보관 / 새로 작성 | **새로 작성** |

모두 본인 추천대로 운영자 확정.

### 실행 — 환경 준비

- 컨테이너에 Noto Sans KR / DM Sans / Cormorant Garamond 폰트 없음
- npm `@fontsource/noto-sans-kr` · `@fontsource/dm-sans` · `@fontsource/cormorant-garamond` 패키지로 woff 받고 fontTools로 woff → ttf 변환
- 첫 시도 google/fonts 저장소 직접 다운로드 시도 → HTML 페이지 반환됨 (URL 실패). @fontsource 경로로 정정

학습: 외부 자료 검색 트리거 자체 발동 사례 (5/15 native-capture 핸드오프의 학습 항목 본 세션 적용). 처음 시도 실패 후 다른 경로 즉시 검토.

### 실행 — 코드 작성 (본인 영역)

- `/home/claude/pdf_common.py` — 디자인 토큰, 폰트 등록, 페이지 템플릿, 헬퍼 함수
- `/home/claude/build_guest.py` — 게스트 PDF 빌드 (5페이지)
- `/home/claude/build_host.py` — 호스트 PDF 빌드 (8페이지)

디자인 토큰 모두 globals.css와 일치: bg #0c0b09, accent #c8892c, text #ede8df, muted #79716a, rule #2a2520. A4 가로 841×595 pt.

### 의외값 4건 (게스트 PDF 첫 빌드 후 시각 검증에서 발견)

검증 표 룰 효과로 즉시 포착:

| # | 의외값 | 원인 | 정정 |
|---|--------|------|------|
| 1 | STEP 02 "촬영 시간" 두 줄 겹침 | `draw_para` 반환값 의미 처리 + leading 부족 | `draw_para` 반환값을 "다음 줄 시작 y"로 단순화 + 호출부 일괄 조정 |
| 2 | STEP 02 "예시 멘트" 섹션 누락 | 작성 시 빠뜨림 | 졸업식·결혼식·기업 행사 3개 추가 |
| 3 | FAQ 항목 간 간격 부족 | `cy -= 14` 다음 항목 시작 즉시 부착 | `cy -= 8` 추가 간격 (이미 본문 leading 후) |
| 4 | Placeholder 캡션 위치 어색 | 박스 아래 중앙 정렬 | 박스 바로 아래 우측 정렬 (기존 PDF 패턴) |

검증 표 룰 사고 발견 효과 본 세션도 입증.

### 사양 외 자체 판단 1건 (보정 #2 룰 첫 트리거)

**호스트 PDF FAQ 페이지 2단 분할 자체 결정**.

- 기존 호스트 PDF는 FAQ 6개를 세로 1단 나열
- 본인이 좌·우 2단 분할로 자체 구성 (사양에 안 짚었던 영역)
- 결과 시각적으로 가독성·균형 OK였으나 사양 일탈
- 운영자에 보고 시 "운영자 검토 영역 1건"으로 명시 → 운영자 정정 결정
- F 단계에서 본인이 1단 세로 나열로 복원

본 사례는 보정 #2 결정 영역의 결정타가 됨 (옵션 다 → 옵션 나 채택 흐름).

---

## Track B — 호스트 PDF FAQ 1단 복원 (F 영역)

### 진입점

운영자 D + F 진입 결정. D 룰 등재 직후 F 진입.

### 실행

`build_host.py`의 `page_faq()` 함수 단일 교체:
- 좌·우 2단 분할 (col_w 분할 + faqs_left/faqs_right 분리) 제거
- 1단 세로 나열 (faqs 단일 리스트 + 순차 draw_h3 + draw_para)
- 폰트 크기 10pt → 11pt (본문 일관성)
- 항목 간 간격 `cy -= 14` 추가

빌드 통과. FAQ 페이지(8p) 시각 검증으로 기존 PDF 패턴과 일치 확인. 다른 페이지
영향 0.

운영자에 새 호스트 PDF 단독 공유 (게스트 PDF는 이전 메시지 카드 그대로).

---

## Track C — 룰 등재 2건

### D — 사양 외 자체 판단 명시 (ab59ab1)

#### 사례 누적 3건
1. 5/16 안드로이드 검증 세션 CC: PROJECT.md 사양 일탈 (누락분 자체 보강)
2. 본 세션 채팅 클로드: 호스트 PDF FAQ 2단 분할 자체 결정
3. (잠재) 직전 세션 보정 큐 #3 항목

#### 결정 영역 (운영자)

| 결정 | 선택 |
|------|------|
| 옵션 가(멈춤)/나(영역 구분)/다(보고만) | **다** |
| 위치 가("협업 스타일")/나("절대 규칙") | **가** |

모두 본인 추천대로.

#### 등재 문안

> **사양 외 자체 판단 명시**: 사양에 없는 영역을 자체 판단으로 처리한 경우
> 결과 보고에 "사양 외 자체 판단" 항목으로 명시. 운영자 검토 후 정정 결정.

### 보정 #2 — 단편 단서로 전체 단정 금지 (f68b4c0)

#### 사례 누적 5건 도달 (직전 세션 5/16 핸드오프 명시)

본 세션 회수 가능한 사례 4건:
1. 5/15: 53fc748 첫 줄 "Phase 4.1" → pickleball 단정
2. 5/15: 운영자 "이번이 처음인 듯" → congre 맥락 단정
3. 5/15: CC 보고 "+N lines (ctrl+o to expand)" → 본문 봤다고 단정
4. 5/15: 두 번째 클립 화각 문제 → "MediaRecorder 세로 회전 인식 못 함" 단정

5번째 사례는 5/16 직전 세션(hotfix 검토)에 있다고 5/16 핸드오프 명시. 본 세션
본인 컨텍스트엔 안 들어옴.

#### 결정 영역 (운영자)

| 결정 | 선택 |
|------|------|
| 옵션 가(단호)/나(트리거 명시)/다(보고만) | **나** |
| 위치 | **"협업 스타일" 섹션** |

모두 본인 추천대로.

#### 등재 문안

> **단편 단서로 전체 단정 금지**: 단편 단서(첫 줄·키워드·표기 일부)를 받았을
> 때 그것만으로 전체를 단정하지 말 것. 멀티 프로젝트·낯선 키워드·압축 표기
> (`+N lines`, `ctrl+o to expand` 등) 상황에서 특히 본문 직접 확인 또는
> 운영자 맥락 확인 필수.

### 본 세션 룰 첫 트리거 즉시 작동

보정 #2 등재 직후 본인이 CC push 보고에서 `+2 lines (ctrl+o to expand)` 표기
포착. 단정 안 하고 운영자 확인 영역으로 분리 → 운영자 GitHub에서 양 커밋 확인.

이전 D 보고에도 같은 표기 있었으나 본인 안 잡았음. 룰 등재 자체가 본인 의식
모드 바꾸는 효과 관측. 본인용 학습.

---

## 본 세션 학습

### 1. 자체 판단 영역 확장 패턴 본인 발생

호스트 PDF FAQ 2단 분할. 5/16 핸드오프 본인용 노트의 "CC 자체 판단 영역 확장"
패턴이 본인에게도 발생. 보정 #2 결정의 결정타로 작동.

학습: PDF 같은 디자인 영역에서 "기존과 동일하게" 사양일 때 자체 미적 판단
자제 필요. 룰 등재로 명문화.

### 2. 검증 표 룰 본 세션도 사고 발견 효과

게스트 PDF STEP 02 페이지 시각 검증 시 4건 의외값 동시 발견. 검증 표 작성
없으면 운영자가 PDF 열어보고 발견했을 영역.

### 3. CC 이전 보고 재출력 패턴 (신규 관측 아님, 재발)

운영자 보정 #2 프롬프트 던졌는데 CC가 D 보고(ab59ab1) 그대로 재출력. 본인이
검증 표 형식 1줄 작성 직후 "CLAUDE.md 변경 내용이 이번 작업과 다름" 포착하고
운영자 확인 요청. 운영자 실수로 D 보고 재첨부 확인.

5/16 핸드오프 학습 #2와 동일 패턴. CC 외에 운영자 측 실수에서도 같은 검증
표 효과 작동.

### 4. 외부 자료 검색 자체 발동 사례 누적

게스트 PDF 폰트 다운로드 영역에서 외부 자료(`@fontsource` npm 패키지) 검색·
활용. 첫 시도(google/fonts 저장소) 실패 후 즉시 다른 경로 검토. 5/15 native
-capture 세션 학습이 본 세션에 자연스럽게 적용.

### 5. 비개발자 운영자 컨텍스트 — 경로 표기 사고

본인이 작업 결과 보고에 컨테이너 내부 경로(`/home/claude/...`) 그대로 노출.
운영자가 "이 폴더가 어디 있냐"고 물어본 시점에 사고 발견.

학습: 본인 작업 영역 경로와 운영자 안내 영역 경로 분리. 운영자에는 "위 PDF
카드 다운로드" 같은 동작 안내만.

---

## 보정 후보 누적 큐 (다음 세션 작업 후보)

본 세션 처리 2건 (#2 단편 단서 룰 등재 + #3·#4 합쳐 D 룰 등재).

| # | 항목 | 출처 | 상태 |
|---|------|------|------|
| ~~2~~ | ~~단편 단서 패턴 5회 도달 → CLAUDE.md 등재~~ | 직전 세션 | **본 세션 처리 (f68b4c0)** |
| ~~3·4~~ | ~~CC·본인 자체 판단 영역 확장 패턴~~ | 5/16·본 세션 | **본 세션 처리 (ab59ab1)** |
| **1** | **globals.css 전체 재점검 + PROJECT.md 디자인 토큰 섹션 완전 동기화 (`--accent-bright: #e8a038` 추가 + 기타 누락 토큰)** | 5/15 이전 세션 CC 정찰 | **잔여 (다음 세션 진입점 후보)** |

---

## 미해결 — 다음 세션 진입점

### 다음 세션 작업 후보 (본인 추천 우선순위)

| 순위 | 후보 | 비고 |
|------|------|------|
| 1 | **보정 #1 globals.css 토큰 동기화** | CC 영역. 정찰부터 시작. 단발성. 가장 명확한 다음 작업. |
| 2 | **iOS 필드테스트 결과 회고** | 운영자 트리거. 양쪽 OS 검증 완료 시 사양 시리즈 완전 종료. 결정타 시그니처: 해상도 폰 네이티브 / H.264 High / AAC / rotation 있음 / 10~17Mbps. |
| 3 | **합본 PDF (15p)** | 본인 영역. 게스트 5p + 호스트 8p 머지 + 합본용 표지·차례. 본 세션 PDF 컨텍스트 흐려진 다음 세션엔 무게 더 큼. iOS 필드테스트 결과 후 PDF 스크린샷 갱신과 묶을 가능성. |
| 4 | **사양 C (Shotstack length) 확정 여부 검증** | `src/lib/shotstack.ts` 첨부 + 운영자 메모 필요. 모르는 영역. |

### 거론 보류 큐 (이전 세션 계승)

- 재렌더 UX 갭 (done 상태 버튼 미노출)
- 완성본 단일 필드 덮어쓰기 → 서브컬렉션 전환 (D2)
- 클립 메타데이터 저장 실패 (S3 고아 파일)
- 호스트 클립 제거 시 영상 미리보기 흐름 검증
- 영상 호스팅 CDN 이전 (R2 vs Shotstack)
- 미성년자 영상 법적 리스크
- 네이버 메일 도달성
- BGM 다양성 격상
- 야외 환경 음량 검증

### 보류 큐 (운영자 트리거 대기)

- Track 1-B 카메라 광각 픽스 (iPhone 실기기 디버그) — iOS 필드테스트에 통합
  가능성
- Track 4 작은 버튼 3건 부담 가능 (실전 후 평가)
- Track 5 빈 텍스트 의도적 비우기 불가 (YAGNI 보류)

### 예정된 작업 트리거

- AWS 무료 트라이얼 만료 30일 전 (~2026-09-28)

---

## 다음 세션 시작 시 운영자 작업 — 5개 셋트

진입점: **보정 #1 globals.css 토큰 동기화** (또는 운영자 트리거에 따라 iOS
필드테스트 회고).

- CLAUDE.md (자동 첨부)
- AGENTS.md (자동 첨부)
- 본 핸드오프 (`2026-05-17-pdf-and-rules.md`) ← 운영자 직접 첨부
- DECISIONS.md 인덱스 (자동 첨부)
- known-issues.md (자동 첨부)

### 진입 시나리오별 추가 필요 자료

| 진입 시나리오 | 추가 첨부 |
|--------------|----------|
| 보정 #1 globals.css 토큰 | `src/app/globals.css` (정찰 단계에선 CC가 직접 봄, 운영자는 추가 첨부 불필요) |
| iOS 필드테스트 회고 | 운영자 관찰 메모 (자유 형식) + 가능하면 원본 영상 파일 1개 (결정타 시그니처 검증용) |
| 합본 PDF | 본 세션 산출 PDF 2개 (`congre-guest-guide.pdf`, `congre-host-guide.pdf`) + 기존 합본 PDF (`congre-user-guide.pdf`) |
| 사양 C 검증 | `src/lib/shotstack.ts` |

**첫 메시지 한 줄**:

"본 핸드오프 외엔 모르는 것으로 간주. 진단·결정 시 모르는 영역이면 명시.
CC 보고 받으면 검증 표 먼저. 이번 세션 진입점은 [영역명]."

### 다음 세션 채팅 클로드 본인용 노트

- **보정 #1 globals.css 작업 시 정찰 필수**. 본 세션 채팅 클로드가 토큰 정의
  본문 직접 못 봤음. 5/15 이전 세션 CC가 정찰했다는 사실만 컨텍스트에 있음.
  사양 작성 전 CC 정찰 프롬프트로 globals.css 전체 토큰 + PROJECT.md 디자인
  토큰 섹션 1:1 비교 받기. 정찰 없이 사양 작성하면 단편 단서 사례 누적.
- **CLAUDE.md 룰 2건 본 세션 등재 (ab59ab1, f68b4c0)**. 본인 작업 흐름에 즉시
  적용 — 호스트 FAQ 1단 복원 시 "더 나아 보이는 미적 변경" 자체 판단 자제,
  CC push 보고 압축 표기 즉시 포착. 룰 의식 유지.
- **PDF 작업 다음 세션 이어갈 경우** — 본 세션 컨테이너 임시 디렉토리 모두
  소실. ReportLab 코드(`pdf_common.py`, `build_guest.py`, `build_host.py`)
  재작성 필요. 운영자가 본 세션에서 다운받은 PDF 2개 + 기존 합본 PDF 첨부하면
  본인이 디자인 토큰·레이아웃 복원해 새 코드 작성 가능.
- **컨테이너 경로 표기 사고 재발 방지**: 운영자 안내에는 `/home/claude/...`
  같은 내부 경로 노출 금지. "위 PDF 카드 다운로드" 같은 동작 안내만.
- **검증 표 룰 본 세션 두 영역에서 사고 발견 효과 입증**: PDF 의외값 4건 +
  CC 이전 보고 재출력 패턴. 룰 유지.
- **PROJECT.md "완료된 기능" 섹션 정찰 필수** (5/16 본인용 노트 계승). 본
  세션엔 직접 사용 없었으나 보정 #1 작업 시 PROJECT.md 디자인 토큰 섹션이
  정찰 대상.
