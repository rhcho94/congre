# 2026-06-22 핸드오프 — 피클볼 에이전트 이식 + L10 OG 정찰/처방 + 운영개선 리서치

## 한 줄 요약
별도 프로젝트(피클볼)에 Congre의 4-에이전트 시스템 이식 완료(피클볼 레포에서 커밋·push). Congre 쪽은 L10 OG 프록시 라나 발견 3건을 scout로 실측해 가르고(②폐기·①실재·③실재) 라나 외부리서치로 처방 두 갈래(패턴 A/B)를 known-issues에 박음 — 지금 안 고침 결정. 마지막에 "세션 연속성·효율 개선" 라나 리서치까지 받아 다음 세션 적용 후보로 정리.

## 본 세션 커밋 (Congre 레포, push 완료)
- 6c72208 docs(known-issues): L10에 OG 프록시 처방 두 갈래(패턴 A/B) 추가 (1 file, +12)
  - 그 전에 한 번 잘못된 전체 교체를 했다가 git checkout으로 원복 후, 옛 6/19 블록 보존 + 처방만 append하는 방식으로 다시 함 (아래 사고 참조).

## L10 OG 프록시 — 실측 결과 (이게 본 트랙 핵심)
대상: src/app/api/og-image/[eventId]/route.ts. 라나 외부표준 지적 3건을 scout 실측으로 가름:
- ② Cache-Control 누락 → 폐기. route.ts:58–61에 24h 캐싱 이미 존재(6/19 커밋 418761f로 추가 완료된 것). 라나 "추정"이 빗나감.
- ① 전체 버퍼링 413 → 실재. route.ts:55 transformToByteArray() 원본 통째 적재, 크기 가드 없음. intro 업로드는 클라(page.tsx:615–617) 10MB 가드뿐, presign 서버측(api/upload/presign/route.ts:116–123) 상한 없음. 가공 없이 원본 서빙 → 4.5MB↑면 413.
- ③ fallback 302 → 실재. route.ts:8 Response.redirect(FALLBACK_URL, 302) 여전. 9ebf887은 FALLBACK_URL 상수만 바꿨지 302 구조 그대로(6/19 git 이력 정찰로 확정됨).

## L10 처방 두 갈래 (라나 리서치, 미실행 — 격상 트리거 대기)
①·③ 상충: 패턴 B는 302를 수단으로 써서 ①을 풀어도 ③ 미해결.
- 패턴 B (presigned 302): 의존성 0(기존 getSignedUrl). ① 해결. 단 OG 크롤러 302 추적 미검증(추측), 버킷 노출·캐시 충돌 리스크. ③ 미해결.
- 패턴 A (sharp 리사이즈): ①+③ 동시 해결 가능. 단 sharp가 Vercel linux-x64 런타임 에러 반복 재발(2025년까지), 0.34.5 핀닝 우회, 번들 +15MB. 비개발자 디버깅 불리.
- @vercel/og: 텍스트→이미지 생성용이라 부적합(제외).
- 결정(Ray 승인): 지금 안 고침. 트리거 미도래(FGT 전, 주 채널 카카오) + 두 해법 다 미검증 리스크. 격상 트리거 = FGT/출시 후 OG 카드 채널(인스타·페북·슬랙)에서 "미리보기 안 뜬다" 신고 시.

## 피클볼 에이전트 이식 (별도 레포 — 여기 핸드오프엔 기록만, 상세는 피클볼 handoff-135)
- Congre의 lana/scout/eye/axe 4개를 피클볼(React+Vite+Supabase)에 이식. 이름 유지, 프로젝트명만 스왑, baseline은 피클볼 실측값 0/4. 피클볼 레포 커밋 329dbb1, push 완료. 시운전 3명 합격.
- 발견: .claude/가 글로벌 gitignore(C:\Users\PC\.gitignore_global:2)에 막혀 있었음. 로컬 .gitignore negation 2줄로 해소. → 다른 프로젝트에서 .claude/ 커밋 안 되면 글로벌 gitignore 1순위 의심.

## 세션 연속성·효율 개선 리서치 (라나, 다음 세션 적용 후보)
라나 보고는 "기능 존재 신호"일 뿐 — 구체 수치·기능명(200줄 상한, Auto Memory 버전, memory:project 등)은 적용 전 공식 문서 재확인 필수(라나 자체 경고).
- 다음 세션 1순위 (싸고 효과 즉각): ① 4명 description을 영어 + "Use proactively when…" 패턴으로 재작성, ② CLAUDE.md에 에이전트 라우팅표("외부 라이브러리→lana, 내부 코드→scout…") 추가. → 매번 이름 호출하던 걸 자동 위임으로. (CC는 명시 안 하면 서브에이전트 거의 자동 호출 안 함 — 공식 확인)
- 검토 후보: SessionStart 훅으로 최신 핸드오프 자동 로딩 — CC 연속성엔 효과 크나 채팅 코난엔 안 닿음(훅은 CC 터미널 전용). 비개발자 셋업 난도 높음. 도입 전 Windows 동작 공식 검증 필요.
- 보류: Auto memory/서브에이전트 memory(기능명 미검증), CLAUDE.md 다이어트(코난 소관 별도 트랙).

## 본 세션 사고 + 학습
- 사고: L10 갱신을 낡은 자동첨부본(미검증 상태)으로 출발 → 6/19에 이미 정확히 갱신돼 있던 블록을 부정확하게 덮을 뻔. 커밋 전 발견해 원복.
- 학습 1 (핵심): 자동첨부 docs도 git 최신이 아닐 수 있다. "code = 1차 소스" 룰을 docs에도 적용 — 문서 갱신 트랙은 시작 전 해당 블록 현재 상태를 CC로 실측하고 출발. (Project Knowledge ≠ git 자동 동기화)
- 학습 2: 긴 터미널 출력은 (ctrl+o to expand)로 접힘 — 프롬프트로 잘 안 풀림. 긴 diff 확인은 처음부터 Ctrl+O 펼침 안내해 왕복 줄이기.
- 학습 3: 파이프라인 풀코스(라나→정찰관→승인→실행관→검증관) Congre 첫 실전. 잘 돌았고, axe가 글로벌 gitignore 막힘·덮어쓰기 위험에서 멈추고 물어본 게 사고 방지.

## 미완 작업
- 없음. (L10은 결정 완료, 코드 변경 의도적 보류.)

## 다음 세션 후보 (우선순위)
1. (中) 에이전트 자동위임 개선 — description 영어화 + CLAUDE.md 라우팅표. 싸고 효과 즉각.
2. (中) Gmail SPF alignment — FGT 침투 마케팅(2단계) 전. 네이버 정상화 경위 규명도 이때.
3. (低·검토) SessionStart 훅 도입 여부 — 공식 문서 검증 후 결정.
4. (대기) FGT 실행 결과 대응 — Ray 카톡 발송 후 막힌 지점 들고 오면.
