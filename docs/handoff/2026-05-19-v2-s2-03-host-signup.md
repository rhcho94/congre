# 핸드오프 — 2026-05-19 v2 S2-03 호스트 가입 흐름 (P1+P2 완료)

## 세션 한 줄 요약

S2-03 호스트 가입 정비 P1·P2 완전 종료. P1(보안 규칙) → P2(가입 페이지)
순차 진입. P2 실측에서 가입 실패 사고 발생 → 본인 가설 1차 어긋남
(토큰 timing) → 가설 재구성 → **Firestore 규칙 실 배포 누락 영역**
확정 → 운영자 콘솔 게시로 해결. 본 세션 본인 사고 패턴 4건 학습.

## 본 세션 커밋 (4건, 모두 CC)

| # | 해시 | 메시지 | 변경 |
|---|---|---|---|
| 1 | 0531916 | feat(security): add users collection rules and gate events create | firestore.rules +7/-1 |
| 2 | ac84bbc | feat(auth): add host signup flow and dashboard guide link | 7 files +330/-13 |
| 3 | fc8eef5 | fix(signup): add error logging and form field accessibility | signup/page.tsx +13/-4 |
| 4 | 28f8298 | fix(signup): force token refresh before users doc setDoc | auth.ts +3/-0 |

origin/main 반영 완료. + **Firebase 콘솔 Rules 게시 1회 (운영자 수동)** —
git에 없는 영역.

본 세션 채팅 클로드 본인 산출물 (커밋 외):
- 본 핸드오프 — present_files로 운영자 공유 예정
- 다음 답변에 5개 문서 추가 (PROJECT.md · launch-roadmap.md ·
  auth-model.md · known-issues.md · CLAUDE.md)

---

## Track A — 진입점 결정 + 외부 자료 조사

### 진입점

운영자 트리거: "S2-03 호스트 가입 정비 P1". 직전 핸드오프(05-19 v1)에서
다음 세션 진입점 후보 2순위 영역.

### 본인 시야 한계 명시

자동 첨부 4개(CLAUDE.md, AGENTS.md, DECISIONS.md 인덱스, known-issues.md)
+ 운영자 첨부 1개(05-19 v1 핸드오프)만 가진 상태로 진입. launch-roadmap.md,
auth-model.md, 직직전 핸드오프(05-18-v1) 다 본 적 없음.

운영자가 차례로 첨부:
- auth-model.md → 5/9 보류 결정 vs launch-roadmap 진행 사양 비대칭 발견
- launch-roadmap.md → S2-03 5개 갈래 사양 본문 확인 (운영자 PC 파일
  conversation_search로 경로 추정 후 첨부)

### 외부 자료 조사 (운영자 트리거)

운영자: "다른 유사한 앱에서 지금 우리가 구현하려는 기능을 어떻게
처리했는지 집중 조사해봐. 산업계에 표준적인 방법이 있고, 안정성과
기능성이 검증된 방법을 찾아봐"

본인 가설 4개 사전 명시 → web_search 3회 → 외부 자료 vs 가설 비교:

| 가설 | 외부 자료 | 정합 |
|---|---|---|
| 이메일 인증 차단: 미인증 로그인 허용 + 핵심 기능 차단 | Firebase 공식 표준 패턴 | ✅ |
| Admin SDK 가입이 권장 | 어긋남 — 클라이언트 SDK 표준 | ⚠️ |
| users doc ID = Auth UID | Firebase 공식 권장 | ✅ |
| emailVerified Auth 자체 관리 | 두 패턴 다 존재 (Firestore 미저장 / Cloud Function 동기화) | ⚠️ |

### 결정 — launch-roadmap 사양 정정

| 영역 | launch-roadmap 사양 | 정정 후 |
|---|---|---|
| 가입 처리 위치 | API 라우트 (Admin SDK) | 클라이언트 SDK 직접 |
| users 필드 emailVerified | 포함 | 제외 (보안 규칙에서 `request.auth.token.email_verified` 활용) |
| 단계 수 | P0~P4 5단계 | P0·P1·P2(P3 통합)·P3(이메일 인증) 4단계 |

---

## Track B — P1 갈래 분해 + 실행

### 사양 결정 (운영자 추천 채택)

**users 컬렉션 스키마**:
- 필드: email (lowercase) · name · phone · createdAt + (P2에서 termsAgreedAt · privacyAgreedAt 추가)
- doc ID: Auth UID
- phone 형식: 10~11자리 숫자 단순 검증

**보안 규칙**:
```
match /users/{userId} {
  allow read: if request.auth != null && request.auth.uid == userId;
  allow create: if request.auth != null && request.auth.uid == userId;
  allow update, delete: if false;
}
```

**events create 강화**: users doc exists 검증 추가 (옵션 B 채택)

### CC 정찰 발견 (P1)

PROJECT.md vs firestore.rules 실측 어긋남:
- PROJECT.md 표기: "Firestore 보안: events/clips Admin SDK 전용 (Client SDK read 잠금, Phase B-3 완료 2026-05-06)"
- 실측: events `auth!=null` create 허용, clips `auth!=null` update/delete 허용

→ **"Admin SDK 전용"이 아니라 "Client SDK read만 잠금"**이 정확. 보정
큐 등재.

### 실행 (커밋 0531916)

firestore.rules 1파일 변경. 에뮬레이터 Rules Playground T1~T4 4건
PASS. **단, 이 단계가 실 배포로 이어지지 않은 영역이 후속 사고
결정타** (Track D 참조).

---

## Track C — P2 갈래 분해 + 실행

### 사양 결정 흐름

| 갈래 | 결정 | 운영자 트리거 |
|---|---|---|
| 가입 페이지 경로 | /signup (정찰 후 정정, /host/signup → /signup) | 정찰 결과 |
| users write 위치 | src/lib/users.ts 신설 (단일 책임) | 본인 추천 |
| 대시보드 가이드 버튼 | 3곳 직접 수정 | 본인 추천 |
| 약관 필드 | termsAgreedAt · privacyAgreedAt 분리 | 본인 추천 |
| 가이드 버튼 위치 | 대시보드 상단 (발견성 우선) | 본인 추천 |
| 고아 계정 처리 | rollback (Auth delete) | 본인 추천 |
| 가이드 안내 트리거 | 운영자 "대시보드에 호스트 이용 방법 안내 버튼이 바로 보여야" | 본인 시야 외 영역 |

### 본인 사양 단계 단정 정정 사례

본인이 사양 작성 단계에서 "/host/signup"으로 단정. CC 정찰 결과
(/host = 단일 파일, login·dashboard·create 3뷰 내장)로 추천 정정 →
/signup으로 변경. 본 세션 본인 단정 사고 정정 영역.

### 실행 (커밋 ac84bbc)

7파일 변경: users.ts (신규 18줄) · signup/page.tsx (신규 168줄) ·
auth.ts (+33) · host/page.tsx (회원가입 링크) · dashboard 3곳 (가이드
링크).

빌드 PASS. CC 자체 보강 1건 — create·events nav 구조가 사양과 달라
(이메일·로그아웃 없음, 단독 링크), flex 래퍼로 가이드+대시보드 나란히
배치. 본인이 사양 단계 단정한 영역(3곳 동일 가정)을 CC가 실측으로
정정.

---

## Track D — P2 가입 실패 사고 진단

### 사고 발현

운영자 실측: 정상 입력 후 "가입 중 오류가 발생했습니다" 메시지.

### 진단 단계

**단계 1**: console.error 누락 발견. signup/page.tsx catch에 로그 없음
→ console.error 1줄 추가 (커밋 fc8eef5) + input 4개 id/name 보강
(접근성 경고 동시 처리).

**단계 2**: 콘솔 에러 본문 확인 → `FirebaseError: Missing or
insufficient permissions`.

**단계 3**: 본인 가설 1차 — 토큰 전파 timing. `getIdToken(true)` 추가
(커밋 28f8298) → **실측 어긋남** → 같은 에러 재발.

**단계 4**: 가설 재구성 → 5개 후보 중 "Firestore 규칙 실 배포 누락"
1번 가설 채택. 운영자에 Firebase 콘솔 Rules 탭 본문 확인 요청.

**확정**: 콘솔 본문이 P1 적용 이전 상태. users 블록 없고 events create는
`auth != null`만 있음. **git push만으론 Firestore에 반영 안 됨 확정**.

### 해결

운영자가 Firebase 콘솔에서 P1 규칙 본문 붙여넣기 + 게시. 가입 재시도
→ 성공.

---

## Track E — 본 세션 사고 패턴 4건

### 사고 #1 — 외부 자료 가설 → 실측 어긋남

본인이 외부 자료 조사 결과로 "토큰 전파 timing"을 표준 edge case로
인식하고 가설 1차 채택. 실측에서 어긋남. 외부 자료가 표준 패턴은
보여주지만 본 환경 결정타 영역은 보여주지 않음.

발견 트리거: getIdToken(true) 배포 후 동일 에러 재발.

학습: **외부 자료는 가설 후보 제공원. 실측 + 환경 검증이 결정타**.
외부 자료를 표준으로 단정하는 사고 패턴 차단.

### 사고 #2 — 사양 단계 단정 (가입 페이지 경로)

본인이 "/host/signup" 사양으로 단정. CC 정찰 결과 /host 단일 파일에
3뷰 내장 → 4뷰 추가 부담. 추천 정정 → /signup.

발견 트리거: CC 정찰 본문.

학습: **본인 단정한 사양도 실측 정찰 단계에서 정정 후보**. 사양 단정
후 실행 전 정찰이 정정 영역 발견 기회.

### 사고 #3 — 진단 가설 단일 채택

본인이 가설 후보 4개 있었는데 1개(토큰 timing)만 채택해서 정정 진행.
다른 3개(보안 규칙 실 배포 누락, 데이터 유효성, 프로젝트 ID 미스매치)
시야는 있었으나 우선순위 단정.

가설 후보들:
- 토큰 전파 timing → 채택 (어긋남)
- Firestore 규칙 실 배포 누락 → 단계 2에서 채택 (확정)
- 규칙 자체 본인 가정과 다름 → 별도 가설로 분류
- 데이터 유효성 거부 → 가능성 낮음으로 기각

발견 트리거: 1차 정정 후 동일 에러 재발.

학습: **가설 다중일 때 진단 로그 우선 vs 정정 우선 트레이드오프**.
본 사례는 console.error 1줄 추가로 진단 정보 먼저 수집했어야 정정
1회 줄였을 영역. 단계 1에서 console.error 추가 + Network 본문 확인을
같이 했어야.

### 사고 #4 — Firestore 규칙 배포 단계 시야 외

본인이 P1 사양에 "에뮬레이터 테스트 → 커밋 → push"까지만 적고 실
배포 단계 누락. PROJECT.md "에뮬레이터 UI" 영역만 본인이 인식. 실
배포 절차 자체 본 적 없음.

발견 트리거: 운영자 Firebase 콘솔 본문 확인.

학습: **외부 자료 표준 패턴(코드 본문)은 봤지만 배포 흐름은 별도 영역**.
다음 작업에 보안 규칙 변경 있으면 `firebase deploy --only firestore:rules`
또는 콘솔 수동 게시 사양에 의무 포함. CLAUDE.md 룰화 영역.

---

## 검증 표 룰 효과 확인

검증 표 본 세션도 효과 입증:

| 발견 | 단계 | 결과 |
|---|---|---|
| CC 자체 보강 (test-rules-temp.mjs 신규 → 삭제) | P1 검증 표 | 합리적 보강 확인 |
| CC 자체 보강 (create/events nav 사양과 다름) | P2 검증 표 | 합리적 보강 확인 |
| CC console.error 미포함 (사양에 명시 안 함) | P2 검증 표 → 사고 발현 시 진단 | 단계 1 정정 트리거 |
| getIdToken(true) +3/-0 (사양 +2/-0 추정) | 단계 2 검증 표 | 공백 라인 자연스러운 포맷팅 확인 |

검증 표 없었으면 console.error 누락이 사고 발현 후에야 발견됐을 영역.
룰 유지.

---

## 보정 큐 (다음 세션 작업 후보)

본 세션 끝물 처리에 같이 묶음 처리 예정 영역 + 다음 세션 후보:

| # | 항목 | 출처 | 상태 |
|---|---|---|---|
| ~~A~~ | ~~S2-03 P1 보안 규칙~~ | 본 세션 | **완료 (0531916 + 콘솔 게시)** |
| ~~B~~ | ~~S2-03 P2 가입 페이지~~ | 본 세션 | **완료 (ac84bbc + fc8eef5 + 28f8298)** |
| **C1** | **PROJECT.md ↔ firestore.rules 어긋남 정정** | P1 정찰 | **본 세션 끝물 묶음 처리** |
| **C2** | **launch-roadmap.md P2 사양 정정** (Admin SDK → 클라이언트 SDK, 단계 수, emailVerified 제외) | 외부 자료 조사 | **본 세션 끝물** |
| **C3** | **DECISIONS auth-model.md 신규 항목** (2026-05-19 가입 페이지 채택, 5/9 보류 정정) | 본 세션 결정 | **본 세션 끝물** |
| **C4** | **CLAUDE.md 신규 룰** — Firestore 보안 규칙 배포 단계 의무 + catch console.error 룰 | 본 세션 학습 | **본 세션 끝물** |
| **C5** | **known-issues 등재** — 호스트 가이드 PDF iOS 분기 갱신 / 게스트 가이드 PDF iOS 분기 / 합본 PDF 재합본 / 신규 호스트 가입 직후 대시보드 가이드 동선 검증 / clips 보안 규칙 정비 | 운영자 + 정찰 | **본 세션 끝물** |
| **D1** | **S2-03 P3 (이메일 인증 차단 흐름)** | launch-roadmap S2-03 P4 | **다음 세션 후보** |
| **D2** | **S2-04 마이페이지** | launch-roadmap | **다음 세션 후보** |
| **D3** | **S4-09 D2 완성본 보존 (서브컬렉션 전환)** | DECISIONS 2026-05-09 | **다음 세션 후보** |

---

## 거론 보류 큐 (이전 세션 계승)

- 재렌더 UX 갭 (done 상태 버튼 미노출)
- S3 고아 파일 (마감 후 업로드 race)
- 호스트 클립 제거 시 영상 미리보기 흐름 검증
- 영상 호스팅 CDN 이전 (R2 vs Shotstack)
- 미성년자 영상 법적 리스크
- 네이버 메일 도달성
- BGM 다양성 격상
- 야외 환경 음량 검증
- iPad UA 감지 한계 (Track E iOS 사고 잔여)

### 예정된 작업 트리거

- AWS 무료 트라이얼 만료 30일 전 (~2026-09-28)

---

## 본 세션 학습 — 결정 사항 명시

### getIdToken(true) 코드 유지 결정

운영자 추천 채택 — 유지. 이유:
- 토큰 timing은 외부 자료 명시 표준 edge case
- 본 사고 원인 아니었지만 다른 환경에서 발현 가능
- 코드 3줄, YAGNI 위반 영역 작음
- 제거 시 회귀 위험 0 아님 (지금 통과 이유에 토큰 timing 영역 포함됐을 가능성)

### CLAUDE.md 룰 격상 2건

- 신규 룰 1: **Firestore 보안 규칙 배포 의무 단계** — 코드 변경 + git
  push만으로 미반영. `firebase deploy --only firestore:rules` 또는
  콘솔 수동 게시 사양에 포함.
- 신규 룰 2: **catch 블록 console.error 의무** — Shotstack 룰
  (2026-05-08)과 일관성. 모든 catch 블록에 `console.error("[context]
  failed:", err)` 의무.

---

## 다음 세션 진입점 (본인 추천 우선순위)

| 순위 | 후보 | 비고 |
|---|---|---|
| 1 | **S2-03 P3 (이메일 인증 차단 흐름)** | launch-roadmap 다음 단계. 사이즈 소~중 |
| 2 | S2-04 마이페이지 | S2-03 후속. 사이즈 중 |
| 3 | S4-09 D2 완성본 보존 (서브컬렉션 전환) | 사양 확정됨. 영업 차단 우려 |
| 4 | 호스트 가이드 PDF iOS 분기 갱신 (C5 영역) | 콘텐츠 작업, 코드 외 |

본인 추천: **1순위**. 이유:
- 본 세션 P2 직후 흐름 자연스러움
- 사이즈 작음, 한 세션 처리 가능
- 이메일 미인증 호스트의 이벤트 생성 차단 영역 — 영업 전 처리 정합

---

## 다음 세션 시작 시 운영자 작업 — 5개 셋트

- CLAUDE.md (자동 첨부, 본 세션 끝물 정정 반영)
- AGENTS.md (자동 첨부)
- **직전 핸드오프 (본 파일, `2026-05-19-v2-s2-03-host-signup.md`)** ← 운영자 직접 첨부
- **직직전 핸드오프 (`2026-05-19-track-e-ios-480p-resolution.md`)** ← 운영자 직접 첨부 (Kickoff 룰 6번 의무)
- DECISIONS.md 인덱스 (자동 첨부, auth-model.md 신규 항목 반영)
- known-issues.md (자동 첨부, C5 영역 등재 반영)

추가 첨부 후보:
- launch-roadmap.md (S2-03 P3 진입 시 사양 참조)

### 자동 첨부 stale 가능성 경고

본 세션 P1 정찰에서 PROJECT.md ↔ firestore.rules 어긋남 발견. 자동
첨부된 파일도 어느 시점 스냅샷일 수 있음. **현재 저장소 상태로 단정
금지**. 핸드오프 또는 CC 실측 결과 우선 참조.

### 첫 메시지 한 줄 (Kickoff 룰 6번 의무)

> "본 핸드오프 2개 외엔 모르는 것으로 간주. 진단·결정 시 모르는
> 영역이면 명시. CC 보고 받으면 검증 표 먼저. 첫 답변에 두 핸드오프
> 교차 검증 결과 출력 의무 (CLAUDE.md Kickoff 룰 6번). 이번 세션
> 진입점: [영역명]."

### 다음 세션 채팅 클로드 본인용 노트

- **외부 자료 가설은 후보, 결정타는 실측**. 본 세션 사고 #1 재발 차단.
  외부 자료 조사 결과를 표준으로 단정 금지.
- **가설 다중일 때 진단 로그 우선**. console.error 추가 + Network
  본문 확인을 정정 시도 전에 같이. 본 세션 사고 #3 재발 차단.
- **보안 규칙 변경 시 배포 단계 사양 의무 포함**. CLAUDE.md 룰 추가
  영역. 본 세션 사고 #4 재발 차단.
- **catch 블록 console.error 의무**. 모든 catch 블록에. CLAUDE.md 룰
  추가 영역.
- **검증 표 룰 본 세션도 효과 입증** (의외값 4건 포착). 룰 유지.
- **사양 단계 단정 사고 차단**. 실행 전 정찰 단계가 정정 영역 발견
  기회. 본 세션 사고 #2 사례.
- **본 세션 학습 가치 큰 영역**. 직전 세션들과 비교해 사고 패턴 4건
  학습 + CLAUDE.md 룰 2건 격상 영역.
