# 2026-08-16 (2) 핸드오프 — 문서 사이클

## 한 줄 요약
이월돼 있던 문서 부채를 전부 닫았다. 커밋 5건, 원격 반영 완료.
**코드 변경 0건** — `src/` 는 이 세션에서 한 줄도 건드리지 않았다.

---

## 이번 세션 커밋 (5건, 전부 push 완료)

| 해시 | 내용 |
|---|---|
| `d00258c` | docs(changelog): 2026-08-16 재렌더 유료화 4건 + 8/15 `c29efdc` 누락분 등재 (1 file, +38) |
| `68ecfbe` | docs(known-issues): 후속 7건 등재 + 해소 1건 이동 + 기존 2건 갱신 (2 files, +75 −11) |
| `6bdca8a` | docs(decisions): 재렌더 유료화 결정 14건 등재 + 토스 2차 회신 제출 결정 (5 files, +80 −2) |
| `b6f7263` | docs(handoff): 2026-08-15 토스 제출 · 2026-08-16 재렌더 유료화 세션 (2 files, +489) |
| `8236e1f` | docs(claude-md): 학습 룰 8건 승격 (1 file, +8) |

`325600c..8236e1f` push 완료. 로컬·원격 동기, 워킹 트리 깨끗.
lint baseline 12 errors / 3 warnings, delta 0 유지 (문서 변경이라 코드 지표 무영향).

---

## 처리된 이월 항목

| # | 내용 | 결과 |
|---|---|---|
| F-1 | 가입 문구 항목 → `known-issues-resolved.md` 이동 | 완료 (`68ecfbe`) |
| F-2 | CHANGELOG 8/15 절에 `c29efdc` 추가 | 완료 (`d00258c`) |
| F-3 | 통신판매업·유선전화 미기재 제출 결정 등재 | 완료 (`6bdca8a`, legal 결정 4) |
| F-4 | 토스 가이드 화면 요건 등재 | 완료 (`6bdca8a`, legal 결정 5) |
| F-5 | 8/15(2) 핸드오프 커밋 | 완료 (`b6f7263`) |
| F-6 | 사업자 정보 2파일 하드코딩 known-issue 등재 | 완료 (`68ecfbe`) |
| F-7 | 8/14 legal 결정 1 전제 변경 각주 | 완료 (`6bdca8a`) — 이 세션에서 새로 발견 |

**F-7 경위**: `legal.md` 2026-08-14 결정 1의 근거 문단이 "재렌더가 confirm을
거치지 않아 이 경로가 원천적으로 없다"였는데, `8290dbe`로 재렌더가
`payment/confirm` 을 거치게 되면서 무효화됐다. 결정 자체는 유지되므로
본문을 고치지 않고 각주 한 줄을 붙였다.

---

## 등재 내역

### decisions — 2분산 확정
결정 배치는 영역 4분산이 아니라 **2분산**으로 결정했다(Ray 판단).
사양 결정을 한 곳에서 통째로 읽을 수 있는 응집성을 택했고,
분산 시 검색성 손실은 포인터로 보완했다.

- **`market-product.md`** — `## 2026-08-16 — 재렌더 유료화 사양`, 결정 9건
  (금액 80% 산출 / 기준은 최초 결제액 고정 / mode 서버 자동 판정 /
  `firstPaidAmount` 기준값 / rerender confirm 쓰기 범위 / 409 first 한정 /
  클립 가드 / 토글 비활성 조건 / `participantNotifiedAt` 리셋 제거)
- **`legal.md`** — `## 2026-08-16 — 재렌더 유료화 약관 정렬 + 토스 2차 회신 제출`,
  결정 5건 (환불 시계 갱신 / 모달 고지 / 48시간 창 유지 /
  통신판매업·유선전화 미보유 제출 / 토스 화면 요건)
- **포인터 1줄씩** — `data-flow.md:5`, `rendering.md:5` 에
  market-product 2026-08-16 항목을 가리키는 블록쿼트. 날짜 헤더 아님.
- **인덱스** — market-product 17→18, legal 10→11.

**인덱스 숫자의 단위는 날짜 헤더 수**로 실측 확정됐다(개별 결정 수가 아니다).
게이트에서 9개 영역 전수 대조: notifications 8 / auth-model 6 / infra 6 /
landing 21 / misc 7 / rendering 41 / data-flow 22 / market-product 18 / legal 11.
**기존 드리프트 0건.**

### known-issues — 신규 7건
N-1 클립 영구 잔존(`participantNotifiedAt` null + `done`) /
N-2 48시간 리터럴 2곳 축 혼동 / N-3 랜딩 pricing 기한 누락 /
N-4 용어 3종 드리프트 / N-5 재렌더 결제 서버 미강제 /
N-7 모달 금액 미표시 / F-6 사업자 정보 2파일 하드코딩.

- **N-6(재렌더 중복 결제)는 독립 항목으로 만들지 않고** 기존
  "prepare 새로고침 pending 누적" 항목에 재렌더 축으로 덧붙였다.
  같은 구조가 두 곳에 생기는 것을 피했다.
- **N-8(테스트 키 `TOO_MANY_REQUESTS`)은 등재하지 않았다.** 초안 검토 중
  누락된 것으로 보이며, 실키 전환 후 자연 해소 예상 항목이다.
  **다음 세션에서 등재 여부를 판단할 것.**
- **N-3 격상 트리거를 "토스 승인 회신 시점 — 실키 전환 전 필수"로 못 박았다.**
  랜딩은 git 외부 트랙이라 본 저장소에서 고칠 수 없다.
- 기존 refundStatus 항목은 격상 트리거(재렌더 유료화 착수)가 발동해
  재평가 결과로 갱신했다. 새 트리거는 "환불 흐름 재작업 시 / 실제 환불 발생 시".

### 게이트에서 확인된 코드 사실
- `cron/cleanup/route.ts:15` — 48시간 리터럴 존재, 라인 번호 일치
- `payment/confirm/route.ts:130` — 48시간 리터럴 존재, 라인 번호 일치
- `render/start/route.ts:52` — 결제 가드 `plan === "paid" && !unlocked` 확인
- `terms/page.tsx:367-372`, `privacy/page.tsx:438-447` — 사업자 정보 독립
  하드코딩 확인. 두 파일 모두 공유 상수·컴포넌트 import 없음
  (import 는 `BrandName`·`LANDING_URL`·`PageBackdrop` 뿐)

### CLAUDE.md 학습 룰 8건 승격
기존 서식 `- **제목**: 본문` 에 맞춰 등재. 승격 후 불릿 36개 전부 볼드 통일.
후보 7건에 이 세션에서 발생한 사고 1건을 더해 8건이 됐다.

---

## Project Knowledge 재업로드 대상 ★ 미처리

git 과 자동 동기되지 않으므로 **수동 재업로드가 필요하다.**
이걸 하지 않으면 다음 세션이 stale 사본을 읽는다.

| 파일 | 사유 |
|---|---|
| `CHANGELOG.md` | 8/16 절 + `c29efdc` |
| `known-issues.md` | 신규 7건 + 이동 + 갱신 2건 |
| `known-issues-resolved.md` | 가입 문구 항목 수신 |
| `DECISIONS.md` | 인덱스 18/11 |
| `decisions/market-product.md` | 결정 9건 |
| `decisions/legal.md` | 결정 5건 + 전제 각주 |
| `CLAUDE.md` | 학습 룰 8건 |
| (선택) `decisions/data-flow.md`·`rendering.md` | 포인터 1줄씩 |

---

## 다음 세션 작업 목록

### 1. 토스 회신 대응 (최우선, 수동 대기)
- 회신 예상 8/17, **하드 마감 8/24**
- 승인 시 → 결제위젯 전용 키 발급 요청 → 실키 전환
- **승인이 나도 국민카드는 열리지 않는다.** 통신판매업 신고 →
  국민카드 별도 심사가 남는다(legal 2026-08-16 결정 4).
  오픈 시 이 사실을 어떻게 고지할지 미결정.
- 보완 요청 가능성이 가장 높은 항목: 통신판매업·유선전화

### 2. 유료 개시 전 필수 (승인 회신 직후 착수)
- **`render_delayed` "30분" 문구** — 문자열 3곳. `refund50At` 이 결제 후
  4시간이 되면서 차이가 3시간 30분으로 커졌다. 고객에게 직접 발송되는 문구.
  바꿀 값 결정 필요.
- **랜딩 `pricing.html` 에 48시간 기한 추가** (N-3). git 외부 트랙,
  Vercel CLI 배포 필요.
- **409 재발행 경로** — API 수정 동반.

### 3. 판단 대기
- N-8(테스트 키 rate limit) 등재 여부
- 재렌더 백필 — `firstPaidAmount` 는 `8290dbe` 부터 기록되므로 그 이전
  결제 이벤트는 재렌더가 `MISSING_FIRST_AMOUNT` 로 막힌다.
  실고객 0이라 만들지 않았다(YAGNI). 실고객 유입 전이면 계속 불필요.

### 4. 격상 트리거 대기 중 (지금 손대지 않음)
N-1 클립 영구 잔존(발생 경로 정찰 선행 필요) / N-2 리터럴 상수 분리 /
N-4 용어 통일(약관 개정 동반) / N-5 재렌더 결제 서버 강제 /
N-7 모달 금액 표시 / F-6 사업자 정보 공유화(070 신청 시점)

---

## 이번 세션 학습

- **커밋을 미루면 `git diff` 는 그 사이 모든 턴의 누적이다.**
  "삭제선 0줄" 검증 조건을 두 번 걸었고 두 번 다 오탐이 났다. 원인은
  직전 턴 작업이 미커밋 상태로 diff 에 함께 잡힌 것. 턴 단위 검증은
  훅 범위나 예상 삭제 줄 수로 써야 한다.
  **두 번째 사고는 이 내용을 룰로 등재하는 턴에서 났다** — 룰을 문서에
  적는 것과 그 룰이 작업 절차에 반영되는 것은 별개다.

- **eye 가 근거 없는 주석을 2회 달았다.** "CLAUDE.md 에 lint baseline 11/3 이
  기재돼 있다"는 서술을 두 게이트에서 반복했으나, 실측 결과 CLAUDE.md 에
  `baseline`·`11 errors`·`12 errors` 문자열이 전부 0건이었다. 판정에는
  영향이 없었지만 게이트 보고에 미확인 사실이 섞인 사례다.
  세 번째 게이트에서 "출처를 추정하지 말라"고 명시하자 "확인 못 함"으로
  올바르게 남겼다.

- **axe 는 조건에 걸릴 때마다 정직하게 멈췄다.** 자기 작업이 아닌 변경을
  넘기지도, 덮지도 않았다. 멈춤 보고가 잘못된 조건 때문이었던 두 경우 모두
  axe 쪽 판단이 맞았다.

- **확정 문안에 손을 댄 곳 1건.** CLAUDE.md 볼드 서식 교체 시 마지막 2줄
  ("문서 형식이 안 맞으면", "선택지 라벨")은 원문이 한 문장이라
  `- **제목**: 본문` 형태가 성립하지 않아 본문을 새로 붙였다.
  원 사고 맥락에서 나온 내용이나, 확정 문안 수정에 해당하므로 기록해 둔다.

- **문서 정리는 삭제보다 되돌릴 수 있는 쪽을 택했다.** 토스 테스트 계정의
  이벤트 4건을 정리할지 논의했으나, PPT 제출 자료와의 불일치 위험과
  재렌더 재현 수단(유일하게 `firstPaidAmount` 를 가진 이벤트) 상실을 이유로
  손대지 않기로 했다. 심사 회신 대기 중에는 되돌릴 수 없는 조작을 하지 않는다.
